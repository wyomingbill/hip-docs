# HIP build state: built, built-not-integrated, planned

Status: CURRENT
Reconciled-Against: roadmap HEAD at landing time (2026-07-28/29); every
status line below verified directly against docs/INDEX.md, the named REQ
docs' own Status headers, and docs/deliverables/MANIFEST.md before
landing -- not copied from the draft unchecked.

UPDATED 2026-07-28 20:43 MT (D-08 close-out, same evening): the demo-cutover
consent-assertion rows below went stale minutes after landing — the D-03
build COMPLETED and Bill ruled REQ_DEMO_PREFLIGHT_CONSENT_ASSERTION MET on
the demo-cutover branch tonight (D-03c full green; close-out f971377,
D-03d). Rows carry inline [updated] marks; branch nuance stated there.

UPDATED 2026-07-29 (D-22): three rows refreshed against ruled state —
strip-context MET (D-20, 0e6ae00); partition table run 12/13 with row 3
deferred to clinical-ethics review (D-18 + D-21, e357005); consent-cutover
row confirmed already accurate (MET on demo-cutover f971377, unmerged).

UPDATED 2026-07-29 (D-31b): learner-signal isolation gate is now MET (REQ_LEARNER_SIGNAL_ISOLATION, Bill's D-31b ruling) — the D-30 provenance-authenticity fix passed --full (D-31), 6 D-25 holes closed, with one named DATA limit (members.household_id unpopulated -> production resolver fails closed on member facts). All three ContextArch preconditions (G0, prompt-fidelity, learner-isolation) now MET.

UPDATED 2026-07-30 (D-39): the D-31b MET above was PULLED BACK 2026-07-30 (D-37) and is now RE-MET, both same-day. An external review (Fable, D-35) plus its verification dispatch (D-36) found and confirmed by live reproduction a 7th hole in the isolation gate: `target["household_id"]`/`audience` were caller-supplied and never authenticated (D-30 had authenticated only the example side), and an empty target audience (`frozenset()`, not `None`) bypassed the fail-closed check entirely, admitting every scope in the household. D-36 additionally confirmed the 23-case adversarial battery that guards the gate was wired into no runner at all — referenced by nothing outside its own docstring. D-37 built the fix under a new successor REQ, `REQ_LEARNER_TARGET_AUTHENTICATION` (target-side derivation mirroring D-30's example-side fix, battery wired into `scripts/run_harness.sh` and PROVEN load-bearing by fault injection), following the same xfail(strict=True)→XPASS→real-PASS pattern D-27 used for the original six holes. D-38 cleared a `--full` memory block and ran it green (27/27 battery, LI1+CS1+all-ABSOLUTE PASS, RATCHET PASS). Bill ruled `REQ_LEARNER_TARGET_AUTHENTICATION` MET on that evidence (D-39, 2026-07-30), and `REQ_LEARNER_SIGNAL_ISOLATION` is MET again through it as successor. Same D-39 ruling also carries `REQ_CURATOR_SHADOW_SCORER` to MET — the Curator Stage 2 shadow scorer (built D-33, not in this doc's tables below as BUILT until this update) is shadow-proven, cold-start byte-identical to the rule order, routes every training example through the now-hardened isolation gate, and passed `--full` (CS1 PASS) in the same D-38 window. See the row corrections below and each REQ's own D-37/D-38/D-39 history for full evidence.

UPDATED 2026-07-30 (D-42): `REQ_CURATOR_SHADOW_SCORER`'s D-39 MET above is PULLED BACK to BUILT, same day. D-40 routed the scorer to two independent Fable reviewers; D-41 confirmed by direct reproduction — running the actual code against the real log, querying the live registry via a copy so as not to mutate it, stubbing the gate to isolate a cross-household experiment, and mutation-testing the metamorphic checks — that four defects are real: (1) the correction/label substrate was structurally dead (a field-name mismatch meant `classify_outcome` could never detect a real supersede, so every training label was inverted to a positive and the corpus was single-class); (2) `register_model` had zero callers by design, so the model was never registered and total gate refusal read silently as ordinary cold-start; (3) the weight cache was keyed with no household component, proven live to serve one household's fitted weights to another; (4) `historical_acceptance` was unbounded, `validate_shadow_output` was tautological (order was never checked, only membership), and the metamorphic reword check was vacuous in both places it existed (a variable-name match a renamed parameter would evade). D-42 built fixes for all four under the REQ's own Part B fix spec, each proven with a test that demonstrated the defect before the fix (the D-27 xfail pattern, or — where the fix changed a return-value arity — direct comparison against the committed pre-fix source): `classify_outcome` now reads `transition`/`prior_fact_id`, the keys the write path actually produces; `_weights_for` reports a new `"gate_refused"` regime, distinct from `"cold_start"`; a standalone, never-auto-invoked operator script (`scripts/register_curator_shadow_model.py`) exists for the deliberate registration act; the weight cache is keyed per household; `historical_acceptance` is clamped to `[0,1]`; `validate_shadow_output` now checks ranking order against its own logged scores; and both metamorphic checks now prove a genuine `sio_attribute` dependency plus a structural, code-object-level query-independence check. 44 standing-battery test cases across four files now run on every harness pass (`scripts/run_harness.sh`, wired alongside the D-37 isolation battery). `--layer 7` green (L7 27/27, AUDIT 8/8 — 59 checks, 0 missing artifacts, LI1 PASS, CS1 PASS, RATCHET PASS). `--full` NOT run this pass — 0.08GB free, well under the 2GB TD-129 threshold, not forced. **Not self-MET; staged for Bill.**

UPDATED 2026-07-30 (D-43): `--full` now GREEN, closing the one item D-42 left open. Memory freed the D-38 way (3 idle Ollama models unloaded on the dev daemon 11434 only; frozen demo daemon 11435 — `qwen2.5:7b` loaded "Forever" — untouched and confirmed unchanged; 0.39GB→2.31GB free). All 5 ABSOLUTE checks PASS (G0/PSA1/CTX-STRIP/LI1/CS1), standing batteries 44/44, AUDIT 8/8 unchanged, RATCHET PASS. Two items observed, both pre-existing and unrelated, named not regressions: `L1:P2` iteration `i019` (the same documented async-write-timing flake seen in the D-38 run; baseline already records it `false`) and `L2:three_zone_demo.T02` (TD-125, OPEN, an already-quarantined Groq extraction flake). Live end-to-end proof the scorer can now learn, run against the actual fixed pipeline with a genuine production-shaped supersede: `classify_outcome` returns `kind: "correction"`; the corrected fact's training label is `0`; `shadow_outcome_agreement` returns a real, hand-verified `0.0` (not `None`) — the exact mechanism D-41 found structurally dead, now running correctly. Both preconditions REQ_CURATOR_SHADOW_SCORER's own Part B section named for re-earning MET (the four fixes, `--full` green) are now demonstrated. **Not self-MET; staged for Bill.**

UPDATED 2026-07-30 (D-44): **`REQ_CURATOR_SHADOW_SCORER` is MET — Bill's ruling, recorded.** Per the standing protocol a session never self-marks MET; this records the call Bill made in the D-44 dispatch. It rests on the full arc now on record: the scorer is shadow-proven (byte-identical prompt with the scorer on and off), reproduces the deterministic rule order byte-identical at cold start, and routes every training example through the MET-hardened isolation gate (`REQ_LEARNER_TARGET_AUTHENTICATION`, which the D-42 pullback explicitly did not undermine and which stays MET). External adversarial review (D-40, two independent blind Fable reviewers) → verification by direct reproduction (D-41) → fixes (D-42) → `--full` green (D-43, 81da8b0). As the ruling's own evidence, D-44 re-ran D-41's exact reproduction against the fixed code: `classify_outcome` emits `kind: "correction"` (D-41: never), the corrected fact's training label is `0` (D-41: `1` — the inversion), `outcome_event_count` is `1` (D-41: `0`), `shadow_outcome_agreement` returns a real `0.0` (D-41: `None`), and `historical_acceptance` clamps to `0.0` on the overflow case (D-41: `-4.0`). **The corpus is no longer single-class — the learner can learn.** HONEST LIMIT, carried into the MET rather than buried: the trained regime remains **CLOSED**. Fix 2 made registration possible and made silent refusal visible (a distinct `gate_refused` regime); it did not perform the registration. Verified read-only at this commit that the `learner_models` table still does not exist in the live registry, so `curator-shadow-default` does not resolve and every training example is still refused at gate V0b. This MET therefore certifies that the learner **can** learn, demonstrated end-to-end on a real correction — not that it **has** learned in production. That is the correct posture (registration is a deliberate operator act, and this REQ's original coverage entry already named "the trained regime live on a real graph" as UNCOVERED), but opening it is a separate decision: `scripts/register_curator_shadow_model.py`. Whoever performs that act should expect the D-41 findings latent behind it — notably the attacker-influenced cold-start threshold, where ~100 confirmation declines flip the deployment out of the byte-identical safe regime — to become live at that moment.

Purpose: one page answering what is DONE, what exists but is not yet in the
running solution, and what is next in priority order. Sourced from REQ
statuses and INDEX; corrected against the repo at landing time.

## 1. BUILT AND IN THE SOLUTION (MET or BUILT, running)
| Component | Evidence |
|---|---|
| Crypto sealing, operator-blind at rest, master key destroyed | REQ_CRYPTO_P3_OPERATOR_BLIND MET |
| G0 output-fabrication backstop | REQ_G0_OUTPUT_INVARIANT MET |
| Prompt-to-record fidelity | REQ_PROMPT_RECORD_FIDELITY MET |
| Injection contract INJ-1..7 | BUILT per hardening spec |
| PSA1 prompt-fidelity check, wired, fault-twinned | BUILT, runs under --layer 7/--full |
| Harness runner, doc rendering, coverage measurement | three REQs MET 2026-07-28 [corrected from draft: draft claimed a fourth, strip-context-completeness, was also MET; its own Status header reads NOT MET, and it has no docs/INDEX.md row at all -- item 4's same-session before/after RATCHET pairing was never captured and can't be retroactively produced; found and reported in this session's own DISPATCH_44 -- since RESOLVED: ruled MET by Bill 2026-07-29 (D-20, 0e6ae00), see the strip-context row below] |
| Strip-context-completeness (D-28 fix) | REQ_STRIP_CONTEXT_COMPLETENESS **MET** [updated 2026-07-29, D-22]: ruled MET by Bill (D-20, commit 0e6ae00) — item 4's intent satisfied by the recorded baseline plus every green run since, including the 2026-07-28 --full (DISPATCH_44); the missing INDEX row landed in the same commit. Fix live at HEAD (FACT_BEARING_SECTION_HEADERS derivation), L7:CTX-STRIP ABSOLUTE-tier with both fault-injection directions. [prior state retained for the record: NOT MET pending item 4's same-session proof form — found by DISPATCH_44] |
| write_rule.classify mutation coverage | 34/34, score 1.00 |
| Curator Stage 0 outcome instrumentation | REQ_RETRIEVAL_OUTCOME_INSTRUMENTATION MET, e207e05, code live-wired [corrected from draft: "collecting" is not currently accurate -- logs/turns_demo.jsonl is empty (0 records) at landing time, almost certainly reset by intervening demo-cutover testing activity; the instrumentation fires on every real text-query turn and is proven live-wired (verified in this session against real turns), but there is no accumulated data right now] |
| Groq call resilience | roadmap d83a111; ported to hip-dev ac455f4 |
| Frozen demo | preflight 10/10, T02 12/12 (both independently re-verified in this session) [corrected from draft: "all five beats green" -- no direct evidence found in either checkout for this specific claim under that name; marked [unverified] rather than restated as fact] |

## 2. BUILT OR RATIFIED, NOT YET IN THE SOLUTION
| Item | Gap |
|---|---|
| Four partition scopes + write precedence | [updated 2026-07-29, D-22] The 13-row write-rule acceptance table RAN for the first time 2026-07-29 (D-18): 12/13 pass; row 3 deferred to clinical-ethics review by Bill's D-21 ruling (e357005) — standing medication facts seal pair-private (conservative default) while medication_status change events reach the care team; reviewers choose between widening the coordination enum and keeping changes-only. REQ_PARTITION_CUSTODY status: NOT MET — 12/13 verified; row 3 deferred pending clinical-ethics review (a known parked decision, not an unfinished build). [prior text: table had not been run] |
| AW-06 KV-cache extraction detection signatures | SPECIFIED, NOT BUILT (confirmed verbatim against the Architecture Weakness Register) |
| Demo cutover consent assertion | [updated 2026-07-28, D-08 close-out] Build COMPLETE and REQ ruled MET by Bill on the demo-cutover branch (D-03c: all three assertions green end-to-end — pane == exactly the two derived D10/D11 record IDs, approve → real setback answer via openai:gpt-4.1, decline → no send; MET close-out f971377, D-03d). Branch nuance: on roadmap the REQ file still reads NOT MET because the MET flip lives on demo-cutover, unmerged — this row moves to Section 1 at merge. [prior correction retained: the REQ was filed 2026-07-22 (v20260722_2018); D-03 was tonight's build toward it] |
| Confidential computing | Adopted, not integrated into the inference path; operator-blind at inference is NOT claimed and must not be (confirmed verbatim: "The claim is exact and must stay exact: operator-blind at rest, not operator-blind at inference. The two are never the same sentence.") |
| Curator research corpus (memos A, B, reconciliation) + Stage-1 isolation gate + Stage-2 shadow scorer | [updated 2026-07-30, D-42] Research landed/registered. Stage-1 learner-signal isolation gate BUILT and MET (harness/learner_isolation.py; REQ_LEARNER_SIGNAL_ISOLATION MET again per Bill's D-39 ruling, RE-MET through its successor REQ_LEARNER_TARGET_AUTHENTICATION after a D-37 pullback found+fixed a 7th hole) — provenance derived from an un-forgeable fact_id AND (D-37) target scope derived from a registered model_id, both operands now authenticated, --full green (D-38). Stage-2 shadow metadata scorer's MET WAS PULLED BACK 2026-07-30 (D-42) to BUILT: D-40 routed the scorer to two independent Fable reviewers; D-41 confirmed by direct reproduction (running the code against the real log, querying the live registry via a copy) that the scorer's ISOLATION properties hold (shadow-only, value-blind, cold-start byte-identical, gate over-refuses) but its LEARNING substrate was structurally dead — no delta anywhere in the codebase carried the keys the correction-outcome classifier required, so every training label was inverted to a positive, the corpus was single-class, and the offline agreement metric returned None on the real log; separately, register_model had zero callers (by the module's own design) so the model was never registered and the trained regime was silently indistinguishable from cold-start; the weight cache was keyed with no household component; and historical_acceptance was unbounded with a tautological order-check and a vacuous metamorphic test alongside it. D-42 BUILT fixes for all four under REQ_CURATOR_SHADOW_SCORER's own Part B fix spec, each with an xfail-until-fixed test flipped to real PASS (44 standing-battery cases total, wired into every harness pass): classify_outcome now reads the delta fields the write path actually produces; _weights_for reports a new "gate_refused" regime distinct from "cold_start"; a standalone operator script (scripts/register_curator_shadow_model.py) exists for the deliberate registration act, never called by any training-path code; the weight cache is now keyed per household; historical_acceptance is clamped to [0,1]; validate_shadow_output now checks ranking order against its own logged scores, not just membership; and the metamorphic checks (CS1 scenario + harness_audit probe) now prove a genuine sio_attribute dependency plus a structural (code-object) query-independence check, replacing a variable-name string match. --layer 7 green (L7 27/27, AUDIT 8/8, LI1 PASS, CS1 PASS, RATCHET PASS); --full NOT run this pass (0.08GB free, well under the 2GB TD-129 threshold — not forced). NOT self-MET; staged for Bill. **[updated 2026-07-30, D-44] Stage-2 is now MET (Bill's ruling)** — D-43 proved the four fixes with a green --full at 81da8b0, and D-44 re-ran D-41's own reproduction against the fixed code as the ruling's evidence: correction emitted, corrected fact labels 0, shadow_outcome_agreement returns a real 0.0 (not None), historical_acceptance clamps to 0.0 (not -4.0). The corpus is no longer single-class. HONEST LIMIT carried into the MET: the trained regime is still CLOSED — learner_models still does not exist in the live registry (verified read-only at this commit), so curator-shadow-default does not resolve and every example is still refused at V0b. The MET certifies the learner CAN learn, demonstrated end-to-end, not that it HAS learned in production; opening the trained regime is a separate deliberate operator act (scripts/register_curator_shadow_model.py). All three ContextArch preconditions (G0, prompt-fidelity, learner-isolation) remain MET; the first learner exists, is isolation-sound, and now demonstrably learns — in shadow, on demand, not yet in production. HONEST NOTE, unchanged by this update: Gate A's own entry trigger (REQ_CURATOR_SHADOW_SCORER's OPEN QUESTION 1) was never formally set a number |

## 3. PLANNED, IN PRIORITY ORDER
1. Merge demo-cutover into roadmap — the build landed and was assessed
   tonight (MET ruled, f971377); the merge itself is Bill's call, after the
   demo. [updated 2026-07-28, D-08 close-out: was "build lands and is
   assessed (in flight)" — both now done]
2. Bill's five Curator rulings; then Curator Stage 1: the learner-isolation
   gate check (ABSOLUTE tier, two-household fixture, fault twin) built
   BEFORE any learner exists.
3. Run REQ_PARTITION_CUSTODY's thirteen-row acceptance table; move it to MET
   or name what fails.
4. Write-time classifier (auto-assignment of subject_visibility scope);
   REQ_WRITE_TIME_CLASSIFIER NOT MET (confirmed).
5. ~~Curator Stage 2 shadow scorer~~ — SUPERSEDED 2026-07-30 (D-39): built
   D-33, MET D-39 (Bill's ruling). See Section 1's Curator row, updated same
   date. Gate A's entry-trigger number itself was never formally set — named
   there as an honest open note, not a re-opened blocker.
6. Scope-doc ratification pass: line 34 five-tier fix, attribute-count
   wording, then Bill ratifies the doc out of DRAFT. (Confirmed still
   open: HIP_ArchitectureForDiligence line 34 still reads "edge, mid,
   core, and frontier tiers" -- four, unfixed.)
7. Confidential-computing integration: commodity, adopted when vendor stack
   is mature; not on the differentiating path.

## 4. BILL-ONLY ITEMS (no session can do these)
White-paper placeholder paragraph (confirmed still present verbatim,
`docs/rendered/whitepaper/nda/HIP_WhitePaper_Confidential__v20260727_1104.docx.md:162`,
`[PLACEHOLDER -- Bill to write]`); scope-doc ratification; the five Curator
rulings; Gate A entry-trigger number after Stage 0's first baseline; the
demo-cutover merge decision (build complete and MET on the branch — added
back 2026-07-28, D-08 close-out; the draft named it, the landing dropped it
while the build was in flight).
