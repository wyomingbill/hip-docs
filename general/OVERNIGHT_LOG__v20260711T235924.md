# Overnight Run Log — v20260711T235924

Serial run. Gate before push. Stop on double failure. Sonnet throughout.

---

## Item 1 — FINISH voice-path hardening (BUILD-1)

**Status:** COMPLETE (carried from prior session)  
**Result:** GREEN  
**Commit:** fbcd372  
**Gate:** VOICE PASS (4/4), SCHEMA PASS (46/46), DISC PASS (39/39). Only L1:P10 failed — pre-existing, not this build.  
**Summary:** OrchestratorGate._on_user_text now routes through assemble_governed_context at all 3 sites. VOICE-GOV-001–004 conformance gate ratcheted into baseline.

---

## Item 2 — TD-101 part 1: embed_text pre-encryption trace

**Status:** IN PROGRESS


**Status:** STOPPED — gate failed twice (pre-existing L2 flake, no code change)

**Finding:** CLEAN — no pre-encryption value exposure exists.
- `embed_text` in extraction_queue.py L664 embeds `f"{owner} {fact['attribute']}"` only — never `fact["value"]`. Comment at L662 documents the TD-030 reasoning explicitly.
- All three write paths encrypt before any logging:
  - `extraction_queue._write_one` L485: `encrypt_fact_value(fact["value"], owner)` before any log statements
  - `memory_engine/store.encode()` L418: encrypts `new_value` before writing or logging the value
  - `memory_engine/consolidate._write_derived_node()` L490: encrypts `df.value` before write
- `passes_sensitivity_filter()` runs a regex match only — no logging of raw value
- No fix needed. TD-030 is already implemented correctly.

**Gate result (--quick, attempt 1):**  
RATCHET FAIL — `L2:care_coordination.T01`, `L2:care_coordination.T02`, `L2:reveal_demo.R03`, `L2:reveal_demo.R04`, `L2:three_zone_demo.T04`  
These are graph-write propagation tests that fail when the extraction worker hasn't processed jobs within the poll window. All 5 are in baseline as `true` (expected pass). L3/SCHEMA/VOICE all PASS.

**Gate result (--quick, attempt 2):**  
Same 5 failures. Identical pattern.

**Assessment:** These are NOT BUILD-1 regressions (no code was changed for item 2). They are timing-flaky on `--quick` (extraction worker warmup). The second full gate run (fbcd372) did not show these as new failures — only L1:P10 appeared. However, protocol is "fail twice → STOP."

**STOPPED. Awaiting user decision on how to proceed past L2 timing flake on --quick.**

---
