# HIP Tech Debt Register — known issues
STATUS: LIVE
RECONCILED-AGAINST: session 2026-08-05, D-R-178 (register-only for this entry): TD-160 ADDENDUM — the enrollment gap is INTERNAL TO THE FIXTURE, not uniform: `ray` IS dyad-registered while `dad`, `household` and `elena` are not, so two care recipients in the same fixture are built differently and every identity check treats them differently. Bill authorized; original entry kept. Same session: R11 RULED MET (§16) after TD-136 was answered at D-R-174 and closed at D-R-176. Prior: session 2026-08-04, D-151 (register-only; docs lane): TD-159 filed — the A18/A29/A30 CLAIMED-NOT-VERIFIED rows are a VISIBILITY gap in the status board's cross-check, NOT a coverage gap (D-149's own distinction, preserved here): their real coverage exists and runs, under names that predate the test_ceil_a<N>_* convention the cross-check relies on. TD-160 filed — subject="dad" and subject="household" fail the identity registry despite being in scope, which forced THIRD_PARTY_NONCARE_DOSSIER into ABSENT_CLASSES at D-140; consequence stated plainly: one of R9's six never-store categories has no write-time check behind it, and the reason is a DATA gap, not a principle. Also confirmed this dispatch: the 2026-08-01 evaluation-methodology review is ALREADY BANKED (both legs, 2026-08-03 15:27) — D-151's premise that it exists only in a transcript is stale; nothing re-banked. Prior: session 2026-08-04, D-147 (register + code; .env.demo hazard fix, MEM-118 traced): TD-158 filed -- MEM-118 (and MEM-116/117, one shared root cause, traced not guessed) fails deterministically with delta transition='unresolved' under the CORRECT interpreter ($HIP_DEV_PYTHON): TD-151's own D-130 fix (_restore_ray_medication_fixture) reseeds the (maya,ray,medication) D9 fixture to CORROBORATED trust after every run, so the live pipeline's own supersede attempt is outranked and P8's cross-principal write-monotonicity override (memory_engine/store.py) parks it -- confirmed live via server/voice_orch.py:2403's own log line, reproduced 3x. REAL, not environmental -- two independently-correct mechanisms interacting, not a bug in either alone. SEPARATE finding recorded in the same entry: this session's own prior eval.memory_harness invocations (D-130 through D-146) used system python3, which lacks pipecat and produces a DIFFERENT, misleading failure mode that happens to also read red -- the pass/fail COUNTS those dispatches reported were not affected, but any citation of MEM-116/117/118's specific failure TEXT from before D-147 should be treated as possibly interpreter-confounded. Not fixed, per instruction. Same dispatch: .env.demo untracked and gitignored (D-146's .hip-graph precedent), fault twin + anti-vacuity added. Prior: session 2026-08-03, D-142 (Index Demo 2 task 3, register-only, no code changed): TD-154 AMENDED, original text preserved, addendum appended -- Index Demo 1's read-only recon (this same lane) found that REQ_VOICE_TRACE_CAPTURE, which TD-154's own filing said resolved to no file anywhere searched, DOES exist -- on hip-vo branch voice-port's tree (docs/requirements/REQ_VOICE_TRACE_CAPTURE__tts-audio-capture-and-spoken-vs-typed-provenance__v20260801_0923.md, filed Voice 22, commit 84a7146), and its text genuinely discusses transcription_ms being structurally null on the demo path. TD-154's original nine-checkout search only ever inspected each repo's CURRENTLY CHECKED-OUT working tree; hip-vo was checked out to main, not voice-port, so the search never actually looked where the REQ lives. TD-154's own correction was itself incomplete, not fabricated -- the REQ genuinely does not exist on the branches those working trees had checked out at the time. Prior: session 2026-08-03, D-136 (Index Bank 2, register-only, no code changed): filed six TIER 2 findings as TD-152 through TD-157, sourced from docs/research-technical/HIP_RepoBankingGaps__demo-lane__v20260803_1527.md sections 6-10 (banked Bank 1, commit aaadae3) -- epistemic panel v2-fact display gap (TD-152, GATE), maya/sam voiceprint Fernet decrypt failure (TD-153, GATE), transcription_ms structurally null on the demo path (TD-154, ENG; corrects a REQ_VOICE_TRACE_CAPTURE citation that resolves to no file anywhere searched -- roadmap/vo/harness/cutover-demo/dev/ungoverned/crypto-p1/crypto-p2/stage1-wip), six Voice-38 ambiguity leaks (TD-155, SEC) and seven independently-reached structural-empty-set items cross-referenced to the already-RESOLVED TD-143/TD-144 and the still-OPEN TD-149 (TD-156, SEC), and the /tmp voice12 recording script's stale-stdin bug plus its at-risk /tmp location, noting without acting on it that a tested replacement already exists unbanked at ~/Downloads/voice_record_bill.py from this same session (TD-157, OPS). No fixes performed, no REQ written, per explicit instruction. Prior: session 2026-08-03, D-130 (R2 build + register): TD-151 filed and RESOLVED same session -- MEM-116/117/118's live-pipeline write against the real D9 demo fixture (owner=maya, subject=ray, attribute=medication) was never cleaned up (fixed session id 'text-maya', untouched by their randomized-sid cleanup), and three consecutive eval.memory_harness runs (verifying an unrelated R2 fix) fully closed D9 with no active replacement -- caught only because eval/test_structural_refusal.py's anti-vacuity check depends on 'ray' being an active subject. D9 reseeded via demo_seed's own _seed_one (value verified byte-identical by decrypt), MEM-116/117/118 patched with a shared _restore_ray_medication_fixture() cleanup, test_structural_refusal.py 7/7 green for the right reason (ray+dad genuinely active), memory harness stable at 13/17 across 3 runs post-fix with failures exactly {115,116,117,118}. Prior: session 2026-08-03, D-129 (register-only for this entry; Bill's rulings): TD-143 RULED RESOLVED (closed by D-127's fix, three consecutive structural runs). TD-144 RULED RESOLVED-BY-CORRECTION-AND-REQ as proposed — recorded per the ruling that its ORIGINAL HEADLINE WAS WRONG (no dad-medication fact existed; sam owned the admitted content) and that the CORRECTION, not a fix, is half of why it closes; residues stay in TD-149 and TD-136. Same dispatch: REQs filed for TD-149 (REQ_ASKED_ATTRIBUTE_COVERAGE) and TD-150 (REQ_BASELINE_RECORD_INTEGRITY), both NOT MET, no build; and the ceiling REQ preamble de-staled on Bill's first unconditional instruction (fifth flag). Prior: session 2026-08-03, D-128 (register + governance; no code changed): TD-150 filed — the baseline updater is a RECORD-INTEGRITY HAZARD (two governance-relevant side effects in two days, both caught only in diff review: D-126's --accept text applied to EVERY red row clobbering TD-143/TD-144/L1:P2's justifications; D-127's plain --update-baseline dropping the whole _accepted map incl. L1:P2). Fix scoped, not built. TD-143 status PROPOSED RESOLVED (PW010 refuses structurally in three consecutive runs post-D-127; Bill rules). TD-144 status PROPOSED RESOLVED-BY-CORRECTION-AND-REQ (headline corrected at D-125/D-126, row expectation corrected at D-127 ruling d, the CLASS governed by REQ_STRUCTURAL_REFUSAL — RULED MET at D-128; Bill rules the TD). Same session: Bill ruled REQ_STRUCTURAL_REFUSAL MET (six evidence points verified in the final runs; non-coverages recorded: TD-149, TD-136, PW016/PW018 skips). Prior: session 2026-08-03, D-127 (register + code session; see the D-127 dispatch doc): TD-149 filed per Bill's ruling (e) — INJ-6b's asked-attribute detection stays keyword-regex over _TARGETED_ATTRS (12 precise attributes; appointment/preference/schedule excluded by documented over-fire rationale), so the structural-refusal keying D-127 built covers only those; SIO-derived asked-attributes (keying 3 of D-126's proposal) is the widening path, deliberately OUT OF D-127's scope. Same session, recorded here for the register's one-glance state: D-127 fixed resolution blindness (graph-wide Phase-3 known-set, split from the contract's member_ids so INJ-7 is untouched) and removed INJ-6b's candidate_hit carve-out (ruling c); PW010 and PW031-033 flipped to structural PASS — TD-143's red is CLOSED BY FIX pending Bill's ruling; TD-144's row expectation was corrected per ruling (d) (the row was the defect, D-126's trace). Prior: session 2026-08-03, D-119 (register-only, no code changed): TD-147 ADDENDUM — the filed mechanism was WRONG and is corrected in the entry (DIAG-1 instrumentation: the failing calls were groq_status=ok/attempts=1 — the ReadTimeout recovered in-call and transport never dropped anything; gpt-oss-20b returned changes:[] deterministically, both runs, retry included, on the RESTATEMENT-WITH-FACT-PRESENT payload class; and turns_demo contains ZERO atorvastatin turns — that success claim was a conflation with the four sibling harness_run instances). Payload-biased survives testing; arbitrary-availability is excluded; D-119's STOP did not fire. TD-148 filed — the lock protocol is advisory with no enforcement: D-107 write-through + D-118 unread clobber, with the thrice-flagged 'taken:'-timestamp drift folded in (no TD existed; checked). Fix scope named, not built. Prior: session 2026-08-03, D-118 (register-only, no code changed): TD-147 filed — the L6:record-invariants red from D-117's two --full runs (one G1 no-orphan-generation violation, the identical sam/atorvastatin smoke turn both runs; fact_change zero-changes plus a Groq-side ReadTimeout on retry dropped the write, F3 honestly refused the ack, G1 counted the orphan by its own documented design; NOT caused by the D-117 rename, proven structurally — fact_change.py has zero header references and the captured prompts were byte-identical), with MEM-117's new same-family red noted inside the same entry per Bill's instruction, not filed separately. Baseline fact recorded: D-114's RATCHET PASS was layer-7 scope; the last true --full including L6 was 2026-07-30, green — the red is new against a five-day-old baseline, not against yesterday. BASELINE STAYS UNUPDATED, THE RED STAYS LOUD (Bill, D-118). Same session: Bill ruled the REQ_STRIP_CONTEXT_COMPLETENESS header-rename revision MET (evidence and the single-line-pin hardening recorded in that REQ's MET-Ruling). Prior: session 2026-08-02, D-113 (Bill's rulings): R18 RULED MET on the D-111-amended operative rule (REQ_STRUCTURAL_CEILING new version v20260802_2205, ruling text verbatim in section 16; MET WITH THREE ABSENCES RECORDED, not conditioned away -- purpose_id/retention_deadline/policy_version blocked on R23/R21/a real policy version with absence held by a standing test, weak-sense storage-class erase, pre-lineage backfill unruled; rationale: holding R18 open until R21/R23 build would make it hostage to unscheduled requirements -- same shape as R30's MET with named limitation; preamble MET/NOT-MET split re-counted to FIVE ruled / 25 not run, fixing drift vs D-100). TD-139's status column replaced with Bill's wording verbatim (PARTIALLY CLOSED (D-105), eight of eleven implemented, three absent and blocked, pre-lineage question unruled) -- resolving the column-vs-entry discrepancy D-111 flagged. Nothing else ruled. Prior: session 2026-08-02, D-111 (Bill's ruling on TD-140): TD-140 RESOLVED BY REQUIREMENT CHANGE, not by a build -- R18's recompute clause REMOVED from REQ_STRUCTURAL_CEILING (new version v20260802_2112, LATEST repointed; the four reasons recorded in the requirement itself: no stored recipe to re-run, a rule the system cannot perform is not a commitment, invalidate-only errs toward forgetting more never less, recompute-as-fresh-inference deliberately not foreclosed -- returns via inference-permit machinery as its own REQ if ever wanted). derivation_cascade.py docstring updated to the amended rule (comment-only). R18's three debt gaps now: TD-139 partially closed at D-105 (status column still reads OPEN -- flagged, not silently changed), TD-140 resolved here, TD-141 resolved at D-107; R18's MET status deliberately NOT ruled -- evidence both ways recorded in the REQ's section 16 for Bill. Prior: session 2026-08-02, D-110 (register-only, no code changed): TD-146 filed -- MEM-115(b)'s persistent red traced to its root: the cross-member cold recall's rows ARE fetched by the subject-scoped Cypher but are dropped at the caller-scoped decrypt (recall.py:228-236, REQ_CRYPTO_P2) BEFORE the injection contract runs, so denied_count=0 is recorded faithfully with no contract denial to count; proven by D-107 run 2's captured log (two 'decrypt failed ... FileNotFoundError ... skipping' warnings, zero Neo4j failures). HARNESS ARTIFACT, not a disclosure gap -- the scenario predates P2 and tests the contract with member-private facts, the one class crypto now pre-empts; the one real residual is audit fidelity (an attempted-but-unreadable recall is byte-identical in the audit to 'no cold facts exist'). Scoped with two verification shapes (decryptable-class fixture, or an undecryptable_count audit field), deliberately not fixed. With TD-145 this closes the pattern: both persistent memory-harness reds are crypto-era scenario staleness. Prior: session 2026-08-02, D-109 (register-only, no code changed): memory-harness baseline RE-PINNED to "13-15/17, failures a strict subset of {MEM-115,116,117,118}" (old pin "13/17, the same four" falsified pass-ward by D-107's three same-code runs; D-108's four grounds recorded in the Notes entry so the next reader does not re-derive them; outside that range is still a STOP), and TD-145 filed (MEM-116's value verification decrypts via the retired v1 master-key helper at eval/memory_harness.py:1664 on a checkout where OB6 asserts the key's absence as CORRECT -- a structural false-red mode unrelated to the scenario's subject, same class as TD-142; scoped with two candidate verification shapes, deliberately not fixed). Prior: session 2026-08-02, TD-141 RESOLVED (D-107): demo_seed's encode-then-SET traced (efde99b, 2026-07-05, pre-dates every lineage mechanism) and replaced -- encode() gains a DerivedLineage channel so the seed's derived fact goes through create_fact_node's R18 gate with real parents (D4+D5), derivation_method demo_seed.fixture.v1 (X-04 honesty kept structural), audience_policy from the write class; live-proven end to end (D8 carries the full block; retracting D4 cascade-closed D8 with closed_by=lineage_cascade -- the cascade's first action on live data); post-write SET refusal is impossible at the graph level, stated plainly, detection standing in test_lineage_block's source scan and verify_seed's graph query; 5 acceptance cases added (21 total); R18 stays NOT MET (TD-140 open). Prior: session 2026-08-02, TD-140 addendum (D-104, survey only, not fixed): recompute confirmed to require a fresh, non-deterministic model call (memory_engine.consolidate._abstract_pass -> Interpreter.abstract() -> the real production GroqInterpreter), not a mechanical re-derivation from stored data -- R18's text ('recompute child and replace lineage') reads as a different, simpler requirement than what building it would actually mean. Full survey: docs/dispatches/DISPATCH_TD140_RECOMPUTE__stopped-recompute-requires-a-model-call__v20260802_0639.md. TD-140 not resolved or changed in shape, only in what's known about it. session 2026-08-02, TD-142 filed (mutation-harness hardcoded file:line addressing is brittle, not fixed here): eval/harnesslib/mutation_targets.py's _INJ7_LINENO_RANGE and its survivor.lineno equality check hardcode absolute line numbers inside harness/injection_contract.py to scope/identify one seeded mutant; D-101's unrelated R12 fix (docstring + new logic inserted above that point) shifted the target from :619 to :664 and desynced both, producing a false red (L7V2 27/28 -> 25/28) that read as a governance regression until traced. D-102 resynced the two coordinates (now :656-674 / :664) and files this as the durable-addressing gap; not fixed. session 2026-07-31, TD-137 RESOLVED (D-75): canonical sensitivity registry built at harness/sensitivity.py under REQ_STRUCTURAL_CEILING R29/R30 (filed b0bc8e3) -- one authoritative ordering, all four consumers importing it, local enums and ordering defaults removed, unknown values failing CLOSED at the router threshold, the permissions deny rule, and the extraction write boundary (which had been silently coercing unknown to `medium`). R30 inventory ran first and found zero unmapped stored values. 31-case standing battery added and wired; --layer 7 exit 0 with all 5 ABSOLUTE PASS incl. CS1, AUDIT 8/8, RATCHET PASS. R29/R30 NOT self-ruled MET; R30 item 5 (registry version on durable artifacts) explicitly not implemented. Prior: session 2026-07-31, TD-138 filed (care-team epoch-blindness, not fixed): harness/care_team_keys.py:141 is_active_caregiver takes no epoch parameter and delegates to list_caregivers, which filters only removed_at IS NULL -- the CURRENT roster -- while REQ_CARE_TEAM_READ_AUTH:11 requires authorization at the FACT'S ROSTER EPOCH, so a newly-enrolled caregiver gains read access to care-team-private facts predating their enrollment. Bill ruled (D-69) the epoch rule stands and the code is the defect; the removal direction stays deferred to REQ_PARTITION_CUSTODY's backfill-grant policy. The REQ's acceptance row 4 tests only the removal direction, so a fully-passing acceptance would have missed this -- row 1 needs amending to assert against the fact's roster epoch. Dormant today (both care-team tables empty), arms on first enrollment. Prior: session 2026-07-31, TD-137 filed (sensitivity-encoding critical-misrank finding, not fixed): harness/curator_shadow.py:95 _ORDINAL and harness/hipconfig.py:30 SENSITIVITY_RANK both omit a "critical" key that harness/extraction_queue.py:95's SENSITIVITY_LEVELS declares, so a "critical"-tagged fact ranks below "high" in the curator score (defaults to neutral 0.5) and below "low" in the routing force-local-at check (defaults to 0) -- the most sensitive class currently gates depth/escalation LEAST of the three encoded levels. No authoritative encoding declared; not fixed pending Bill's ruling. session 2026-07-27, TD-136 ported (DISPATCH 36 register reconciliation phase 2, HIP_RegisterReconciliation__cross-branch-id-collisions__v20260727_1930.md): main branch TD-131 (commit `4390240`) ported verbatim as TD-136 per Bill's ruling that roadmap's numbering is authoritative -- main's own TD-131 is unrenumbered and now diverges permanently from roadmap's TD-131 (a different, unrelated git-worktree finding). Prior: session 2026-07-27, TD-135 added (REQ_DOC_RENDERING build): whitepaper/archive/HIP_White_Paper_Augmented.docx found corrupt (truncated zip, unreadable since first commit) during the docx-to-text renderer's baseline run -- named, not fixed; its rendering stays MISSING by design until the source file is repaired or removed. Prior: session 2026-07-27, TD-134 burn-down (check_g0 mutation survivors): 4 of the 5 surviving `harness/g0_invariant.py:check_g0` mutants killed via 4 new killer probes in `eval/harnesslib/mutation_targets.py`; check_g0 score 3/8 (0.38) -> 7/8 (0.88); overall mutation score 78/128 (0.61) -> 82/128 (0.64); the fifth survivor (:67 `delete_last_operand(Or)`) confirmed a true equivalent mutant, left unkilled by design -- see TD-134. check_g0's own behavior unchanged; no new mutation sites; all other modules' survivor counts unchanged.
Rule: one running register. New issues appended with an ID. Resolved issues marked RESOLVED with the commit that closed them, never deleted. When this file changes materially, cut a new timestamped version and repoint LATEST_DEBT.

## Severity: SEC (security) | GATE (blocks a promotion/demo) | ENG (engine track) | OPS (tooling/reliability)

| id | sev | issue | source | status |
|----|-----|-------|--------|--------|
| TD-101 | SEC | Unauthenticated dashboard endpoints still present; embed_text(fact["value"]) TD-030 gap open (embedding path may touch a fact value pre-encryption); git-history scrub pending | SECURITY_AUDIT (INDEX, STALE) | OPEN |
| TD-102 | GATE | issue_INT-001_*.json appeared in eval/integration_issues/ after a passing 6-check gate; likely transient run against unseeded state. Verify before next promotion. | Truth-layer delivery report | OPEN |
| TD-103 | OPS | launchd bootstrap for com.hip.voice.orch fails 1-in-N (I/O error 5), does not inject NEO4J_PASSWORD reliably; voice server start is non-deterministic. Fix: committed start_manual.sh that exports the password and launches directly. | 2026-07-05 session | OPEN |
| TD-104 | OPS | Neo4j password contains shell-special char (!); trips zsh/plist/sed edits. Rotate once to a 24-char alphanumeric to remove the recurring friction. | 2026-07-05 session | OPEN |
| TD-108 | SEC | Per-fact consent-and-routing ledger not yet shipping: every fact must carry sensitivity classification, owner identity, source provenance, consent scope, allowed destinations, retention limit, and immutable audit trail before the router can enforce delivery. PRIMARY LIABILITY-SEVERITY REDUCER - ship pre-scale, not as a later hardening step. Breach blast radius is unbounded without this control. FINAL CHG-6 see HIP_FinalizationOrder. Delivered as HEL: Phase 1 BUILT (a637fa4, dual-write + encryption + crypto-shred); Phase 2 SPEC'd (fact.detect + value.decrypt, LATEST_HEL_PHASE2.md); per-fact timeline = Projection 2 over HEL, not a separate store. | HIP_PropagationWorkOrder CHG-1/CHG-6; specs/LATEST_EPISTEMIC_LEDGER.md | IN_PROGRESS |
| TD-119 | ENG | [model-robustness] Edge model (qwen2.5:7b) phrase-sensitive under PERSONAL_FACT_GROUNDING_GUARD: with "(about Elena) Jardiance 10mg" verbatim in context, "What medication is Elena on now?" answers correctly but "What medication does Elena take?" returns the refusal string. Deterministic; confirmed by prompt-identical A/B 2026-07-08 during TD-114 fix. Guard wording must not be loosened casually — it exists against verified confabulation (see orchestrator.py guard comment). Candidate fixes: guard phrasing that names the "(about X)" annotation explicitly, or paraphrase-robustness eval across N phrasings per fact before any wording change. RESOLVED via four stacked fixes, gated by T119 (5-phrasing paraphrase matrix, best-of-2 per leg): (1) cross-subject admitted facts render in a dedicated 'Confirmed facts about other people' prompt section on question turns (second-person section header beat the inline '(about X)' annotation; A/B inline 9/15 vs section 15/15; instruction-only guard clause had zero effect); (2) imperative openers tell/show/give/list/name/remind added to question-opener regex (was classified declarative, skipped guard, fired write-detection); (3) possessive normalization in entity extraction ('Elena's' now resolves to elena); (4) imperative-dative strip ('tell me X' no longer resolves subject to the requester). Edge text-path temperature 0.3→0.0 (held-fact answers flickered ~1-in-8; demo/test surface is now deterministic modulo Ollama load noise, absorbed by best-of-2). Declarative turns keep inline annotation — partition broke the E1 ack (caught by ratchet, scoped back). Tier L 10/10 twice consecutively, baseline locked. | G2 harness build 2026-07-08 | RESOLVED |
| TD-114 | ENG | [correctness, HIGH, SYSTEMATIC] HITL-3 — Bill's just-asserted Elena→Jardiance fact not retrievable one turn later ("not confirmed yet"). SECOND reproduction of write-then-can't-read pattern (first: TD-111 Sam penicillin). Epistemic pane shows the just-asserted fact in closed/struck state with SUPERSEDE label — active head landing in retired state on write. LIKELY ROOT CAUSE for both TD-111 and this finding: new fact is being written but closed immediately (possibly self-supersede loop residue, or the CORROBORATED seed is being set as the new head while the incoming ASSERTED fact is closed as the prior). Fix must explain why two independent members/attributes both exhibit the same symptom. RESOLVED: two independent defects — (1) Groq "switched from X to Y" emits update+retract; unscoped retract_fact(owner, attribute) closed the just-written head 25ms after creation (closed_by='retracted', no successor); (2) literal "null" subject string (TD-111). Fix in harness/fact_change.py: same-batch retract suppression for updated attributes + subject-scoped retracts + _clean_subject(). Gated by Tier L G2 (both legs), RED-confirmed pre-fix, 9/9 twice post-fix, baseline locked. See docs/testing/LATEST_TD114_FIX.md. | HITL-3 2026-07-08 | RESOLVED |
| TD-115 | ENG | [correctness] HITL-3 turn 3 — query about "my mother Elena" answered with "Dad's health." Wrong subject and wrong gender. Subject resolution is mapping Elena to the wrong household member. May be a Groq extraction error (subject="dad" instead of "elena") or a retrieval/injection error surfacing the wrong member's facts. Reproduce: ask as bill "What do we know about my mother Elena?" — check subject extracted by fact_change and what injection_contract surfaces. RELATED (2026-07-09, Layer 2): stable ack misattribution — Maya says "Ray takes metformin 500mg twice daily", ack replies "YOU take metformin..." even when the write correctly lands subject=ray (graph verified). Ack wording ignores the fact's subject. Pinned as known-failure L2:three_zone_demo.T01. | HITL-3 2026-07-08 | OPEN |
| TD-116 | ENG | [demo] HITL-3 — Dialogue pane does not auto-scroll to newest turn. Evaluator cannot see current exchange without manual scroll. Must auto-scroll on new turn render. RESOLVED (F-6, b5f0acc): scrollIntoView on a sentinel was unreliable inside the flex/overflow:hidden layout; replaced with scrollTop=scrollHeight on the scroll container, fires on each reveal. Browser-verified. | HITL-3 2026-07-08 | RESOLVED |
| TD-117 | ENG | [demo] HITL-3 — Q+A render simultaneously, reads as pre-packaged. Undercuts live-routing story — evaluator sees the answer appear at the same instant as the question, no latency signal that routing/thinking occurred. Consider streaming reply or staged render (Q lands → brief pause → A appears). RESOLVED (F-7, 77dbd01): visibleCount staged reveal — user Q near-instantly, HIP A 700ms later, always a prefix of the current turns array so a shrunk fetch resyncs. Also fixed a stale-session leak found in testing (first transcript poll raced the 1s status poll and used the previous run's session_start, flooding the pane with old history; status now refreshed before the session enables). Browser-verified: 4-turn run, 8/8 turns rendered, no drops/duplicates, stagger visible on every pair. | HITL-3 2026-07-08 | RESOLVED |
| TD-118 | ENG | [demo] HITL-3 — Routing pipeline does not record every turn. Some turns missing from routing row log, giving inconsistent history panel. Evaluator cannot reconstruct full session from the routing view. | HITL-3 2026-07-08 | OPEN |
| TD-113 | GATE | [demo-credibility] HITL-1 — Epistemic pane is developer-view, not operator-view. Raw engine vocabulary surfaces (SUPERSEDE/HARDEN/DERIVED/confidence enums), no visual hierarchy, hero object (medical supersede old→new) buried in list. Required for demo/credibility: (1) group-by-member; (2) confidence as visual indicator (makes trust-regression legible without explanation); (3) supersede event as centerpiece — "changed by X, on [date]" prominent; (4) transition-type labels moved to hover/tooltip. Currently unusable as a live demo pane without narration to decode every field. | HITL-1 2026-07-08 | OPEN |
| TD-111 | ENG | [correctness] HITL-1 turn 6 — Sam's penicillin allergy not retrieved after write+ack. Fact confirmed in timeline (Sam·allergy·penicillin ASSERTED). INJ-6b empty-set guard fires as if no admitted fact exists. Read path or injection contract not surfacing the just-written fact for the speaking member's own personal query. Possible link to TD-044 (INJ-6b keyword bypass). Reproduce: assert allergy as sam, immediately ask "What allergies do I have?" as sam → gets empty-set refusal instead of retrieval. Root cause: Groq literal "null" subject string; fixed via _clean_subject() with TD-114 (same commit); gated by Tier L G2 leg 1. See docs/testing/LATEST_TD114_FIX.md. | HITL-1 2026-07-08 | RESOLVED |
| TD-112 | ENG | [naturalness] HITL-1 turns 6+7 — single refusal string ("I don't have that confirmed yet") used for two structurally different guards: INJ-6b empty-set (missing fact) and INJ-3 cross-member privacy deny. Privacy boundary reads as uncertainty. Audience cannot distinguish "governed refusal" from "data gap." RESOLVED via INJ-7 (F-4): a personal question about another REGISTERED member is refused structurally with access_control_refusal() ("That's Maya's information — I can only share it with Maya") — model never invoked. Existence-invariant: membership is the boundary, so the refusal is identical whether or not the fact exists (nothing leaks). Registered members are now resolvable subjects (retrieval is owner-scoped, so the other member's facts never reach the contract — membership, not INJ-3 denial counts, is the detection signal). Care recipients (Ray/Elena) unaffected. Gated by G4 (4 assertions incl. existence-invariance) + E6 pins the empty-set string; Tier L 11/11 twice, injection harness 11/11. | HITL-1 2026-07-08 | RESOLVED |
| TD-110 | ENG | [governance-decision-required] Cross-member write authority gap — HITL-1 finding. Any authenticated member can supersede another member's health fact (e.g. Maya overwrites Ray's medication record) with no authority check, no provenance gate, and no corroboration requirement. Two unresolved forks: (A) caregiver authority is intended — demo must surface it visibly or it reads as a bug; (B) cross-subject writes should land as UNCONFIRMED and require a second signal to supersede. Also: new fact opens as ASSERTED while superseded seed was CORROBORATED — write path does not check trust-level regression. INJ-3 blocks cross-member READ but no equivalent gate exists on WRITE. Decision required before fix. | HITL-1 2026-07-08 | OPEN |
| TD-120 | ENG | [model-robustness] "What did I tell you about my mother's medication?" retrieval fails — same class as TD-119 (phrase-sensitivity under PERSONAL_FACT_GROUNDING_GUARD). Diagnosis (2026-07-09) found THREE defects: D1 first-person token suppressed named-entity subject resolution (FIXED, 20db3ed — Phase 3 now additive); D3 INJ-2 keywords missed inflections, e.g. plural "medications" (FIXED, 20db3ed); D2 the relational bridge ("my mother's") requires a relationship fact the write path never stores from "My mother Elena ..." utterances — OPEN, separate bounded task (touches Groq extraction). The headline phrasing is a D2 case and still fails: pinned as known-failure L2:care_coordination.T02 in the harness baseline. | care_coordination run 2026-07-09 | OPEN (D2 only) |
| TD-121 | ENG | [write-path, demo-credibility] Groq fact-change extraction appeared NONDETERMINISTIC across runs on identical utterances (three_zone T04 supersede sometimes never landed while the ack claimed it). DIAGNOSIS (2026-07-09, live-verified): Groq at temperature 0.0 is stable PER INPUT (12/12 identical on the T04 prompt); the variance was in the detector's INPUT — call sites passed the turn's retrieval output, and with the target fact absent from the block Groq returns changes:[] 7/8 (measured). Retrieval membership varied because NOTHING in the demo graph carries an embedding (see TD-122), so every retrieval silently falls back to read_user_facts(limit=8, timestamp DESC) — a recency window with timestamp-tie ordering variance. Temperature/seed pinning was already in place and was NOT the cause. FIXED: (F1) detect_and_apply now reads the full active owner+household set itself (limit 50, no embedding dependence) — callers' facts are only a fallback; (F3) write-confirmation reply gate — a supersede-phrased declarative whose detection cycle produced 0 mutations and 0 idempotent no-ops gets an explicit "unable to save, nothing was changed" reply instead of the model's false ack (no silent retry, by decision). Gated by Layer 2 with T04/T05 un-quarantined. AMENDED 2026-07-16 (risk-memo §9 item 0, HIP_SIA_PhaseB__risk-memo__v20260716_1624.md): F3's phrase scope (_SUPERSEDE_PHRASE_RE) let a plain declarative with no supersede keyword ("I take atorvastatin 20mg every morning") skip the check entirely — a zero-write outcome for it shipped as a false ack (I-10's cited evidence turn). Widened to every declarative (`is_declarative_utterance`), one condition, live-verified (forced Groq 401 → sam's shellfish-allergy declarative correctly refused instead of falsely acked). See TD-125 for the retry shipped alongside it. | Layer 2 first sweep 2026-07-09; amended 2026-07-16 | RESOLVED |
| TD-122 | ENG | [retrieval, systemic] memory_engine/store.encode() writes every fact with embedding: None ("engine track doesn't embed in Phase A") — and BOTH the demo seed (scripts/demo_seed.py) and the fact-change write path route through encode(). Consequence: NO fact in the demo/harness graph has an embedding; search_facts_by_embedding always returns [] and every retrieval silently runs on the read_user_facts fallback (timestamp DESC, limit voice_max_facts=8). "Semantic retrieval" is currently a recency window. This was the hidden variance source behind TD-121 and is a standing risk: any graph >8 visible facts silently drops the oldest from model context. TD-030 TENSION, and its resolution pattern: embedding the VALUE regresses TD-030 (embedding invertible next to ciphertext) — but extraction_queue.write_facts already demonstrates the safe pattern, embedding subject+predicate ONLY (embed_text(f"{owner} {attribute}")). Fix direction: encode() adopts the same subject+predicate embedding; separately consider raising/removing the fallback's limit-8 window. **RE-CONFIRMED LIVE 2026-07-21 (REQ_CRYPTO_HARNESS_V2, vector-index taint coverage work), with the missing piece of WHY named for the first time: harness/fact_change.py's live detect_and_apply was migrated from write_facts() to store.encode() specifically for Phase 2/crypto reasons — that file's own line-749 comment: "route through encode() instead of write_facts() so the new node gets write_state + confidence_log" — meaning the migration that caused this was deliberate and unrelated to embeddings; nobody noticed it silently orphaned write_facts()'s embedding computation as a side effect. Live-verified (memory_engine/store.py:210-211): a fact written via the current live path (`_write_plain` -> `store.encode()`, the same call `fact_change.py` now makes) carries `embedding: None`; a fact written via the still-live-but-uncalled `extraction_queue.write_facts()` DOES get a real 768-dim embedding, confirming the dead-code diagnosis precisely. NEW IMPACT surfaced by this REQ: the vector-index taint guarantee (a canary planted in a fact's value can never appear in the embedding, because only owner+attribute are embedded) is only PROVEN STRUCTURALLY today, on the one code path that still computes an embedding — it is not exercised by any live-written fact, because no live-written fact has an embedding at all. Confirming this held REQUIRED calling write_facts() directly on a throwaway synthetic fact, bypassing the live path entirely, since no real fact in the graph could be used for the check.** | TD-121 diagnosis 2026-07-09; re-confirmed 2026-07-21 | OPEN |
| TD-123 | ENG | [write-path, data-model] Groq fact detector misfiles the fact's VALUE into the person-typed subject slot (subject="shellfish" for "I'm allergic to shellfish") — P2 i008 root cause. FIVE fix attempts each patched one downstream layer and regressed another: INJ-2 shortcut (fact never reached INJ-2), INJ-1 bypass 183d372 (admitted but then partitioned to other-people), retrieval merge b7ca9e6 (no-op in harness: NO fact has an embedding, see TD-122), known-persons write guard 9c441ff (nulled first-introduction elena; reverted 7cce12e), value-bleed write+partition guard 0fce966/47aecd0 (narrative values name their person subject — seed D9 "Ray takes metformin..." — broke PW012; partition change reverted). FINAL: single intervention at the write boundary — _normalize_subject in _apply_changes enforces the subject field's person type BEFORE the fact acquires graph identity: accept iff subject is a known person (members + existing fact subjects) OR (named in the utterance AND not bleeding from the change's own value). Covers corrupt entities (in value), first introductions (in utterance, not in value), and narrative values (known short-circuits before bleed is consulted). WARNING on every null. Read side fully restored to the T119-gated baseline; 183d372 INJ-1 bypass retained for live graphs with legacy rows. REMAINING: detector prompt hardening ("subject must be a PERSON, never the allergen/item/event" + negative example). CITATION WARNING (2026-07-17, `DISPATCH_ENUM_AUDIT__seed-fixtures-outside-detector-schema__v20260717_1135.md`): this ticket has been cited as the fix track for at least two OTHER, DIFFERENT bugs that are not subject-slot misfiling — `DIAG__p2-i019-detection-miss__v20260714_1500.md` cites "TD-123" for dietary/preference attribute disambiguation, and D-21 (HIP_DefectRegister) was cited as fixed-via-TD-123 for a missing-enum-category gap (`incident` not in `CANONICAL_ATTRIBUTES`). Neither is this bug. Before citing TD-123 as a fix track, check that the target defect is actually subject-slot person-typing — it usually isn't. | P2 i008 diagnosis 2026-07-12 | OPEN (normalization shipped, prompt hardening pending) |
| TD-124 | ENG | [write-path, resilience] Fact-change detector migrated Scout->openai/gpt-oss-20b under Groq's 2026-07-17 Scout retirement (b394d89; A/B harness eval/fact_change_ab.py + 24-case golden set f2f7007; Developer tier 1,000 RPM required — free tier 30 RPM caused the P2 i016/i019 429-drop class). P1 FOLLOW-UPS, in order: (1) durable outbox for detection writes — persist-first, replay on failure, NEVER silent-drop (today a Groq outage past the retry budget silently loses the fact while the poll window burns); (2) LiteLLM gateway in front of the detector call for provider portability; (3) DeepInfra as automatic fallback provider. Tertiary local fallback (Qwen 7B on Mini) requires a 500-1000 example validation set before it may enter the chain — the 24-case golden set is a migration smoke, not a fallback qualification. | Scout retirement 2026-07-12 | OPEN |
| TD-125 | ENG | [write-path, resilience] risk-memo §10 required a detection retry to ship alongside TD-121's F3 widening: gating every declarative on zero-write means Groq's P2/i019 semantic false negative (gpt-oss-20b returns changes:[] for some multi-party contexts, deterministic at temperature=0.0) now surfaces as a spoken refusal instead of a silent no-op — a silent lie traded for a loud, frequent failure without a retry. MITIGATION (2026-07-16): `detect_and_apply` retries once at temperature=0.2 (non-identical resample, since a same-temperature retry of a deterministic call cannot recover) before finalizing a zero-changes outcome. COST, not fully closed: (1) every genuinely-idempotent turn (Groq correctly returns changes:[], ~29-40% of turns per DIAG__p2-i019-detection-miss__v20260714_1500.md §7) now costs a second Groq round-trip before the outcome is stored — doubles detection latency for that class, and pushes some turns closer to DETECT_WAIT_S=30s; (2) temperature=0.2 is a heuristic, not a validated recovery rate — no measurement yet of how often it actually recovers a true false negative vs. just adding latency to the legitimate-empty case. TD-123's prompt-hardening (dietary/preference disambiguation) is still the correct fix for the false-negative rate itself; this retry only prevents that rate from becoming user-visible in the interim. | risk-memo §10, 2026-07-16 | OPEN |
| TD-126 | OPS | `scripts/demo_reset.py`'s log and voiceprint paths are hardcoded to `~/hip-harness` (`_HARNESS_ROOT = pathlib.Path.home() / "hip-harness"`) regardless of which checkout invokes it — running it from hip-dev during REQ_D03_D18's live proof (2026-07-16) deleted `hip-harness/data/voiceprints/maya.npz`/`sam.npz` (the frozen demo checkout's speaker-verification enrollment) and truncated its `voice_orch.log`/`router.jsonl`, even though Neo4j deletion was correctly scoped to the dev instance via `NEO4J_URI`. `demo_seed.py` recreates SYNTHETIC placeholder voiceprints on reseed, not a re-enrollment of whatever audio was actually there. **PARTIALLY FIXED (REQ_TD126_REMEDIATION, 2026-07-16): `_LOGS` derived from `ROOT`/`__file__` (matches `turns_demo.jsonl`'s existing pattern) — a hip-dev reset now truncates hip-dev's own `logs/voice_orch.log`/`router.jsonl`, verified live (hip-harness's copies' mtimes confirmed unchanged across a live run). Voiceprint deletion still reaches hip-harness for maya/sam specifically — that path comes from the REGISTRY's recorded `voiceprint_path` (set by `harness/speaker_id.py:43`'s shared `DATA_DIR` at enrollment/seed time), not this script's local constant, and fixing it would touch the shared-voiceprint architecture, out of scope for this build. CORRECTED fix direction (supersedes the original "re-enroll" note above): per item 3 of the same dispatch, DO NOT re-enroll voiceprints and DO NOT build speaker verification further — see TD-127.** | REQ_D03_D18 live-proof session, 2026-07-16; log-path half fixed 2026-07-16 (REQ_TD126_REMEDIATION) | PARTIALLY FIXED |
| TD-127 | GATE | **Speaker verification (Resemblyzer GE2E) is a stand-in for a vendor, not a production component — error rate unquantified.** Same posture as Deepgram/Cartesia on the voice leg: a placeholder proving the seam exists, not a shipped capability. Real vendors for this exact problem exist and are already deployed at scale (Pindrop, Nuance, Veridas; cable operators already run Pindrop in call centers) — HIP is not competing on speaker verification itself. First measurement (2026-07-16, `DISPATCH_TD126__speaker-verification-floor-analysis__v20260716_1846.md`): a genuinely different real voice recording scored **0.632** against the **0.50** "medium" threshold the main attribution path trusts — a different human clears the bar. Independently reproduced same-day during REQ_TD126_REMEDIATION's live verification: two different real audio samples scored 0.677 and 0.654 against Bill's own real print. This is not a bug to fix with a better threshold; it is a research library (GE2E, not built for adversarial/production speaker verification) being asked to do a job production verifiers are built and sold for. **Do not claim speaker ID in any demo or document.** The deliverable is the seam (governed context assembly keyed off an identified speaker) — swapping in a real vendor later does not touch the seam's shape. | DISPATCH_TD126, item 3 of the follow-up dispatch, 2026-07-16 | OPEN — by design, not a target for this codebase to close |
| TD-128 | SEC | **Frontier tier (Script 1 T04, `harness/frontier_client.py`) uses ONE household-wide Anthropic API key from `~/.env.dev`, not a per-member key.** Bill, 2026-07-17 (DISPATCH FRONTIER_TIER_BUILD Q4): "one key in ~/.env.dev, same as everything else. Per-member key storage is debt, not demo work. Register it." Any household member who can reach the disclosure gate (harness/disclosure.py) can trigger a spend/usage-visible call under the same credential — no per-member attribution, rotation, or revocation exists. Same convention as GROQ_API_KEY/OPENAI_API_KEY elsewhere in this repo today, so this is not a regression, but it is the first time a BYOK-framed feature ("Maya's key" in the prep doc's own language) actually ships on a shared key instead of a per-member one — the framing and the mechanism now visibly diverge. Fix direction: per-member credential storage (encrypted at rest, keyed to member_id) before this feature is presented as "Maya's key" rather than "the household's key." Separately noted live during this build: the key currently in `~/.env.dev` returned `401 authentication_error` ("API key is invalid") on a real call — reported to Bill directly, not a debt-register item (a broken credential isn't tech debt, it's a blocker; see DISPATCH_FRONTIER_TIER_BUILD's OPEN section). | DISPATCH FRONTIER_TIER_BUILD, 2026-07-17 | OPEN |
| TD-129 | OPS | **Two `ollama serve` daemons running simultaneously on this box** (PID 1399 since 2026-07-11, PID 937 since 2026-06-03), plus multiple `ollama runner` subprocesses, causing intermittent local-model inference timeouts under load. Found during REQ_MASTERKEY_PATH's `--full` verification (2026-07-18): `L3:INJ-7` and `L4:PW027` both failed with `"Sorry, I couldn't process that."` (`server/voice_orch.py:3238`), traced to `APITimeoutError('Request timed out.')` on the local-Ollama fallback call. Root-caused as pre-existing resource contention, not fix-induced. **CORRECTION (2026-07-19): the original "fix direction" below was wrong and, when tried, took down a load-bearing service.** The two daemons are NOT a stale/live duplicate pair -- they serve two DIFFERENT, both-necessary ports: PID 1399 is a harness-classifier-pinned instance on :11435 ("[harness] classifier Ollama ready at http://localhost:11435"); PID 937 (brew-managed, `homebrew.mxcl.ollama.plist`) was the DEFAULT-port :11434 instance that embeddings, model warmup, and the local text-query fallback all depend on. Stopping PID 937 via `brew services stop ollama` (tried 2026-07-19 on the theory it was the stale one) took :11434 fully down (`Connection refused`, not a timeout) and cascaded into 41 harness failures across L1-L4 -- an environment outage, not a code regression. Restored immediately via `brew services start ollama`; both ports confirmed healthy again. Real root cause is still open: two genuinely necessary services contending for the same underlying GPU/inference hardware under a 100-iteration harness load, occasionally pushing a local call past its timeout. Neither daemon is safe to simply kill. Fix direction (revised): investigate whether the two services can share load more gracefully (e.g. raising the local-call timeout, or scheduling the harness run to avoid overlapping heavy local-model use elsewhere), not "stop one." | REQ_MASTERKEY_PATH `--full` verification, 2026-07-18; correction 2026-07-19 | OPEN |
| TD-130 | OPS | **`scripts/demo_player.py` cannot reach an HTTPS/self-signed-cert server** (e.g. `server/voice_https_orch.py`, the actual server behind `hip_client.html` on port 7860) — its `requests.post` call passes no `verify=False`/cert handling at all, so any HTTPS target fails with `SSLCertVerificationError: self-signed certificate`. Found live during `REQ_STEP2_CALLER_SIGNING`'s proof (2026-07-19): the script's own signing fix worked (confirmed against a throwaway plain-HTTP `demo_dashboard.py` instance, real reply returned), but the identical command against `voice_https_orch.py` over HTTPS failed on the cert, not on identity. The script's own default target (`http://localhost:8080`, `server_default` in `main()`) is plain HTTP, suggesting `voice_https_orch.py` (HTTPS-only, self-signed cert, port 7860) was never its actual intended target — but this was never confirmed, and the mismatch was never previously visible because the script has presumably not been run against a live HTTPS demo server. Fix direction: either add `verify=False` (matching every other dev-tooling caller of this box's self-signed cert, e.g. `eval/integration_live.py`, `HarnessServer`) if HTTPS targets are meant to work, or leave it HTTP-only and correct the docstring/usage examples to stop implying it works against `hip_client.html`'s actual server. | `DISPATCH_STEP2_CALLER_SIGNING__hip-client-demo-player-preflight-tier-l__v20260719_1254.md`, 2026-07-19 | OPEN |
| TD-131 | OPS | **A fresh `git worktree` checkout can't run `eval.harness` without two manual, undocumented steps.** Found building REQ_CRYPTO_P1_DYAD_KEYS (2026-07-20) on a brand-new worktree (`roadmap-crypto-p1`): (1) bare `python3 -m eval.harness` fails inside the harness-owned subprocess server with `ModuleNotFoundError: No module named 'pipecat'` — the project venv is `[REDACTED-USER-PATH]/hip-dev/.venv`, shared across worktrees, but nothing in `eval/harness.py`/the harness server checks or documents this, so a plain `python3` looks like it should work and doesn't; (2) `certs/voice.key` (gitignored, only `voice.crt` is tracked) doesn't exist in a fresh worktree, so the subprocess server fails with `Missing cert/key: .../certs/voice.crt, .../certs/voice.key` even though `voice.crt` IS present — worked around by copying a matching key+cert pair from `hip-dev`'s checkout (confirmed matching pubkey via `openssl x509 -pubkey`/`openssl pkey -pubout`), not by regenerating (which would have produced a mismatched, uncommitted `voice.crt` diff). Fix direction: a `--guards`-time check (mirroring the existing `DEV_MARKER.txt`/`NEO4J_URI` checks in `eval/harness.py:_guards`) that fails loud with the exact fix ("run via hip-dev/.venv/bin/python3", "certs/voice.key missing, copy from an existing worktree or regenerate with certs/san.cnf") instead of a bare traceback three layers down. | REQ_CRYPTO_P1_DYAD_KEYS build, 2026-07-20 | OPEN |
| TD-132 | SEC | **Canary/taint scanning (`eval/harnesslib/canary_taint.py`, REQ_CRYPTO_HARNESS_V2) matches on literal substring, so it catches a verbatim leak but not a paraphrased one.** Found live 2026-07-21 building `CT-OUTPUT-GAP`'s real model call: the local edge model naturally paraphrases rather than quoting a fact's value verbatim — a bracketed canary marker embedded in AUTHORIZED content (f1, the alice/bob/mary fixture) did not survive into the model's real reply ("You take lisinopril daily for your health." — the medication name persisted, the literal canary tag did not). This is not specific to that one check: `scan_for_canary`'s whole design (`plant_canary`/`scan_for_canary`) is a literal-substring search across every surface (prompt, retrieval, vector index, logs, output). If a FORBIDDEN fact's secret were restated in different words on its way into any of those surfaces — a summary, a translation, a synonym swap, a number reformatted — instead of quoted directly, the planted token would never appear and the scan would report clean while the secret's substance still leaked. IMPACT: output-taint (and every other canary-based surface this REQ checks) currently proves the literal marker didn't appear verbatim, NOT that the underlying secret didn't leak in substance — a materially narrower guarantee than "the review prescribed" methodology implies at face value. This is a real hole in the taint method itself, not a bug in one check's construction. Fix direction: a SEMANTIC-canary approach — e.g. embedding-similarity between the forbidden fact's value and each downstream surface's content (flag anything above a similarity threshold, not just an exact substring match), or an explicit paraphrase/entailment check — replacing or supplementing literal-substring matching. Not attempted here; this register entry exists so the gap is never silently assumed closed by a passing `scan_for_canary` result. | REQ_CRYPTO_HARNESS_V2, CT-OUTPUT-GAP build, 2026-07-21 | OPEN |
| TD-133 | GATE | **Four-part check-discipline gaps (REQ_HARNESS_DISCIPLINE): standing audit (`eval/harnesslib/harness_audit.py` + `check_registry.py`, runs on every `--full`).** The audit enumerates all gated checks (L7/L7V2 Scenario scan + L6 record invariants) and verifies each declares a fault-injection twin, ground-truth fixture, coverage entry, and metamorphic wrapper. Reference implementations pass all four (PS1/PS2/OB4/OB5/G0/OB6 + G1-G4). **BURN-DOWN 2026-07-26 (same day, later): 7 TWIN gaps CLOSED** — `seal-to-dyad` (a node stamped `key_version=1` despite a real dyad-sealed DEK correctly fails the `kv==2` stamp check), `DK1` (a genuine plaintext/unwrapped D_priv row inserted directly is caught by the parse-as-bare-X25519 check, removed immediately), `DK4` (a wrap row for a fake dyad_id, bypassing `create_dyad`, correctly flips the SAME `events>=wraps` comparison DK4 uses once scoped to that one dyad_id — the global aggregate has too much historical slack, 241 vs 137, for a single-row injection to move it, so this scopes the identical formula narrowly instead), `P1` (removing sam's real `household_key_wraps` row locks him — and only him — out of a real household fact bill/maya still read; row restored and re-verified immediately after), `N1` (a fact stamped `owner='bill'` but actually sealed to maya's member key is correctly decryptable by maya, proving the cross-member-decrypt count can go non-zero), `N4` (a real, bill-owned, bill-decryptable fact whose value coincidentally matches a dyad_values string IS flagged by the same value-match logic — read_user_facts's own decrypt-or-skip behavior means a mis-stamped-owner dyad fact could never reach bill's view in the first place, so this is the correct-granularity proof of the check's OWN match logic), `P4` (row4's real classification checked against a deliberately wrong expected class correctly reports MISMATCH). All 7 verified live: `L7: 23/23`, `AUDIT: 3/3`, `four-part-roster PASS`, `RATCHET PASS`, no side effects left behind (each SQL-level injection removed in a try/finally; P1's restore additionally re-verified). **REQ_CRYPTO_P3_OPERATOR_BLIND part (c) step 5 reconciliation, same session:** PS1/PS2's own dedicated fault-injection twins (`PS1-fault-injection`/`PS2-fault-injection`) were retired along with PS3/PS4 (Bill's decision: v1-simulation fixtures, now impossible on roadmap post master-key-destruction) — PS1/PS2 registry entries now honestly debt-flagged (were stale `{"scenario":...}` refs to deleted scenarios) rather than silently broken; PS3/PS4 registry entries removed entirely; `L7:OB6` (the destroyed-state successor) registered with `twin: {"na": ...}` (self-fault-injecting, same pattern as `L7V2:MT2`). REMAINING gaps, by artifact (UPDATED 2026-07-27, ITEM 5 first slice — see BURN-DOWN below, DK2 and MT1 CLOSED and removed from this TWIN list): TWIN — PS1 (retired, see above), PS2 (retired, see above), RI1, P4-EXT(+row12/row13), RE2-RE7 (inline fault attempts exist per REQ_CRYPTO_P4 but markers were not individually re-verified by this audit build), ABM-route, FF1, FF3, FF4, SC1, SC1-E2E, CT-OUTPUT-GAP have no red-on-command demonstration. FIXTURE gaps — RI1 (structural assertion, no human-verified fixture row). METAMORPHIC gaps (seal-to-dyad/DK2/N2/DK3/P2/P1/N1/N4 CLOSED 2026-07-27 via property-based generation, removed from this list) — P4/P4-EXT family (write_rule.classify's write-time directive/attribute path — explicitly SKIPPED 2026-07-27, Bill has an open ruling on whether write-time directives should check attribute and closing these mutants would lock in current behavior before he decides; not attempted, not silently dropped), RE1-RE7, CT/CT-VECTOR-INDEX/CT-OUTPUT-GAP (inherit TD-132's paraphrase-evasion limit — a literal-substring canary IS the missing semantic-metamorphic wrapper), SC1/SC1-E2E, MT2-DECRYPT-REVOKE still have no rewording/property wrapper. Additional characterized limit found building the audit's `ps_scan_pattern` probe: the PS1/OB4 static-scan regex does not match a call with a space before the paren (`encrypt_fact_value (x)`) — asserted as current behavior in the probe so any change is noticed. Every flag prints on every run (FLAGGED, not silent); burn-down = replace `{"debt": "TD-133"}` entries in `check_registry.py` with real scenario/marker/probe refs. This entry is the register-side half of the audit's "zero HIDDEN gaps, not zero gaps" contract. **BURN-DOWN 2026-07-27 (ITEM 5 of Bill's five-item 2026-07-27 sprint instruction, first slice — 10 of the 46 then-open TD-133 gaps CLOSED, in the priority order docs/deliverables/HIP_TestingBestPractices gives: "CheckList template expansion + Hypothesis generation... closes the largest number of flagged twin/metamorphic gaps with two mechanisms" ranked first):** PROPERTY-BASED (Hypothesis-style; the `hypothesis` package is not installed in this venv, so — per checklist_gen.py's own precedent of an in-repo generator over the real CheckList library — this is a small in-repo, seeded (`random.Random`, deterministic) value generator, `eval/harnesslib/layer7_crypto.py::_property_seeded_values`) closed 8 checks' metamorphic gaps: `seal-to-dyad`, `N2`, `DK3`, `P2`, `P1`, `N1`, `N4` each now re-assert their own invariant across 6 generated synthetic fact VALUES (not the one hand-picked demo string) through the SAME write helpers every other check in this file already uses, cleaned up by the SAME written_fact_ids/finally mechanism. `DK2` closed BOTH its twin and metamorphic gaps — the twin needed zero new code: `L7:fault-injection` (the existing misseal-to-wrong-D_pub fault) already asserts maya, DK2's own rightful custodian, is locked out of her own dyad's fact under that fault — DK2's exact property, inverted — so its registry twin now reuses `{"scenario": "L7:fault-injection"}`, the same twin N2/DK3/P2 already reference. CHECKLIST-MECHANISM closed 2 checks' twin gaps: `MT1`/`MT1-CHECKLIST` — neither proved the admit/deny judge (`apply_injection_contract`) could actually say NO; added ONE fault-injection (not a new corpus member — REQ_CRYPTO_HARNESS_V2's own CONSTRAINT that MT1/MT2 must never be conflated is respected) asserting bob (enrolled, but not on alice's dyad for f1) is correctly DENIED the SAME base query alice's 12/168 variants all admit, via INJ-3's cross-member-deny rule (`harness/injection_contract.py:475-519`) — live-verified: `bob_admitted=False`. MT1-CHECKLIST's twin reuses MT1's new one (`{"scenario": "L7V2:MT1"}`) — same judge, same directional-flip property, same reuse pattern ABM-route/FF1/FF3/FF4's metamorphic entries already use. EXPLICITLY SKIPPED, not attempted: `P4`, `P4-EXT`, `P4-EXT-row12`, `P4-EXT-row13` (7 flagged lines) — all on `write_rule.classify`'s write-time directive/attribute path; Bill has an open ruling on whether write-time directives should check attribute, and closing these mutants would lock current behavior in as intended before he decides. Also not attempted this slice (deferred, not skipped-forever): `PS1`/`PS2` (retired v1-fixtures — rebuilding either would violate the post-destruction/no-master-key-touch constraint), `CT`/`CT-VECTOR-INDEX`/`CT-OUTPUT-GAP` (belong to the doc's priority-2 "semantic canary" mechanism, not this slice's CheckList/Hypothesis focus), `RI1`/`RE1-RE7`/`ABM-route`/`FF1`/`FF3`/`FF4`/`SC1`/`SC1-E2E`/`MT2-DECRYPT-REVOKE` (kept the slice at exactly 10 checks, per Bill's own "ten, not more, so the work stays reviewable"). VERIFIED LIVE: `L7: 24/24`, `L7V2: 27/28` (1 unchanged opt-in skip), `AUDIT: four-part-roster PASS` (56 checks enumerated, 0 missing, 35 debt-flagged gaps — down from 46), `RATCHET PASS` — checkpointed after every stage of this build (property generator, directional twin, each registry batch), not just once at the end. `harness/g0_invariant.py`, `harness/write_rule.py`, and every other file outside `eval/harnesslib/layer7_crypto.py`, `eval/harnesslib/layer7_crypto_v2.py`, and `eval/harnesslib/check_registry.py` is untouched. See `docs/dispatches/DISPATCH_TD133_BURNDOWN_ITEM5__checklist-and-property-based-ten-checks__v20260727_1731.md` for the full session record.** | REQ_HARNESS_DISCIPLINE audit build, 2026-07-26; burn-down (7 twins) same day; ITEM 5 first slice (10 checks) 2026-07-27 | OPEN (35 of original 46 remain; write_rule family untouched pending Bill's ruling) |
| TD-134 | GATE | **Mutation-score survivors (REQ_COVERAGE_MEASUREMENT Metric 2, `eval/harnesslib/mutation_score.py` + `mutation_targets.py`, wired as `L7V2:MUTATION-SCORE`): 50 of 128 generated mutants SURVIVED (score 0.61) across the four named gate modules — each is gate logic no current check exercises, located to file:line. NO-SILENT-DISAPPEARANCE RULE (REQ acceptance item 7): this list is the baseline. A later run's survivor count/list may only shrink if each removed entry is either (a) killed by a new/strengthened killer in `mutation_targets.py`, or (b) still present but explicitly carried forward under this ID with a note — a shrink with no accounting is a FAIL of the metric's own contract, not a quiet improvement. BASELINE, by module: **`harness/injection_contract.py` `_inj1_subject_scope`** (1/5 survived) — :413 `delete_last_operand(Or)`. **`_inj2_relevance`** (10/20 survived) — :443 `delete_last_operand(Or)`; :447 `flip_bool_const(True->False)`; :450 `delete_last_operand(Or)`; :450 `swap_compare Eq->NotEq`; :450 `delete_last_operand(And)`; :451 `swap_compare IsNot->Is`; :452 `swap_compare In->NotIn`; :453 `flip_bool_const(True->False)`; :460 `swap_compare In->NotIn`; :471 `flip_bool_const(False->True)`. **`_inj3_cross_member_deny`** (6/15 survived) — :505 `flip_bool_const(True->False)`; :508 `delete_last_operand(Or)`; :510 `delete_last_operand(Or)`; :514 `negate_if_test`; :517 `negate_if_test`; :518 `flip_bool_const(True->False)`. **`apply_injection_contract`** (1/9 survived, INJ-7 block only) — :619 `delete_last_operand(And)` (drops the `intent in _PERSONAL_INTENTS` clause from the refusal guard — no current killer exercises a non-personal-intent turn through this exact path). `_inj4_household` and `_inj5_never_volunteer`: 0 survivors (both 100%). **`harness/write_rule.py` `classify`** (0/34 survived — ALL 19 KILLED 2026-07-27, dispatch 30, see UPDATE below) — none as of that dispatch. UPDATE 2026-08-05 (D-R-176, rule 3a subject-gate fix, REQ_STRUCTURAL_CEILING R11): 4 survivors as of this dispatch, ALL ACCOUNTED FOR, not a coverage loss — :217 `delete_last_operand(And)`, :218 `swap_compare Is->IsNot`, :218 `swap_compare Eq->NotEq`, :218 `delete_last_operand(Or)`. The first two are the SAME two mutants this dispatch's own intermediate run (2026-08-05T15:03Z) recorded at :204 `swap_compare Is->IsNot` and :204 `delete_last_operand(And)` — D-R-176's fix added a third OR-clause (`subj == "household"`) and reformatted the if-statement across multiple lines, shifting coordinates 204->217/218 with no change to which branches were already uncaught. The other two are genuinely NEW mutation sites on that newly-added clause (built to close a real blast-radius finding: a household-attribute fact authored by a real member with the 'household' pseudo-subject sentinel was wrongly narrowing before this fix). Same brittle file:line-addressing class TD-142 already names for injection_contract.py's :619->:664 shift — not burned down here; D-R-176's own scope was closing R11's 3a gap, not a mutation-score burn-down. **`harness/g0_invariant.py` `check_g0`** (1/8 survived — 4 killed 2026-07-27, see UPDATE below) — :67 `delete_last_operand(Or)` (confirmed EQUIVALENT MUTANT, see UPDATE — not a live coverage gap). **`eval/oracle/record_invariants.py`**: `g1_no_orphan_generation` (3/12 survived) — :62, :76, :78, all `delete_last_operand(Or)`; `g2_no_cross_owner_admission` (3/5 survived) — :95, :96, :97, all `delete_last_operand(Or)`; `g3_guard_implies_no_inference` (1/5 survived) — :117 `delete_last_operand(And)`; `g4_no_double_valued_attribute` (1/10 survived) — :171 `delete_last_operand(Or)`. PATTERN across nearly every survivor: `delete_last_operand` on a multi-clause `and`/`or` — the existing killer probes each exercise ONE clause's true/false state but none constructs a case where dropping the LAST clause specifically changes the outcome (e.g. a fact that satisfies every OTHER clause but only the dropped one would have blocked/allowed it). Closing these is mechanical per-site (one more killer probe per survivor) but not attempted in this build — Bill's dispatch scoped this session to building and verifying the metric itself, not burning down what it found. Fault-twin self-test (both directions) PASS, verified live: seeded mutant (`negate_if_test` @ `injection_contract.py:619`, the INJ-7 refusal condition) killed with its 3 designated killers present, survives with them excluded. `L7V2:MUTATION-SCORE`/`MUTATION-SCORE-SELFTEST` registered in `check_registry.py`, AUDIT roster clean (rows=53, missing=0). UPDATE 2026-07-27: the no-silent-disappearance rule stated above is no longer aspirational -- it is mechanically enforced every run. eval/harnesslib/mutation_score.py now persists each run's survivors to logs/mutation_survivors.jsonl and compares against the previous run's record; this exact baseline (78/128 killed, these 50 survivors) is what the first persisted run wrote, matching this entry number for number. A future run's survivor list may only shrink if each removal is killed (present in that run's own killed-mutant set) or still named here under this ID -- verified live via two real harness runs plus a four-direction synthetic self-test, wired as L7V2:MUTATION-NO-SILENT-DISAPPEARANCE and its SELFTEST twin. UPDATE 2026-07-27 (later, burn-down): four of the five `harness/g0_invariant.py:check_g0` survivors killed by four new killer probes added to `mutation_targets.py` -- `_kill_g0_resolved_without_admitted_fires` (:65 `delete_last_operand(And)` -- exercises resolved_subjects truthy / admitted_facts falsy, the non-empty-but-mismatched half of line 65's OR the module's own docstring names, never previously constructed by a killer), `_kill_g0_none_member_no_crash` (:68 `delete_last_operand(Or)` -- member=None, distinct from merely-empty, crashes the mutant on `.strip()`), `_kill_g0_none_resolved_subjects_no_crash` (:74, same None-vs-empty distinction on `list(resolved_subjects or [])`), `_kill_g0_none_admitted_facts_no_crash` (:75, same on `len(admitted_facts or [])`) -- each verified to return None (no false-kill) against the real unmutated check_g0 and to positively kill only its own targeted mutant. check_g0 score 3/8 (0.38) -> 7/8 (0.88); overall 78/128 (0.61) -> 82/128 (0.64), all other modules' survivor counts unchanged (verified via a full re-run of the sweep). The fifth survivor, :67 `delete_last_operand(Or)` on `find_named_tracked_persons(reply or "", known_subject_ids)`, was investigated and is a TRUE EQUIVALENT MUTANT, not a coverage gap: every falsy `reply` (only `None`/`""` are possible for a `str | None`-typed input) is caught identically by `find_named_tracked_persons`'s own `if not reply: return []` guard whether or not the `or ""` fallback ran first, and every truthy `reply` short-circuits `or` to the same value either way -- no input to `check_g0` observably distinguishes the mutant from the original. Killing it would require changing `check_g0` or `find_named_tracked_persons` behavior, out of scope for a mutation-testing burn-down (and outside this session's mandate not to change check_g0's behavior). `harness/g0_invariant.py` itself was not modified — only `mutation_targets.py` (killers) and this register entry changed. See `docs/dispatches/DISPATCH_G0_MUTATION_BURNDOWN__kill-five-survivors-of-check_g0__v20260727_1648.md` for the full session record. This entry (TD-134) itself is what a disappearing survivor gets checked against by name, so it should not be deleted or rewritten out from under the mechanism -- burning down an individual survivor means removing ONLY that survivor's own file:line/operator line above, not this entry's structure. UPDATE 2026-07-27 (later, dispatch 30): `harness/write_rule.py:classify` burn-down — all 19 survivors KILLED, 0 remain, score 15/34 (0.44) -> 34/34 (1.00). Ten new killer probes added to `eval/harnesslib/mutation_targets.py`, `classify()` itself untouched: `_kill_classify_none_subject_resolves_via_first_person` (:146, both `delete_last_operand(Or)` sites on `subj = (subject or "").strip().lower() or None` — subject=None with a first-person utterance distinguishes both the crash-on-None-strip mutant and the skip-resolution mutant in one call), `_kill_classify_coordination_attribute_blocks_narrowing` (:216, the `delete_last_operand(And)` that would let a coordination attribute reach 3c's mandatory-exclusion return, which is 3b's territory not 3c's), `_kill_classify_share_household_widens_self` + `_blocks_other_subject` (:166's four mutants, the household-circle widening restriction's self-vs-other distinction), `_kill_classify_share_care_team_positive` + `_requires_enrollment` (:170/:174/:178, an active-caregiver fixture plus a recipient-exists-but-nobody-enrolled fixture), `_kill_classify_flag_safety_positive` + `_requires_enrollment` (:181/:184, same shape), `_kill_classify_coordination_default_positive` + `_requires_enrollment` (:200/:205, level 3b's AND-chain and its `subj in exclude` ternary — an EMPTY exclude set already distinguishes `in`/`not in`, no standing_policy fixture needed). Six of the ten need `harness.care_team_keys` state to exist — built via RAW SQL insert/delete-in-finally directly against `care_teams`/`care_team_members` (same pattern DK1/DK4/P1 already use for `dyad_registry`/`household_keys`), deliberately bypassing the public `add_caregiver`/`ensure_care_team` API because those emit an APPEND-ONLY HEL `care_team.grant` event with no erasure path for a synthetic test row — SQLite fixture state only, the Neo4j graph is never touched, no reset, no reseed. All ten killers verified to return `None` (no false-kill) against the real unmutated `classify()` before being wired in. Overall mutation score: 78/128 (0.61) baseline -> 82/128 after the 2026-07-27 g0_invariant slice -> **101/128 (0.79)** after this slice — verified live via a full re-sweep, all other targets' survivor counts unchanged. No findings: every one of the 19 was killable without changing `classify()`'s behavior — none left as a debt-registered gap this time. See `docs/dispatches/DISPATCH_30__write_rule_ruling-and-mutation-burndown__v20260727_1926.md` for the full session record, including the write_rule-directive/attribute RULING this same dispatch recorded in REQ_COVERAGE_MEASUREMENT and REQ_PARTITION_CUSTODY. | REQ_COVERAGE_MEASUREMENT Metric 2 build, 2026-07-26; g0_invariant burn-down 2026-07-27; write_rule.classify burn-down 2026-07-27 (dispatch 30) | OPEN (baseline; enforcement mechanism live 2026-07-27; write_rule.classify fully closed, g0_invariant.check_g0 1/8 remains — confirmed equivalent mutant, not a coverage gap — 27 survivors remain total, all outside write_rule/g0_invariant) |
| TD-109 | SEC | BUILD REQUIREMENT CHG-8 - Biometric consent-and-retention control for on-device speaker recognition. Gates any public claim that speaker recognition has consent controls. Scope: (1) speaker recognition off by default; (2) per-speaker written or electronic consent at enrollment (or legally authorized representative); (3) consent screen must state: what is collected (local speaker embedding), purpose (speaker recognition, role permissions, household privacy controls), storage (local device only, encrypted at rest), sharing (not sold, not advertising, not model training, not uploaded unless separately authorized), retention (deleted on profile removal, consent withdrawal, purpose satisfied, or statutory maximum); (4) published biometric retention schedule: delete on request or profile deletion, delete after speaker-recognition disablement, delete no later than 3 years after last interaction where BIPA-style rules apply, device reset and household transfer deletion; (5) non-biometric fallback (PIN, passphrase, device possession, manual speaker selection); (6) audit enrollment, consent, deletion, and export events. | HIP_FinalizationOrder CHG-8/DECISION-3 | OPEN |
| TD-135 | OPS | **`whitepaper/archive/HIP_White_Paper_Augmented.docx` is corrupt and cannot be rendered.** Found building REQ_DOC_RENDERING (2026-07-27): the file is 150 bytes, a truncated zip (PK header present, no end-of-central-directory record — confirmed via `unzip -l`), unreadable by macOS `textutil` ("Error reading ... couldn't be opened", exits 0 anyway) and by extension unreadable by any docx tool. `git log --follow` shows it has been this way since its first commit (`4eee583`) — not a regression, a bad file from the start. `scripts/docx_to_text.sh` refuses to write a rendering for it rather than emit a misleading empty one; `docs/rendered/whitepaper/archive/HIP_White_Paper_Augmented.docx.md` does not exist and the AUDIT:DOC-RENDERING check reports it MISSING every run, by name, until this is closed. Not fixed here — recovering a truncated archive file isn't a rendering-tool problem, and the file is superseded archive content (`whitepaper/HIP_White_Paper_Augmented__v20260702_1113.docx` and later dated versions are the live lineage). Fix direction: replace with a good copy if one exists (Drive, another checkout), or delete it from `whitepaper/archive/` as dead weight and drop the corresponding gate exemption once it's gone. | REQ_DOC_RENDERING build, 2026-07-27 | OPEN |
| TD-136 | GATE | **[PORTED 2026-07-27 from `main` branch `TD-131`, commit `4390240`, per DISPATCH 36 register reconciliation phase 2 -- see `HIP_RegisterReconciliation__cross-branch-id-collisions__v20260727_1930.md`. Body verbatim from that point on; roadmap's own `TD-131` is a different, unrelated git-worktree-checkout finding and was not touched.]** Household facts reach the outbound Groq payload unfiltered on every MID and CORE turn. Mechanism: `harness/injection_contract.py`'s `_inj4_household` (lines 341-343) admits `owner=="household"` facts unconditionally, before INJ-5's intent gate. `server/voice_orch.py:2603` renders them into the system prompt; that `messages` object passes untouched to `_groq.chat.completions.create()` at line 3314. `strip_context_for_tier` at line 3240 is the only filter before a client call and is gated on `_frontier_strip`, which never fires for MID or CORE. Traced live on both tiers, 2026-07-22: five rows went verbatim -- address, zone_district, schedule, trash pickup, and Dad's risk_pattern (elevated fall-risk). Member-owned facts were correctly excluded in the same trace -- 8 candidates, 5 admitted, Maya's own appointment and medication and Ray's medication all denied. Not a defect in the sense of broken code -- the household exemption (INJ-4) is by explicit design, and member isolation holds. What is unresolved is whether that exemption should extend past the network boundary: today it does not distinguish "shared within this household" from "sent to an external provider's servers." Proposed resolution: extend `strip_context_for_tier` to MID and CORE, or accept the exposure and document it as an architecture decision. This is Bill's call, not a session's. Cross-reference: run-of-show correction at `9e61641`; backlog #48 (adjacent but distinct -- that item is about household state carrying across demo scripts within the local graph, not about the network boundary). No architecture-weakness register exists in this repo today to cross-reference; checked, not found. | Live trace, run-of-show correction session, 2026-07-22 | OPEN -- Bill's decision required |
| TD-137 | SEC | **RESOLVED 2026-07-31 (D-75), commit `d50225e` (the fix; this hash was stamped by the immediately following commit, because a commit cannot contain its own hash -- the first attempt stamped a pre-amend hash that the amend then orphaned). AUTHORITY: `REQ_STRUCTURAL_CEILING__dimensioned-collection-limit__v20260731_2129.md` (filed b0bc8e3) R29 'single source of truth' and R30 'migration and fail-closed behavior'. This was never an open question about which encoding to prefer — R29/R30 already ruled it, so it was an unimplemented requirement, and the original 'Bill's ruling required before fix' status was superseded by the REQ rather than by a separate ruling.** FIX: new `harness/sensitivity.py` is the one authoritative registry (LOW=10 / MEDIUM=20 / HIGH=30 / CRITICAL=40; order authoritative, numbers serialization aids, 10/20/30/40 spacing deliberate per R29). All four consumers now import it: `curator_shadow.py` (the shared `_ORDINAL` split so confidence keeps its own three-valued vocabulary and sensitivity uses the registry), `hipconfig.py` (local `SENSITIVITY_RANK` deleted), `extraction_queue.py` (`SENSITIVITY_LEVELS` is now an ALIAS object of `sensitivity.LEVELS`, not a second literal), `permissions.py` (`_HIGH_SENSITIVITY` DERIVED from the order, so a level inserted above HIGH extends it automatically instead of being silently omitted). FAIL-CLOSED, and three of these failed OPEN before: `rank()` has no default parameter by construction and RAISES `UnknownSensitivity`; the router threshold now forces local on an unknown tag (the old `.get(tag, 0)` / `.get(threshold, 99)` pair failed open in BOTH directions); `permissions` Rule 5 now DENIES on unknown (a bare `in` membership test let an unrecognized value skip the deny entirely). ALSO FIXED, an active R29/R30 violation not named in TD-137 and not caught by D-74's survey: `extraction_queue._coerce_fact` silently coerced any unrecognized sensitivity to `medium` at the write boundary — a downgrade of exactly the kind R29 forbids ('map unknown to low or medium', 'silently coerce a legacy string') and R30 item 3 requires be rejected. An unrecognized non-empty value now REFUSES the fact, matching the CANONICAL_ATTRIBUTES precedent one branch above. R30 MIGRATION: inventory ran BEFORE any change and every stored/emitted value maps cleanly — Neo4j `:Fact.sensitivity` across all 12 nodes incl. closed (low 5, high 5, medium 2, zero NULL), `logs/turns_demo.jsonl` (low/medium/high only), `config.yaml:90` force_local_at=high. No unmapped values anywhere, so no quarantine was needed and no mapping was chosen. `critical` is not stored ANYWHERE today, which bounds the historical exposure — the misranking never fired in production — but says nothing about the next fact tagged critical. EVIDENCE: new standing battery `eval/test_sensitivity_registry.py`, 31 cases covering A29 and A30, wired into `scripts/run_harness.sh` (batteries 44 -> 75 passing). `--layer 7` exit 0: all 5 ABSOLUTE PASS (G0, PSA1, CTX-STRIP, LI1, **CS1**), AUDIT 8/8, RATCHET PASS, COVERAGE-GRID-RATCHET PASS, zero FAIL lines (/tmp/hip_harness_d75_2259.log). CS1 is the one that mattered: the change alters the `sensitivity` FEATURE VALUE inside a MET component (low 0.0->0.25, high 1.0->0.75) whose L7:CS1 check is ABSOLUTE tier and asserts the exact ten feature keys — the key set is unchanged and cold-start ranking uses recency only, and CS1 passing is now evidence rather than the prediction it was at D-74. NOT CLAIMED: R29/R30 are NOT self-ruled MET — evidence reported, Bill rules. R30 item 5 (record the registry version on every policy decision and durable artifact) is NOT implemented; `SENSITIVITY_REGISTRY_VERSION` is exposed but stamped nowhere, and the call sites it would touch are reported separately because they are a schema change across three governed contracts (the D-1 epistemic record, every `:Fact` node in Neo4j, and the append-only ledger). One policy question flagged not silently decided: an ABSENT sensitivity still defaults to `medium` at write time; that is a default classification for a missing field rather than a coercion of an unknown value, and choosing fail-closed instead is Bill's call. | D-58 dispatch (filed), D-75 (resolved under R29/R30) | RESOLVED |
| TD-138 | SEC | **`is_active_caregiver` is EPOCH-BLIND: it authorizes against the CURRENT care-team roster, where REQ_CARE_TEAM_READ_AUTH requires authorization at the FACT'S ROSTER EPOCH. A caregiver enrolled today therefore gains read access to care-team-private facts that predate their enrollment — retroactive access the requirement does not authorize.** Bill's ruling (D-69, 2026-07-31): the REQ's epoch rule STANDS; retroactive read access on enrollment is NOT authorized; **the code is the defect, not the requirement.** EVIDENCE: (1) `harness/care_team_keys.py:141` — `def is_active_caregiver(recipient_ref, caregiver_member_id) -> bool` takes NO epoch parameter; (2) `:142` delegates to `list_caregivers(recipient_ref)`, whose query filters only `WHERE recipient_ref = ? AND removed_at IS NULL` (`:129-130`) — i.e. the roster as of NOW, with no as-of-epoch predicate; (3) the call site `harness/injection_contract.py:515-518` passes only `(recipient_ref, requester)`, so no epoch is available to the check even if the function accepted one; (4) the SCHEMA WAS BUILT FOR EPOCH-AWARE CHECKING and is going unused — `care_teams.epoch` and `care_team_members.epoch_added` both exist (verified against the live registry), which is why this reads as drift from the requirement rather than an unimplemented feature. REQUIREMENT TEXT the code diverges from: the REQ states the rule in Bill's own words at `REQ_CARE_TEAM_READ_AUTH__cross-author-care-team-auth__v20260721_1027.md:11` — "authorization keys on care-team ENROLLMENT (is_active_caregiver AT THE FACT'S ROSTER EPOCH), not authorship." CONSEQUENCE: a newly-enrolled caregiver reads care-team-private facts authored before they joined the team. SCOPE OF THIS RULING — RETROACTIVE HALF ONLY: the opposite direction (whether a REMOVED caregiver retains access to facts from while they were enrolled) is expressly NOT covered here and stays deferred to REQ_PARTITION_CUSTODY's backfill-grant policy, per the REQ's own text at `:49-56` and INJ-3's "no new access, not unremembers" limit. THE ACCEPTANCE CANNOT DETECT THIS AND ALSO NEEDS AMENDING: acceptance row 4 tests only the REMOVAL direction ("a caregiver removed from the care team ... cannot read via this new permit path"). Nothing in rows 1-5 exercises a caregiver enrolled AFTER a fact was written, so **a fully-run, fully-passing acceptance would have missed this defect entirely.** Row 1 must be amended to assert admission against the FACT'S ROSTER EPOCH rather than current membership; as written it asserts only that an enrolled caregiver can read, which the epoch-blind code satisfies. Amending the acceptance is a prerequisite to running it as a meaningful test of this requirement. NOT FIXED, deliberately: adding an epoch parameter changes an access-control predicate on a live read path, and the REQ it implements is itself NOT MET with a 5-row acceptance that no runner references (D-68). Fix, acceptance amendment, and REQ status should move together rather than a code patch landing against an unrun acceptance. CURRENTLY DORMANT, which bounds the exposure but does not close it: `care_teams` and `care_team_members` are both EMPTY in the live registry (D-68, verified on a copy), so `is_active_caregiver` returns False for every input and the INJ-3 care-team permit cannot fire in this deployment today. The defect arms the moment the first caregiver is enrolled. | D-68 acceptance attempt + D-69 ruling, 2026-07-31 | OPEN -- code fix, acceptance amendment, and REQ status to move together |
| TD-139 | ENG | **PARTIALLY CLOSED (D-105, 2026-08-02): 4 of R18's 11 fields IMPLEMENTED, 4 were ALREADY PRESENT under other names, 3 STAY OPEN because they cannot be populated honestly.** **IMPLEMENTED at create_fact_node, scoped to DERIVED artifacts (R18's own scope) and FAIL-CLOSED — a derived write missing one is REFUSED, the creator will not guess:** `artifact_type` (creator-stamped 'fact'; a caller-supplied value is overwritten, since provenance a caller can forge is not provenance), `audience_policy` (write_class.visibility — genuinely NEW: dyad_id and recipient_ref were stored, the CLASS was not), `derivation_method` ('consolidate.abstract.v1', versioned so a second derivation path cannot arrive unlabelled), `source_categories` (the PARENTS' attributes, read from the parent rows and plumbed down from _abstract_pass, which has the episodes in scope — DerivedFact carries parent IDs but not their attributes, so this needed one plumbing hop). **ALREADY PRESENT UNDER ANOTHER NAME — NOT duplicated, because a second copy of a stored fact is DRIFT, not lineage (TD-137's lesson applied to provenance):** artifact_id == `fact_id`; parent_artifact_ids == `derived_from`; sensitivity == `sensitivity`; subjects ~ `subject` (consolidate groups episodes BY subject, so a derived fact has exactly one and a plural copy would add nothing). **Note this corrects TD-139's own original count of '2 of 11'** — three of the eleven were already stored, counting by R18's field list rather than by `derived`+`derived_from`. **STILL OPEN — 3 fields, and what each would need:** `purpose_id` — **no purpose vocabulary exists anywhere in the codebase** (verified ABSENT across harness/ and memory_engine/); needs R23's purpose-trigger vocabulary to be built first. `retention_deadline` — **no retention mechanism exists**; R21 (calculable expiry) is NOT MET, and this field cannot precede it. `policy_version` — **none exists**; `sensitivity_registry_version` is the SENSITIVITY vocabulary's version and **relabelling it as a policy version would be a lie in a provenance field**, which is precisely what R18's block must never contain. All three are asserted ABSENT by a standing test, so a future placeholder cannot be added quietly. **NOT BACKFILLED, and this is asserted rather than assumed:** the 12 pre-existing facts do not carry the block; no backfill was ordered, and unlike R30 item 5's `pre-registry` marker there is no ruling about what a pre-lineage artifact should say. The one live derived fact is a further case — it was created via `encode` and had `derived = true` SET afterwards by demo_seed, so it never passed through the creator as a derived artifact; that is **TD-141's** shape, not a failure of this gate. ACCEPTANCE: eval/test_lineage_block.py, 16 cases, in run_harness.sh — every implemented field present on a real derived write (through consolidate's own writer, not a hand-built dict), a parametrized fault twin per required field proving refusal with NO write issued, blank-string and empty-list treated as missing, non-derived writes proven unaffected, the deliberate absences asserted, and no-duplicate asserted both directions. **R18 REMAINS NOT MET — TD-140 (the recompute branch) is still open and awaiting Bill's ruling, and TD-141 is untouched. This dispatch changed no cascade behaviour.** ORIGINAL FILING BELOW. **R18 IS PARTIALLY IMPLEMENTED: the CASCADE landed, the LINEAGE METADATA BLOCK did not.** R18 requires every derived artifact to carry `artifact_id, artifact_type, parent_artifact_ids, subjects, source_categories, purpose_id, audience_policy, sensitivity, retention_deadline, policy_version, derivation_method`. What exists on a `:Fact` today is `derived` (bool) and `derived_from` (list of fact_id) and nothing else — 2 of 11 fields, and `parent_artifact_ids` only by rename. D-81 built the cascade on top of `derived_from` because that is the field that exists; it did NOT invent the other nine. CONSEQUENCE: the cascade can answer 'which children descend from this parent' but cannot answer any of the questions the rest of the block exists for — it cannot erase 'according to its storage class' (no storage class recorded), cannot honour a retention deadline, and cannot distinguish derivation methods when deciding recompute eligibility. So the third and fourth lines of R18's else-branch ('remove it from retrieval, disclosure, and action' / 'erase it according to its storage class') are satisfied only in the weak sense that closing a fact removes it from retrieval — nothing erases by storage class because no storage class is written. This is why R18 must NOT be ruled MET on the strength of D-81 alone. *(Historical note, D-113: R18 was subsequently ruled MET on far more than D-81 alone — the D-111-amended operative rule, D-105's gate, D-107's live cascade proof — with the three absent fields recorded in the ruling itself.)* | D-81, 2026-08-01; status column per Bill's wording, D-113, 2026-08-02 | PARTIALLY CLOSED (D-105). Eight of eleven fields implemented. Three — purpose_id, retention_deadline, policy_version — absent, blocked on R23's purpose vocabulary, R21's retention mechanism, and a real policy version. Absence asserted by a standing test. The pre-lineage backfill question for durable graphs is unruled. |
| TD-140 | ENG | **RESOLVED (D-111, Bill's ruling 2026-08-02): CLOSED BY REQUIREMENT CHANGE, NOT BY A BUILD — the distinction is the point of this entry.** No recompute was built, and none is now required: Bill took the FIRST of D-104's three shapes and **amended R18's text to remove the recompute clause entirely** (REQ_STRUCTURAL_CEILING v20260802_2112; the four reasons are recorded IN the requirement, per the ruling: no stored recipe exists to re-run — D-104 proved recompute is a second non-deterministic Groq call; a rule describing an operation the system cannot perform reads as a commitment and is not one; invalidate-only errs toward forgetting MORE, never less — the failure mode is lost inference, not retained inference; and recompute-as-fresh-inference is NOT foreclosed — if ever wanted it returns as its own requirement through the inference-permit machinery with the non-determinism handled openly). Invalidate-only — what D-81 built and what this entry recorded as UNDER-DELIVERING — is now the delivered requirement. `cascade_recompute_eligible` / `cascade_recompute_from` remain written-and-unread as breadcrumbs for that possibly-future REQ, per the amendment's door-open clause; `harness/derivation_cascade.py`'s docstring updated to quote the amended rule (comment-only, no behaviour change). ORIGINAL FILING BELOW. **Recompute is DEFERRED, not performed: nothing yet consumes `cascade_recompute_eligible`.** R18's first branch says 'if child can be recomputed solely from still-authorized parents: recompute child and replace lineage'. D-81 deliberately does NOT re-derive inside the retraction path, because re-deriving a consolidated fact needs an LLM abstraction call (`memory_engine/consolidate.py`) and a timeout or refusal mid-retraction would leave the child ACTIVE — the exact state R18 forbids. So the child is closed unconditionally and marked `cascade_recompute_eligible` / `cascade_recompute_from` for a later pass. THE LATER PASS DOES NOT EXIST. Net effect today: every cascaded child is invalidated and none is ever recomputed, which is STRICTER than R18 (no child wrongly survives) but UNDER-DELIVERS it (a recomputable child stays dead). Recorded as debt rather than silently accepted, because 'the cascade is built' would otherwise be read as 'R18's recompute branch works'. It does not. Eligibility threshold is `MIN_PARENTS_FOR_RECOMPUTE = 2`, matching consolidation's >=2-source contract. | D-81, 2026-08-01 | OPEN -- needs a consolidation-side re-derivation pass  **ADDENDUM (D-104, 2026-08-02, survey only, not fixed): recompute confirmed to require a fresh model call, not a mechanical re-derivation.** `memory_engine/consolidate.py::_abstract_pass` calls `Interpreter.abstract()`; the real production interpreter (`GroqInterpreter`) makes a live Groq call and may return zero facts or a different fact than the original derivation. This is a different requirement than R18's text reads on its face (a mechanical recompute-and-replace) -- building it means accepting non-determinism, not just wiring the eligibility flag that already exists. STOPPED per dispatch instruction; needs Bill's ruling on which of three shapes to take (amend R18's text, build it as a non-deterministic second attempt, or leave TD-140 open indefinitely on the grounds that invalidate-only already errs safe) before any code is written. Full survey: docs/dispatches/DISPATCH_TD140_RECOMPUTE__stopped-recompute-requires-a-model-call__v20260802_0639.md. **STATUS SUPERSEDED by the D-111 resolution above: Bill ruled among the three shapes — amend R18's text.** |
| TD-141 | ENG | **RESOLVED (D-107, 2026-08-02): the writer was TRACED (demo_seed, confirmed, not just suspected), and the seed now writes its derived fact THROUGH the creator with real lineage — no backfill was needed or performed, because the fixture graph is rebuilt from seed on every reset and the seed is the sole author of the defective row.** THE TRACE: `scripts/demo_seed.py` (encode-then-SET at the old lines 428-433) wrote D8 via `encode()` — which hardcoded `derived=False` — then flipped the flag with a raw Cypher `SET f.derived = true, f.derived_from = []`. Introduced at `efde99b` (2026-07-05) purely so `trust()` would read DERIVED, using the same post-write-enrichment pattern as CONFIRMED/CORROBORATED; it predates the origin vocabulary (D-97), the single creator (D-96), the cascade (D-81), and the lineage gate (D-105) entirely. **ONE instance, a seed script, not a pattern** — the only other `derived` SET in the tree is store.py's Phase-A migration writing `derived=false` (the non-derived default, not a forgery). THE FIX: `encode()` gains an explicit `DerivedLineage` channel (memory_engine/store.py) that carries derived=True plus derived_from/derivation_method/source_categories into the CREATE itself, with audience_policy stamped from the computed write class (same source as consolidate's writer) — so the D-105 gate at create_fact_node governs the seed's derived write exactly as it governs consolidation's, and refuses it if any field is missing. demo_seed declares D8's parents by label (`DERIVED_PARENTS = {"D8": ("D4","D5")}`), resolved to the real just-seeded fact_ids; the lineage is honest — D4 (fall incident) and D5 (medication discontinuation) are the only other dad-subject facts, both precede D8 in seed order and fixture time, and 2 parents matches consolidation's >=2-source contract. `derivation_method` is `demo_seed.fixture.v1`, NEVER consolidate's string — the fact stays SEEDED, not inferred (X-04's caution holds, now on the node itself). A DERIVED fixture that cannot name seeded parents is REFUSED before any write. **LIVE-PROVEN, read from the graph:** after reset+seed, D8 carries the full implemented block with derived_from naming D4/D5's real fact_ids; retracting D4 (`retract_fact('sam','incident',subject='dad')`) closed D8 with `closed_by='lineage_cascade'`, `cascade_recompute_eligible=false` (1 surviving parent < 2), `cascade_recompute_from=[D5]` — **the R18 cascade acted on live data for the first time**; fixture then reset+reseeded to canonical. **THE LIMIT, STATED PLAINLY:** a post-write `SET f.derived = true` happens below any creator and CANNOT be refused — Neo4j property writes have no gate. It is DETECTED instead, two ways, both standing: eval/test_lineage_block.py's source scan (no script may SET a derived flag; red if TD-141's shape is reintroduced) and eval/harnesslib/fixture.py verify_seed's graph query (any derived=true row missing the lineage block fails every standard fixture reset; zero rows confirmed live). ACCEPTANCE: 5 new cases in eval/test_lineage_block.py (`test_lineage_seed_*`, D-87 complement: flow-through-creator, old-shape-refused-at-create fault twin, honest-parents structural, dishonest-declaration fault twins, source-scan) — 21 cases total in the battery, all green. **R18 REMAINS NOT MET — TD-140 (recompute) is still open and awaiting Bill's ruling; nothing here is ruled MET.** ORIGINAL FILING BELOW. **The one derived fact in the live graph has an EMPTY `derived_from`, so R18's lineage requirement is violated by existing data, not just by absent code.** D-81's pre-change inventory against live Neo4j (read-only): 12 `:Fact` nodes total, 1 with `derived = true`, and **0 with `derived_from` populated**. No derived child had a retracted parent, so the pre-R18 defect had never actually fired in the demo graph — but the reason is that no lineage was ever written, which is its own failure of R18's opening sentence ('every derived artifact SHALL name every parent artifact that materially influenced its existence or content'). CONSEQUENCE FOR THE CASCADE: it is correct and currently inert against live data — a cascade keyed on `derived_from` cannot reach a child whose `derived_from` is empty. Any assessment of R18 must treat 'the cascade passes its battery' and 'the graph is protected' as separate claims. The writer at `memory_engine/consolidate.py:525` does set `derived_from`, so the empty value is most likely from the demo seed path rather than from consolidation; which writer produced it was NOT traced by D-81 and should be, before backfill is attempted. | D-81, 2026-08-01; resolved D-107, 2026-08-02 | RESOLVED -- writer traced, seed writes through the creator with real lineage; no backfill needed (fixture graph rebuilds from seed) |
| TD-142 | SEC | **The mutation-testing harness addresses one seeded mutant by hardcoded absolute file:line inside harness/injection_contract.py, so ANY edit that inserts or removes lines above that point silently desyncs it into a false red indistinguishable, on its face, from a real governance regression in INJ-7 (the cross-member access-denial boundary this seeded mutant exists to prove is caught).** Filed SEC rather than a generic quality note because the brittleness sits directly in the safety net for a privacy/governance-critical check, not in an arbitrary test: a false red here either gets dismissed as "probably just another line-shift" (masking a real future regression) or costs a full re-trace every time (as D-101/D-102 just did) -- both outcomes degrade trust in the one check that proves INJ-7 can't silently regress. TWO COORDINATES MOVED, D-101 -> D-102: `eval/harnesslib/mutation_targets.py::_INJ7_LINENO_RANGE`, a hardcoded `(start, end)` tuple scoping the mutant sweep to just the INJ-7 if-block (so the ~250-line apply_injection_contract() function's many other conditions don't dilute the count) -- was `(611, 629)`, is now `(656, 674)` (D-101's R12 docstring + new aggregation-cap logic inserted 45 lines above it); and `fault_twin_self_test()`'s `_is_seeded_mutant` closure, which matches a survivor by `survivor.lineno == 619` (now `664`) to identify the ONE specific mutant the self-test cares about among however many the sweep generates. Both resynced by D-102 -- this entry is the durable-fix scope, not the fix itself, per instruction. WHAT A DURABLE ADDRESSING SCHEME WOULD NEED, three options named not chosen: (1) AN ANCHOR COMMENT -- a unique, greppable marker string placed immediately above the target `if` statement (e.g. `# MUTANT-TARGET: inj7-refusal`), located by text search rather than line number, cheap and readable but still breaks if the comment itself is ever moved or duplicated; (2) AN AST-LOCATED PREDICATE -- describe the target structurally (e.g. "the first `if` statement inside apply_injection_contract whose test references `member_ids`") and locate it by walking the current AST at sweep time, immune to line shift and even to reordering, but more code to get right and to trust; (3) A CONTENT HASH -- hash the target statement's own source text (via `ast.unparse` or similar) and re-locate by matching the hash, catching drift explicitly (hash mismatch -> loud failure naming exactly what changed) rather than silently addressing the wrong line, but requires the statement's literal text to stay byte-stable, which a future reformatting-only edit would break for no functional reason. All three are more robust than the current hardcoded pair; none was implemented here -- scoping only, per the dispatch's explicit instruction not to fix it in D-102. | D-102, 2026-08-02 | OPEN -- scoped, not fixed |
| TD-143 | SEC | **L4:PW010 — the empty-set gate does not fire on a cross-member NOT-EXISTS query; the refusal is MODEL-PRODUCED.** `sam→ray/allergy [not-exists|direct]`, "What is Ray's allergies?" The record shows `guard_triggered=false`, `guard.kind=None`, **`inference_ms≈450ms`** — the model ran and produced "I don't have that confirmed yet." **SURFACED BY REQ_RECORD_GRADED_REFUSAL (Voice 37 build):** under the previous text-graded assertion this row was GREEN, because `classify_refusal` saw "don't have" and returned `empty_set`; record grading shows no gate fired. Recorded as a KNOWN RED in `eval/harness_baseline.json` via the sanctioned `--update-baseline --accept` path (D-17), NOT silently. **The harness change is correct and is NOT the defect** — it is the instrument that found this. **Product defect: NOT FIXED, deliberately** (Bill's ruling 2026-08-02: accept the findings, do not fix the product in the harness dispatch). Fixing it means making the gate fire on this path, which is a product change outside REQ_RECORD_GRADED_REFUSAL's scope. Lower severity than TD-144 because the fact does NOT exist — nothing was disclosed — but the GUARANTEE is the same shape: the refusal rests on the model's cooperation rather than on a structural gate, so it is a correctness/assurance gap rather than a live disclosure. **THE DISCRIMINATOR, verified not assumed: SUBJECT IS A NON-MEMBER.** Registered members are bill/maya/sam; `ray` and `dad` are not. Cross-member rows whose subject IS a member (PW000/PW004/PW005 → maya) expect `access_control`, INJ-7 fires, and they PASS. The two failing rows have NON-MEMBER subjects and expect `empty_set` — INJ-6/INJ-6b territory, a different guard. **So the gap is in the EMPTY-SET guard for non-member subjects, not in the access-control guard.** **ADDENDUM (D-128, 2026-08-03): PROPOSED RESOLVED — closed by D-127's fix.** PW010 now refuses STRUCTURALLY (guard.kind=empty_set, inference_ms=None) in three consecutive runs (pairwise twice + the full ratchet); the mechanism was resolution blindness (D-126's trace), fixed by graph-wide Phase-3 resolution + admitted-set keying under REQ_STRUCTURAL_REFUSAL (RULED MET, D-128). Baseline row flipped true via the sanctioned path. Proposal only — Bill rules the TD. **RULED (Bill, 2026-08-03, D-129): RESOLVED — closed by D-127's fix, three consecutive structural runs.** | Voice 37 build, 2026-08-02; D-127 fix; D-129 ruling | RESOLVED |
| TD-144 | SEC | **L4:PW015 — A CROSS-MEMBER FACT THAT EXISTS IS PROTECTED ONLY BY THE MODEL'S COOPERATION. No gate fires.** `sam→dad/medication [exists-cross-member|plural]`, "What medications does Dad take?" The record shows `guard_triggered=false`, `guard.kind=None`, **`inference_ms≈453ms`** — the model ran, saw the turn, and chose to answer "I don't have that confirmed yet." **THE FACT EXISTS AND BELONGS TO ANOTHER MEMBER.** This is the more serious of the pair and Bill directed it be filed on its own: sam is currently kept from dad's medication by the model declining to say it, not by the injection contract structurally withholding it. **That is the exact inverse of the product claim** — 'the model cannot decline to reveal what it never saw' — and for this row the claim is not true today. **SURFACED BY REQ_RECORD_GRADED_REFUSAL (Voice 37 build)**, which is precisely what that REQ was built to detect; under text grading the row was GREEN. Recorded as a KNOWN RED via the sanctioned `--update-baseline --accept` path (D-17), visible on the board rather than buried. **NOT FIXED — Bill's ruling 2026-08-02: file it, do not fix it. NEEDS ITS OWN REQ.** The fix is a product change (make the gate fire on this path), not a harness change, and it should not be smuggled into a dispatch about how refusals are graded. **Scope note for whoever writes that REQ — the discriminator was VERIFIED, and it is not what it first looked like.** It is NOT phrasing. It is **member vs non-member subject.** Registered members are bill/maya/sam. Rows whose subject IS a member (PW000/PW004/PW005 → maya) expect `access_control`, INJ-7 fires, and they PASS — the access-control guard is healthy. The two failing rows have NON-MEMBER subjects (`ray`, `dad`) and expect `empty_set`, which is INJ-6/INJ-6b territory. **So the hole is in the empty-set guard for non-member subjects.** Note the interaction with TD-120 D2 (relation→name mapping not built): 'Dad' never resolves to a graph subject, so nothing routes the turn to a structural guard and it falls through to the model. **The first job of that REQ is to establish how wide this is** — every non-member-subject row, or only these two — rather than assuming either. **ADDENDUM (D-128, 2026-08-03): PROPOSED RESOLVED-BY-CORRECTION-AND-REQ.** The headline was corrected on the record (D-125's REQ evidence + D-126's trace: no dad-medication fact exists; sam owned the admitted content; nothing leaked); the row's empty_set expectation was itself the defect and was corrected at D-127 per Bill's ruling (d) (now no_leak, green); 'needs its own REQ' is satisfied — the CLASS is governed by REQ_STRUCTURAL_REFUSAL (RULED MET, D-128), with the untargeted-attribute residue in TD-149 and the household-exemption question in TD-136. Proposal only — Bill rules the TD. **RULED (Bill, 2026-08-03, D-129): RESOLVED-BY-CORRECTION-AND-REQ, as proposed — with the ruling's own record that the ORIGINAL HEADLINE WAS WRONG and the correction, not a fix, is half of why this closes. Residues stay in TD-149 and TD-136.** | Voice 37 build, 2026-08-02; D-125/D-126 corrections; D-127 ruling d; D-129 ruling | RESOLVED-BY-CORRECTION-AND-REQ |
| TD-145 | OPS | **MEM-116's verification can red for a structural reason UNRELATED to its own subject: it decrypts through the RETIRED v1 master-key path on a checkout where that key's absence is the asserted-CORRECT state.** Same class as TD-142 — a harness check whose false red is indistinguishable, on its face, from a real regression until traced. THE EXACT STALE CALL, one line: `eval/memory_harness.py:1664` `decrypt_fact_value(new_row["ct"], new_row["dek"], "maya")`, imported from `harness.encryption` at `:1597` — **the ONLY use of the v1 helper anywhere in the memory harness** (both hits inside `_run_mem_116`). WHY IT REDS: the new (maya→ray, medication) head that MEM-116's T2 supersede produces is written by the LIVE path, which since REQ_CRYPTO_P2 seals v2-by-class (dyad-private, key_version=2) — but `decrypt_fact_value` reaches for the master key at `HIP_MASTER_KEY` → `~/hip-roadmap/data/encryption/.master_key`, which REQ_CRYPTO_P3 part (c) deliberately decoupled/destroyed on roadmap and **L7's OB6 (ABSOLUTE, passing) asserts as the CORRECT destroyed state** ("no master key file exists on roadmap... never silently regenerated"). So when the Groq-dependent detection upstream SUCCEEDS, the scenario proceeds to `:1664` and dies with `MasterKeyMissingError` — observed live in D-107's run 2 (D-108's trace), versus run 1's different, genuinely-environmental mode (detection never landed, assert at `:1644` fired). **A scenario with two unrelated red modes cannot be read from its verdict alone.** ITS STRUCTURAL ASSERTIONS ARE FINE AND ON-SUBJECT (baseline closed with `valid_to`+`superseded_by`, a new active head exists — `:1637-1660`); only the value check is stale. WHAT THE VERIFICATION WOULD HAVE TO CHECK INSTEAD, scoped not fixed, two shapes: **(a) read through the class-sealed path** — fetch `key_version`/`dyad_id`/`recipient_ref` alongside ct/dek in the Cypher at `:1655-1659` and call `harness.partition_crypto.decrypt_fact_value_for_caller(..., owner='maya', caller_member_id='maya')`, the same caller-scoped self-read `eval/harnesslib/fixture.py::decrypt_rows` already uses — keeps the 'jardiance' content assertion; or **(b) drop the decrypt entirely** and assert the structural outcome only (new head's fact_id ≠ baseline's, correct session/write_state) — MEM-116's subject is supersession-under-withholding (the Phase 1 detect_and_apply fix), not crypto, and content-reading belongs to scenarios that own the crypto contract. Either way the fix is a HARNESS change and must not be smuggled into unrelated work; whichever shape is chosen should also re-check MEM-115/117/118 for the same v1-helper class (grep says they are clean today — `:1597`/`:1664` are the only hits — assert it then, not now). NOT FIXED HERE per D-109's instruction: scope only. | D-108 trace, filed D-109, 2026-08-02 | OPEN — scoped, not fixed |
| TD-146 | OPS | **MEM-115(b)'s red is STRUCTURAL AND PERMANENT under the current architecture, not environmental: it asserts a contract-layer denial (`denied_count > 0`) for a cross-member recall of a MEMBER-PRIVATE fact — the one fact class the injection contract can never see post-REQ_CRYPTO_P2, because the caller-scoped decrypt drops the row BEFORE the contract runs.** The scenario is stale against the partition-sealed work the same way MEM-116 (TD-145) is stale against the operator-blind work; with TD-145 this completes the pattern: BOTH persistent memory-harness reds are crypto-era staleness, not product regressions. THE TRACE, each step evidenced: **(1) What fails:** `eval/memory_harness.py:1551` — `denied_count > 0` — is the ONLY failing assertion; the disclosure assertions themselves PASS (`:1542` cross-owner fact absent from results; `:1555` allowed_fact_ids empty). Nothing is disclosed. **(2) Why denied_count is 0 — neither 'nothing fetched from the graph' nor 'count not recorded':** the cold Cypher (`memory_engine/recall.py:67-89`, subject-scoped, no requester filter) DID return both cold rows — proven by D-107 run 2's captured log, which shows exactly two `recall_from_cold: decrypt failed for <fid> (FileNotFoundError(2,...)) — skipping` warnings and ZERO 'Neo4j query failed' lines. Both rows were dropped at Step 2's CALLER-SCOPED decrypt (`recall.py:228-236`, `caller_member_id=requester` per REQ_CRYPTO_P2 — the comment at `:226-227` names the design), so `facts` was empty, Step 3's contract call (`:260-267`) never executed, and `denied_count=0` was recorded FAITHFULLY (`:270-272`). The count is honest; there is no contract-layer denial to count. **(3) Why the requester cannot decrypt, structural not incidental:** member-private DEKs seal to the AUTHOR's keypair, auto-provisioned at write (`harness/partition_crypto.py:102-112`); requester `memtest-115b-*` never wrote anything → no key file at all → `FileNotFoundError`; and even an enrolled requester's key could not unwrap another member's member-private DEK (that impossibility is the design — L7's N1/cross-member checks assert 'cross-member decrypt count == 0' as PASSING product behavior). **(4) Verdict: HARNESS ARTIFACT, with ONE small real gap attached.** Not a governance/disclosure gap: the fact is not disclosed, and the denial is structural crypto — STRONGER than the contract denial the scenario was written to observe (the scenario predates P2, when a fetched fact decrypted for any caller via the master key and INJ-3 was the denying layer). The contract still runs on this path whenever a requester CAN decrypt a fetched fact (household-shared to a circle member; dyad-private to a co-custodian) — §6 invariant 1 holds. **The one REAL gap is AUDIT FIDELITY, named per D-110's instruction:** a cross-member recall attempt against unreadable facts writes `cold_fact_ids_fetched=[], allowed_fact_ids=[], denied_count=0` — byte-identical to 'no cold facts exist'. The decrypt-skip at `:233-236` is a log.warning only; the audit record (`:94-121`) has NO field for it. 'Attempted and structurally unreadable' vs 'nothing there' is a distinction the who/when/why audit cannot make, on the one path (`recall_from_cold`, deliberately pipeline-uncalled per its own docstring `:181-183`) built for deliberate member-initiated recall. WHAT THE SCENARIO WOULD HAVE TO CHECK INSTEAD, scoped not fixed, two shapes: **(a)** exercise the contract with a fact class the requester CAN decrypt — enroll both memtest identities in a household circle and seed a household-shared cold fact (or a dyad with both as custodians) — then INJ-3/INJ-7 fires at the contract layer and `denied_count > 0` becomes assertable for real; **(b)** for the member-private case, assert the NEW structural shape — which first requires the audit to record it: add an `undecryptable_count` (or skipped-fact-ids list) to `_append_recall_audit` and assert THAT > 0; that is an audit schema change and a harness change together, deliberately not done here. Whichever shape lands, the re-pinned baseline note (D-109) should be updated the day MEM-115 goes green so the 13-15/17 range does not quietly absorb it. NOT FIXED per D-110's instruction: scope only. | D-107 runs + D-108 trace, filed D-110, 2026-08-02 | OPEN — scoped, not fixed |
| TD-147 | OPS | [harness-reliability] L6:record-invariants RED on both D-117 --full runs (logs 20260803_0743 + _0814): ONE G1 no-orphan-generation violation, the IDENTICAL record both runs — [sam] "I take atorvastatin 20mg every morning." (harness_run.jsonl smoke turn). NOT caused by the D-117 header rename, proven structurally: harness/fact_change.py has zero section-header references, and D-117 captured before/after prompts byte-identical. Mechanism, evidenced: fact_change extraction returned zero changes, the retry hit a Groq-side ReadTimeout (harness_server.log:77-78), the write dropped, F3 honestly refused the ack ("unable to save it to the household record"), and G1 counted the orphan by its own documented design (declarative with extraction failure deliberately NOT exempt). The same utterance extracted fine in turns_demo.jsonl in the SAME runs — per-call payload-biased service nondeterminism, the DISPATCH_GROQ_400_ROOTCAUSE lineage (July: care_coordination's Elena payload; now this smoke payload). BASELINE FACT: D-114's "RATCHET PASS" was --layer-7 scope (its log has no L1-L6 blocks); the last true --full including L6 was 2026-07-30, GREEN — this red is new against a five-day-old baseline, not against yesterday. BASELINE STAYS UNUPDATED; THE RED STAYS LOUD (Bill, D-118). MEM-117's new red (memory harness 14/17, failures {115,116,117}, inside the D-109 pin) is the SAME fact_change live-write family — noted here per the same ruling, not a separate TD. Handoff (from the D-117 dispatch doc's OPEN): diff this smoke payload's fact_change request against a same-run succeeding one at the call site. **ADDENDUM (D-119, 2026-08-03, DISPATCH_TD147_HANDOFF): the mechanism above is CORRECTED — read the instruments, not the filing. (1) TRANSPORT EXONERATED: write_latency.jsonl shows both runs' failing turns as detect_no_changes / groq_status=ok / attempts=1; the cited ReadTimeout was attempt-0 of the semantic-retry call and recovered inside _call_groq; the whole file holds exactly 3 hard call-failures ever, all owner=bill HTTPError, none these turns. The write dropped because gpt-oss-20b returned changes:[] — DETERMINISTICALLY: the smoke sequence asserts this utterance 5x per run (maya, sam x3, bill), and outcomes are position-identical across both runs (sam #2 — the instance whose facts_block contains the fact sam #1 just wrote — fails 2/2, first call AND temp-0.2 retry; the other four succeed 2/2). The payload class is RESTATEMENT-OF-AN-ALREADY-RECORDED-FACT, the P2/i019 changes:[] family. (2) The 'extracted fine in turns_demo' claim is WITHDRAWN — turns_demo.jsonl contains zero atorvastatin turns; its G1 PASS is vacuous for this shape; the real same-run successes are the four sibling harness_run instances, each with a materially different facts_block (fact absent or context grown), so different requests — not luck. (3) The F3 design already handles restatements (voice_orch.py:2314: mutations==0 with noops>0 is NOT gated) — the defect is the model omitting the restatement so the no-op path starves, and F3's failure claim is FALSE for a fact that exists. Levers scoped in the dispatch doc: extraction-prompt re-assert semantics, and/or a model-free structural restatement check before UNCONFIRMED_UPDATE_REPLY fires. NOT an availability problem; retry policy already saturated for this class. Baseline still unupdated, red still loud.** | D-117 --full runs 2026-08-03; DISPATCH_HEADER_RENAME OPEN; corrected by D-119 | OPEN |
| TD-148 | OPS | [lock-protocol] .hip-lock is ADVISORY WITH NO ENFORCEMENT — two failures in one effort, same class: (1) D-107 (2026-08-02) WRITE-THROUGH — the demo-cutover lane wrote REQ + dispatch + INDEX rows into ~/hip-roadmap while the lock was held by the roadmap lane; (2) D-118 (2026-08-03, 08:56:33) UNREAD CLOBBER — a session took the lock with a bare > after an existence check and overwrote an existing lock, destroying its holder fields; holder never identified (corroborating trace: harness_run.jsonl archived at 08:57:34 by an unidentified process, harness_run.20260803T145734Z_872ad0c.jsonl — the clobbered holder was plausibly starting a harness run). FOLDED IN, no TD existed (checked): the thrice-flagged 'taken:'-timestamp drift — HIP_CHAT_HANDOFF.md:15 (drifts hours from mtime; "likely why concurrent sessions both think they hold the lock"; cheap fix pending since filed), its pending list (:119), and the 2026-08-01 live observation (D-91 lock). FIX SCOPE, named not built: read-before-write (read and REPORT holder fields; defer if held); ATOMIC noclobber creation (O_EXCL / set -o noclobber / mkdir-as-lock); a liveness field that cannot drift (holder PID + filesystem-derived heartbeat mtime refreshed during long dispatches, 'taken:' cross-checked against mtime at read); a dead-holder policy (liveness probe + stale heartbeat threshold -> takeover ONLY with a recorded supersession note preserving the dead lock's fields); same discipline for docs/.INDEX_MANIFEST_LOCK and docs/.GRAPH_HARNESS_LOCK. UNGOVERNED — needs a REQ before any build. | D-107 write-through; D-118 clobber; D-119 filing | OPEN |
| TD-149 | ENG | [guard-coverage] INJ-6b detects the asked attribute by KEYWORD REGEX over _TARGETED_ATTRS (12 "precise" attributes; appointment/preference/schedule deliberately excluded — harness/injection_contract.py:272 records the over-fire rationale: loose keywords fire on queries that are not requests for that attribute). Consequence: the D-127 structural-refusal keying covers only targeted attributes — a resolved-subject query about an untargeted attribute (PW014 appointment, PW017 preference) stays on the model path with no structural refusal available. THE WIDENING PATH (D-126 proposal keying 3, ruled OUT of D-127 scope by Bill ruling e): derive `asked` from the router's SIO attribute classification (classify_sio already computes sio.attribute upstream) instead of keyword regexes — semantic classification does not over-fire the way loose keywords do, so the exclusion list could shrink or empty. Needs: SIO-attribute reliability check against the L4 phrasings, a fallback story for sio_source=fallback turns (regex remains), and family-mapping parity with _attribute_family. UNGOVERNED — needs a REQ before any build (the change moves refusal decisions for every untargeted attribute). | D-126 trace; D-127 ruling (e), 2026-08-03 | OPEN |
| TD-150 | OPS | [record-integrity] eval/harness.py --update-baseline SILENTLY REWRITES the _accepted map — a tool that rewrites accepted-red justifications is a record-integrity hazard, not a convenience bug (Bill, D-128). TWO incidents in TWO days, both caught only in manual diff review, neither announced by the tool: (1) D-126: --update-baseline --accept "<text>" applied the ONE accept text to EVERY currently-red row, overwriting TD-143's, TD-144's, and L1:P2's recorded justifications with an unrelated sentence (restored byte-identical from HEAD before commit); (2) D-127: plain --update-baseline (no --accept) DROPPED the entire _accepted map including L1:P2's still-live justification for a still-red row (restored by hand, L1:P2 only). The justifications are governance records — they carry defect IDs and Bill's rulings — and the tool treats them as derived state. FIX SCOPE, named not built: (a) per-row accept targeting (--accept-row ID "text"; a global --accept REFUSES when more than one row is newly red); (b) preserve-by-default — an _accepted entry is never modified or dropped unless its row flipped to passing in THIS run, and dropping one prints the retired text; (c) refuse to drop a justification whose row is still red in the baseline (the L1:P2 shape — the row did not even run in the D-127 layer-4 invocation); (d) print an _accepted diff at write time so review does not depend on git; (e) a battery case asserting round-trip stability (update with no changes leaves _accepted byte-identical). UNGOVERNED per the register's key — needs a REQ or a DEFECT ruling from Bill before a build (blast radius is the harness's own record of what is allowed to be red). | D-126 + D-127 incidents; D-128 filing | OPEN |
| TD-151 | OPS | **MEM-116/117/118 (memory-harness Phase E) mutate the REAL D9 demo fixture (owner=maya, subject=ray, attribute=medication, 'metformin 500mg twice daily') via the LIVE pipeline (`scripts.text_demo.run_query(..., "maya")`, Ray: metformin→Jardiance) and never restore it — their `finally` cleanup (`_delete_facts_by_session`) deletes only facts tagged with their own randomized test `sid`, but the live write lands under a FIXED session id the pipeline derives internally ('text-maya'), which none of the three ever targeted.** Compounding: `memory_engine/store.py::_tx_supersede`'s no-target branch (`target_fact_id=None`) closes ALL active rows matching (owner,subject,attribute) in one query but creates only one replacement, so each harness run further erodes the shared key. THREE CONSECUTIVE `eval.memory_harness` runs (2026-08-03, D-130, verifying an unrelated R2 fix) fully closed D9 with no active replacement — direct graph query confirmed all 6 facts on the key closed, several `superseded_by` pointers naming fact_ids that no longer exist at all (deleted by a later run's own uncleaned chain link). **CAUGHT ONLY BY `eval/test_structural_refusal.py::test_sref_graph_known_set_is_not_vacuous`** (`{"ray","dad"} <= graph_subject_ids()`) going red — an acceptance check for an ALREADY-MET requirement (REQ_STRUCTURAL_REFUSAL, RULED MET D-128) failed not because anything regressed, but because a fixture eroded underneath it; the anti-vacuity case was the only reason anyone noticed. **FIXED, same session:** (1) D9 reseeded via `scripts.demo_seed._seed_one` — same code path, same FIXTURES tuple, value verified byte-identical by decrypt ('metformin 500mg twice daily', confirmed via `harness.partition_crypto.decrypt_fact_value_for_caller`); `graph_subject_ids()` and `test_structural_refusal.py` (7/7) both confirmed green for the right reason (ray+dad genuinely active, not accidentally non-vacuous). (2) `eval/memory_harness.py` gains `_restore_ray_medication_fixture()` — deletes every fact on the (maya,ray,medication) key regardless of session id, then reseeds the exact D9 tuple through `_seed_one` — wired into all three of MEM-116/117/118's `finally` blocks (checked: no other memory-harness scenario shares this shape — MEM-115 and earlier use only randomized `memtest-*` owners with no live-pipeline leg). Verified stable across 3 consecutive `eval.memory_harness` runs post-fix: 13/17 every time, failing set exactly {115,116,117,118} (Groq-dependent flakiness per TD-145/TD-146, unrelated to this fix), 'ray'+'dad' both active after every run. `--layer 7`: standing batteries 323 passed/8 xfailed, RATCHET PASS. | D-130, 2026-08-03 | RESOLVED |

| TD-152 | GATE | [epistemic-panel-v2-blind] `/api/fact_history` (`demo_dashboard.py:950-1035`) decrypts a fact's value only when `key_version==1` or an `as_member` param is supplied; `demo.html:991` never sends `as_member`. Every partition-crypto (v2) fact therefore renders as a bare `—` in the Epistemic State panel, not just Ray's medication -- confirmed at the data layer, not a display-order artifact. Demo-visible; searched `docs/` -- not documented anywhere as a named limitation. Two forks named, neither decided: pass the caller identity through so values render, or file this as a standing limit and settle what the presenter says about the dash. Cross-reference: Bank 1's `reviews/CUTOVER_C5CaveatVerify__live-verification-trust-ladder-header-panel__v20260803_1527.md` verified this same panel's trust-marker rendering live and found it correct -- for v1-keyed facts only; that verification never exercised a v2 fact, so it did not (and could not) have caught this. | Index Bank 2, D-136, 2026-08-03; source: `docs/research-technical/HIP_RepoBankingGaps__demo-lane__v20260803_1527.md` §6 | OPEN |
| TD-153 | GATE | [voiceprint-decrypt-failure] `~/hip-harness/data/voiceprints/maya.npz` and `sam.npz` fail Fernet decryption against the current key (`InvalidToken`); both touched 2026-08-03 13:16, while `bill.npz` is untouched since Jul 31 and decrypts fine. Not investigated -- found incidentally while confirming Bill's real enrolled print for the Voice 12 recording run (this session). Consequence if it persists: the scripted-voice demo depends on all three prints -- if maya/sam cannot decrypt, they cannot be recognised from audio and the scripted-voice beat breaks for two of three household members. A pre-incident backup exists at `~/hip-harness-backups/voiceprints_20260803_140540/` (outside the repo, SHA-256 verified against the live files at backup time) -- the only recovery path if this needs to be rolled back rather than re-enrolled. | Index Bank 2, D-136, 2026-08-03; found this session (voiceprint-adaptation dispatch); also: `docs/research-technical/HIP_RepoBankingGaps__demo-lane__v20260803_1527.md` §7 | OPEN |
| TD-154 | ENG | [transcription-ms-null] `transcription_ms` is structurally NULL on the demo (text-query) path -- `process_text_query` never performs its own STT, so a spoken turn's epistemic record is indistinguishable from a typed one on this field (`server/voice_orch.py`'s realtime path computes `_transcription_ms_accum`; the text-query path never sets it). Named in the source material's own words as "`REQ_VOICE_TRACE_CAPTURE`'s acceptance rows" but never filed as a defect until now. **CORRECTION ON FILING:** no file named `REQ_VOICE_TRACE_CAPTURE` exists anywhere in this checkout or any sibling worktree searched (roadmap/vo/harness/cutover-demo/dev/ungoverned/crypto-p1/crypto-p2/stage1-wip) -- the closest match is `REQ_DEMO_WEB_REPLAY__remote-voice-demo-surface__v20260801_0732.md`'s **R2 -- TRACE CAPTURE ENTERS THE VOICE CONTRACT** section, which specs per-turn TTS AUDIO retention (output side), not STT timing (input side); it does not actually name `transcription_ms` or any acceptance row for it. The name traces verbatim to the banked source doc, not a filing error introduced here -- recorded so a later reader does not go looking for a REQ that isn't there. **ADDENDUM (Index Demo 2, D-142, 2026-08-03), CORRECTING THE CORRECTION ABOVE:** `REQ_VOICE_TRACE_CAPTURE` DOES exist -- on `hip-vo` branch `voice-port`'s tree, at `docs/requirements/REQ_VOICE_TRACE_CAPTURE__tts-audio-capture-and-spoken-vs-typed-provenance__v20260801_0923.md` (filed Voice 22, commit `84a7146`). Confirmed live via `git show voice-port:...` -- it genuinely discusses `transcription_ms` being structurally null on the demo path, matching this TD's own subject exactly. The CORRECTION ON FILING above was itself incomplete: its nine-checkout search only ever inspected each repo's CURRENTLY CHECKED-OUT working tree; `~/hip-vo` was checked out to `main`, not `voice-port`, when that search ran, so it never actually looked where the REQ lives. The original correction is preserved above, unedited, as the record of what that search actually found (and its limits) -- this addendum does not replace it, it completes it. | Index Bank 2, D-136, 2026-08-03; source: `docs/research-technical/HIP_RepoBankingGaps__demo-lane__v20260803_1527.md` §8; corrected by Index Demo 2, D-142, 2026-08-03 | OPEN |
| TD-155 | SEC | [voice38-ambiguity-leaks] SIX confirmed leaks in the Voice 38 Set 1 baseline (`eval/probes/results/voice38_set1_results.csv`, banked Bank 1), four of them clean and unwarranted -- ALL six on high-ambiguity questions from `eval/probes/HIP_ProbeSet1_General_200.txt` (banked Bank 1), the general/unscripted set where the permission layer is expected to stay silent. Worst instance: "What are we assuming about them?" returned the 9am schedule rule, trash day, and the full street address. Household facts enter every turn by design (recency-window injection); when the model has nothing concrete to answer an ambiguous question from, it reaches for them anyway rather than declining or asking for clarification. Not investigated beyond this measurement; no root cause pinned. | Index Bank 2, D-136, 2026-08-03; source: `docs/research-technical/HIP_RepoBankingGaps__demo-lane__v20260803_1527.md` §9 (first finding); data: `eval/probes/results/voice38_set1_results.csv` (Bank 1) | OPEN |
| TD-156 | SEC | [structural-empty-set-general-knowledge] SEVEN items in the Voice 38 baseline produced the exact structural empty-set refusal string with `guard_triggered=False`, `path=generation`, `reply_source=model` -- the model producing refusal-shaped prose with no guard actually firing, same defect SIGNATURE this repo's own register already tracks as **TD-143**/**TD-144** (both filed from `PW010`/`PW015`, both RULED RESOLVED at D-128/D-129 per this file's own header). These seven were reached INDEPENDENTLY, via general-knowledge questions (`eval/probes/HIP_ProbeSet1_General_200.txt`, banked Bank 1) rather than PW010/PW015's household-fixture path. Likely related to **TD-149** (still OPEN): INJ-6b's structural-refusal keying covers only the 12 `_TARGETED_ATTRS`, so a query landing outside that set stays on the model path with no structural refusal wired -- general-knowledge questions are exactly the kind of query TD-149 already names as uncovered. Not confirmed as the same root cause as TD-149; flagged as the most likely explanation, not asserted. Neither this finding nor its likely TD-149 connection was on the demo lane's register before this filing. | Index Bank 2, D-136, 2026-08-03; source: `docs/research-technical/HIP_RepoBankingGaps__demo-lane__v20260803_1527.md` §9 (second finding); related: TD-143, TD-144, TD-149 | OPEN |
| TD-157 | OPS | [voice12-stale-stdin-and-tmp-risk] `/tmp/voice12_record_bill.py` skips prompts under real use -- `input()` silently consumes a buffered newline left over from the prior ~5s audio-capture window (or a reflexive keypress during it), advancing more than one step per Enter. The script lives in `/tmp` and will not survive a reboot -- `/tmp` has been cleared on this machine before. **STATUS NOTE, not a fix performed by this filing:** a corrected replacement was already written and delivered this same session at `~/Downloads/voice_record_bill.py` -- explicit stdin flush before every prompt and again after each capture (`termios.tcflush` + a non-blocking drain), tested against the real delivered `run()` function through a pseudo-terminal with a scripted stray keystroke injected mid-capture (PASS), and against a deliberately unflushed control copy reproducing the exact reported bug (hung, as expected) -- proving the scenario is discriminating, not just cosmetic. The replacement has not been run for a real recording session and is itself not yet banked into any repo (still `~/Downloads`-only) -- filing this TD against the ORIGINAL `/tmp` artifact specifically, which remains exactly as broken and exactly as at-risk as reported. | Index Bank 2, D-136, 2026-08-03; source: `docs/research-technical/HIP_RepoBankingGaps__demo-lane__v20260803_1527.md` §10; fix delivered this session, unbanked | OPEN |
| TD-158 | ENG | [mem-118-p8-monotonicity] MEM-118 (and MEM-116/117, ONE shared root cause) fails deterministically under the CORRECT interpreter (`$HIP_DEV_PYTHON`, NOT system `python3` -- see the methodology finding below) with `delta transition='unresolved'` (MEM-118), "original fact still open" (MEM-116), "trust level CORROBORATED, expected ASSERTED" (MEM-117). TRACED, not guessed: TD-151's own fix (D-130, `_restore_ray_medication_fixture` in `eval/memory_harness.py`) reseeds the (maya,ray,medication) D9 fixture to CORROBORATED trust in all three tests' `finally` blocks, so every run starts with a CORROBORATED head. When the live pipeline (Groq `detect_and_apply` -> `memory_engine/store.py::encode()`) attempts to supersede it, `encode()`'s own P8 cross-principal write-monotonicity override (subject != owner, incoming trust < existing head trust -> SUPERSEDE forced to UNRESOLVED, head retained) fires -- confirmed live via the exact log line `server/voice_orch.py:2403` emits, "P8: cross-principal trust-regression write parked", reproduced deterministically across 3 consecutive runs. REAL, not environmental: two independently-correct mechanisms (TD-151's fixture-restore discipline; P8's cross-principal trust-monotonicity gate) interact so that MEM-116/117/118's own scripted scenario -- a single live conversational supersede of a CORROBORATED fact -- cannot pass as currently written, since one live utterance can never itself achieve CORROBORATED trust. SEPARATE METHODOLOGY FINDING, recorded here so it is not lost: `python3 -m eval.memory_harness` (system python, `/opt/homebrew/bin/python3`) silently lacks `pipecat`, which MEM-116/117/118's live pipeline needs, producing a COMPLETELY DIFFERENT, misleading failure ("no new record"/similar, swallowed by `scripts/text_demo.py`'s own exception handler) that ALSO reads red, masking the real P8 mechanism above. The correct interpreter is `$HIP_DEV_PYTHON` (`~/hip-dev/.venv/bin/python`) -- `scripts/run_harness.sh` already uses it correctly; this session's own direct `eval.memory_harness` invocations, D-130 through D-146, did not. The memory-harness PIN and gating decisions those dispatches made are UNAFFECTED (the failing COUNT and SET matched either way), but any citation of MEM-116/117/118's specific failure TEXT from before D-147 should be treated as possibly interpreter-confounded. Candidate fix directions, none built: (a) scope P8's cross-principal check to exclude fixture-seeded heads; (b) seed at a lower trust tier than CORROBORATED in the restore; (c) rewrite these three scenarios' own assertions to expect the correct-and-intended UNRESOLVED park rather than SUPERSEDE/ASSERTED. NOT FIXED, NOT SMOOTHED, BASELINE NOT UPDATED, per D-147's explicit instruction. | D-147, 2026-08-04 | OPEN |
| TD-159 | OPS | [tooling-visibility] **A18, A29 and A30 render CLAIMED-NOT-VERIFIED on the ceiling status board — a VISIBILITY gap, NOT a coverage gap. The distinction is D-149's own and is the whole content of this entry: these three requirements ARE tested, and their tests DO run.** R18's coverage is `eval/test_lineage_block.py` (16 cases, D-105/D-107); R29/R30's is `eval/test_sensitivity_registry.py` (31 cases, D-75). Both files are wired into the standing batteries and pass on every harness run. The board's cross-check (D-149) matches acceptance rows to runners by the `test_ceil_a<N>_*` naming convention, and **both files predate that convention**, so the tool cannot see past the names to the coverage. The board reports this precisely rather than implying absence — each row's runner note says the coverage exists under another name — but a reader skimming the matrix sees three amber rows and may read them as untested, which is the risk this entry exists to keep visible. **WHAT WOULD CLOSE IT:** rename the relevant functions in those two files onto `test_ceil_a18_*` / `test_ceil_a29_*` / `test_ceil_a30_*` so the existing coverage becomes machine-visible — a mechanical rename across two files, no new test logic, and the board flips them to verified with no change to what is actually asserted. Named as the natural follow-up in D-149's own OPEN and deliberately left out of its scope (it was dispatched to build the cross-check, not to run a renaming pass through two other files). Secondary option if the rename is unwanted: teach the cross-check an explicit alias map — rejected as the default here because it would put the row-to-test relationship in a second place that can drift, the failure mode this project has paid for repeatedly. | D-149 cross-check, 2026-08-04 | OPEN — mechanical rename, no test logic changes |
| TD-160 | SEC | [data-gap-with-a-control-consequence] **`subject="dad"` and `subject="household"` are NOT enrolled in the identity registry, although both are in scope — and the consequence is that one of R9's six never-store categories currently has NO write-time check behind it.** Found at D-140 while building R8's write-time representation classifier. The classifier's original design derived `THIRD_PARTY_NONCARE_DOSSIER` from a subject-identity test — `subject` absent from `harness.role_resolution.known_subject_ids()` AND not `is_recognized_recipient(subject)` — which is the correct SHAPE for the category (a dossier on someone outside the care relationship is exactly a subject the household has no enrolled relationship with). It was caught by standalone unit tests BEFORE reaching `create_fact_node`: real fixture facts about **dad** and **household** classified as third-party dossiers, because neither passes the registry check, despite dad being the care recipient at the centre of the fixture and household being the shared-scope owner. Rather than ship a classifier that refuses legitimate household writes, D-140 removed the subject-identity check and moved `THIRD_PARTY_NONCARE_DOSSIER` into `ABSENT_CLASSES` — the honest choice, and recorded as such. **THE RESIDUE, STATED PLAINLY: R9 names six categories that must never be stored; five have a live write-time signal and this one has none. The reason is a DATA gap (unenrolled subjects), not a principle — nobody decided third-party dossiers are acceptable, and no requirement was weakened.** The category is currently unreachable by the live write paths, which bounds the exposure but does not close it: it arms the moment a write path produces a genuinely external subject. **WHAT WOULD CLOSE IT:** enroll dad and household (or whatever the registry's correct representation of a care recipient and a shared scope is — that is a modelling question, not a data-entry one), then restore the subject-identity derivation and re-run D-140's 13 standalone cases plus the dad/household fixtures. Until then the absence is asserted by a standing test so a placeholder cannot be added quietly. **ADDENDUM (D-R-178, 2026-08-05, Bill authorized — verified by live registry probe, not inferred): the inconsistency is WITHIN the fixture, not a uniform enrollment gap. `ray` IS dyad-registered — `known_subject_ids()` and `is_recognized_recipient()` both return True; `dad`, `household` and `elena` all fail both. Two care recipients in one fixture are therefore built differently, and every identity-keyed check treats them differently — which is a sharper statement of the defect than 'dad is not enrolled', because it locates the problem in the fixture's own inconsistency rather than in a missing step.** | D-140 R8 classifier build, 2026-08-03 | OPEN — data gap; R9 category unenforced meanwhile |
| TD-R-161 | TOOL | [mutation-target-line-drift] **`eval/harnesslib/mutation_targets.py` addresses its seeded INJ-7 mutant by HARDCODED ABSOLUTE LINE NUMBER inside `harness/injection_contract.py`, so any edit ABOVE that point silently retargets the mutation at whatever now occupies the line — including a comment, which cannot be killed.** Observed at Index Demo 14 (2026-08-04) while building and reverting REQ_UNRESOLVED_SUBJECT_GUARD: a SIX-LINE DOCSTRING addition near the top of the module shifted the INJ-7 refusal condition (`if (member_ids is not None`) from `:664` to `:670`. Line 664 then held a comment line; the harness dutifully mutated the comment, the seeded mutant survived because a mutated comment changes no behaviour, and `L7V2:MUTATION-SCORE-SELFTEST` went RED with `killed_with_full_killers=True survives_with_killers_excluded=False`. **VERIFIED, not inferred:** HEAD `:664` reads `if (member_ids is not None`; the same code in the edited file is at `:670`; the edited file's `:664` is `# the access-control string — not the empty-set string (feigned`. **NOT BEHAVIOURAL — this is the harness mis-aiming, not a governance regression**, and it read as one until traced, exactly as the same mechanism did at D-101/D-102. THIS IS A RECURRENCE OF **TD-142** (*on roadmap*), which recorded the identical failure mode when D-101's R12 fix shifted the target from `:619` to `:664` and D-102 resynced the two coordinates by hand. TD-142 filed the durable-addressing gap and did not fix it; this entry is the evidence that hand-resyncing coordinates does not hold — the SECOND unrelated edit to this file re-broke it, and any third will too. **FIX DIRECTION, scoped not built:** address the mutation target by a stable anchor rather than a line number — an AST/function-and-predicate lookup, a sentinel comment the target carries with it, or a named marker the harness resolves at run time — so an edit anywhere above it cannot retarget it. Filing only; per Requirements Discipline item 8 the fix needs a REQ. Filed under the lane-prefixed scheme (CLAUDE.md STANDARD PREAMBLE item 10, landed `f585443`); this is the roadmap lane's first `TD-R-` entry. | Index Demo 14 + 15, 2026-08-04 | OPEN — filed, not fixed; recurrence of TD-142 |
| TD-R-162 | SEC | [secret-inside-a-low-sensitivity-value] **A CREDENTIAL SITS AT THE BOTTOM OF THE SENSITIVITY ORDER, AND NEITHER CLASSIFICATION AXIS CAN SEE IT.** ORTH-1 case **DC-080** stores `f-hh-wifi` = **"wifi password: hunter2"** under `attribute: household` (`eval/disclosure_conformance.json`, verified on roadmap at `c6117c1`). `household` maps to sensitivity **LOW** (seed D7 "trash pickup is Wednesday", `scripts/demo_seed.py`), so an authentication secret ranks BELOW every other class in the registry that R29/R30 exists to order. **WHY NEITHER EXISTING MECHANISM CATCHES IT — both are ATTRIBUTE-KEYED, and the secret is in the VALUE:** (1) sensitivity is assigned per attribute, and every fact under `household` gets the same level whatever its value says; (2) R8's write-time classifier is the same shape — `classify_representation(attribute='household', ...)` returns **ORDINARY_CLAIM** (executed, not read), because `_HARD_REFUSED_ATTRIBUTE_PATTERNS` matches the ATTRIBUTE NAME (`password`, `passwd`, `pin`, `secret`, `credential`, ... `harness/representation_class.py:160-162`), and this fact's attribute name is `household`. **ONLY PER-FACT (value-level) SENSITIVITY COULD SEE IT.** **R8's `AUTHENTICATION_SECRET` CLASS EXISTS AND IS DEAD CODE AGAINST TODAY'S WRITE PATHS — cited as instructed:** the constant is defined (`harness/representation_class.py:132`) and listed in `REPRESENTATION_CLASSES`, but **ZERO attributes in `CANONICAL_ATTRIBUTES` or `DERIVABLE_ATTRIBUTES` classify to it** (verified by executing the classifier over the whole vocabulary this dispatch: empty set), and even when it IS returned nothing refuses on it — `memory_engine/store.py:448` raises on `UNKNOWN_HIGH_RISK` **and only that class**, so an `AUTHENTICATION_SECRET` verdict would be stamped onto the node and persisted. The class is a backstop for a future attribute, not a live control, exactly as its own module docstring says ("Group 2 ... UNREACHABLE by anything the live write paths produce today"). **THE DEEPER TENSION, recorded rather than resolved:** the fix a reader reaches for — classify on the value — is what D-50 Principle 6 / R8's RULING 1 forbids, and `representation_class.py`'s own Group-3 reasoning turns on exactly that prohibition. So this is not a missing regex; it is a gap between what attribute-level classification can know and what the ceiling requires. **SCOPE:** DC-080 is FIXTURE data, so nothing leaks in production from this row alone — but DC-080 is one of the two cases REQ_UNRESOLVED_SUBJECT_GUARD is built to keep passing UNGUARDED, which means a standing contract case now asserts that a credential-bearing fact is admitted on a subjectless turn. **NOT FIXED, by instruction (Index Demo 26). Filing only.** | Index Demo 26 / D-R-171, 2026-08-04 | OPEN -- filed not fixed; needs a ruling on per-fact sensitivity vs the content-blindness rule |
## Notes
- **2026-08-02 (D-109) MEMORY-HARNESS BASELINE RE-PINNED: "13-15/17, failures a strict subset of {MEM-115, MEM-116, MEM-117, MEM-118}." A run outside that range — any failure outside the four, or a count below 13 — is still a STOP.** The old pin ("13/17, the same four") was falsified in the passing direction by D-107's three same-code runs (14/17, 15/17, 15/17) and D-108 traced why; its four grounds, one line each: (1) MEM-117 flipped FAIL→PASS between two consecutive runs of identical code with no reset between them — a code-caused delta is deterministic, this is not. (2) None of D-107's changed code is on the four scenarios' paths (encode's new param defaults to None and CEIL-CONV pins the props byte-identical without it; the memory harness runs neither demo_seed nor FixtureManager; test_lineage_block is pytest-only). (3) The movement matches the recorded mechanism — MEM-116/117/118 sit downstream of the live Groq detect_and_apply call, and their own pass/fail texts show the supersede landing or not landing. (4) The direction was pass-ward only — no new failure appeared in any run, and MEM-111 (the scenario nearest D-107's change) passed all three. Caveat from the same trace: MEM-116 has a SECOND, structural red mode unrelated to Groq (TD-145) — so its red is not evidence of environment alone until TD-145 is fixed. **ADDENDUM (D-110): the range's ceiling of 15 is now known to be STRUCTURAL, not lucky — MEM-115(b) can never pass under the current architecture (TD-146: the contract never sees a member-private candidate post-P2), and MEM-116 is effectively permanent-red too (TD-145's MasterKeyMissingError mode fires precisely when its environmental mode does not). So the live axis of variation is only {MEM-117, MEM-118}, and 15/17 is the attainable maximum. The day either TD's fix lands, this pin MUST be updated in the same dispatch — a 16/17 run today would be a STOP by this pin, and correctly so, since it would mean a structural impossibility passed.**
- 2026-07-09 care_coordination script note (CORRECTED by Layer 2 first sweep): sarah is the SPEAKER of T04, not the queried subject, and she exists in scripts/dev_seed_registry.py but NOT in the live member registry — /api/text-query returns 400 "unknown member" and the scripted turn cannot fire at all. Fix fork: register sarah on the live path, or change T04's speaker to maya/sam. Pinned as known-failure L2:care_coordination.T04. (The original version of this note mischaracterized the failure as an INJ-7 refusal-type issue.)
- 2026-07-09 epistemic timeline note: the epistemic timeline (and vault) accumulate state across demo runs in the same browser session. Without a reset between runs (POST /api/reset), a second LOAD shows chains from the prior run, which makes the timeline inconsistent with the current dialogue. Operationally: always POST /api/reset before a fresh demo run; the dashboard currently has no auto-reset on LOAD. Consider adding one.
- 2026-07-09 launchd note (pattern-match to TD-103): `launchctl bootstrap gui/501 com.hip.demo.dashboard.plist` on the Mini failed with "Bootstrap failed: 5: Input/output error" after a bootout during F-6 deploy — same I/O-error-5 signature TD-103 records for com.hip.voice.orch. Dashboard is currently running as a MANUAL process (`.venv/bin/python -m server.demo_dashboard --host [REDACTED-TAILNET-ADDRESS] --port 7871`, log /tmp/hip-dash.log) started over SSH; it serves the pulled code fine but will NOT survive a Mini reboot. Re-bootstrap the plist (or extend TD-103's start_manual.sh approach to the dashboard) before the next demo.
- TD-101 is the highest-severity open item; it sits in the STALE security audit. Engine-track, but do not let a public-facing demo run against unauthenticated endpoints.
- TD-103 blocks the voice track and the TD-042 mic test until closed. The 2026-07-09 dashboard note above suggests the I/O-error-5 failure is a machine-level launchd condition, not plist-specific.
- TD-108 is the primary liability-severity reducer for the platform. The per-fact consent ledger caps breach blast radius. It must ship before scale, not after. See CHG-1 in HIP_PropagationWorkOrder and Ecosystem NDA Section 4.3 for the full liability argument.
- TD-109 is a build requirement, not a debt item. No public claim about speaker-recognition consent controls is accurate until TD-109 ships. The CHG-3 wording in the NDA states the architecture fact only; TD-109 is the control that backs any future consent claim.
