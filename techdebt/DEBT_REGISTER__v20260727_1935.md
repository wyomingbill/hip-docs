# HIP Tech Debt Register — known issues
STATUS: LIVE
RECONCILED-AGAINST: session 2026-07-27, TD-136 ported (DISPATCH 36 register reconciliation phase 2, HIP_RegisterReconciliation__cross-branch-id-collisions__v20260727_1930.md): main branch TD-131 (commit `4390240`) ported verbatim as TD-136 per Bill's ruling that roadmap's numbering is authoritative -- main's own TD-131 is unrenumbered and now diverges permanently from roadmap's TD-131 (a different, unrelated git-worktree finding). Prior: session 2026-07-27, TD-135 added (REQ_DOC_RENDERING build): whitepaper/archive/HIP_White_Paper_Augmented.docx found corrupt (truncated zip, unreadable since first commit) during the docx-to-text renderer's baseline run -- named, not fixed; its rendering stays MISSING by design until the source file is repaired or removed. Prior: session 2026-07-27, TD-134 burn-down (check_g0 mutation survivors): 4 of the 5 surviving `harness/g0_invariant.py:check_g0` mutants killed via 4 new killer probes in `eval/harnesslib/mutation_targets.py`; check_g0 score 3/8 (0.38) -> 7/8 (0.88); overall mutation score 78/128 (0.61) -> 82/128 (0.64); the fifth survivor (:67 `delete_last_operand(Or)`) confirmed a true equivalent mutant, left unkilled by design -- see TD-134. check_g0's own behavior unchanged; no new mutation sites; all other modules' survivor counts unchanged.
Rule: one running register. New issues appended with an ID. Resolved issues marked RESOLVED with the commit that closed them, never deleted. When this file changes materially, cut a new timestamped version and repoint LATEST_DEBT.

## Severity: SEC (security) | GATE (blocks a promotion/demo) | ENG (engine track) | OPS (tooling/reliability)

| id | sev | issue | source | status |
|----|-----|-------|--------|--------|
| TD-101 | SEC | Unauthenticated dashboard endpoints still present; embed_text(fact["value"]) TD-030 gap open (embedding path may touch a fact value pre-encryption); git-history scrub pending | SECURITY_AUDIT (INDEX, STALE) | OPEN |
| TD-102 | GATE | issue_INT-001_*.json appeared in eval/integration_issues/ after a passing 6-check gate; likely transient run against unseeded state. Verify before next promotion. | Truth-layer delivery report | OPEN |
| TD-103 | OPS | launchd bootstrap for com.hip.voice.orch fails 1-in-N (I/O error 5), does not inject NEO4J_PASSWORD reliably; voice server start is non-deterministic. Fix: committed start_manual.sh that exports the password and launches directly. | 2026-07-05 session | OPEN |
| TD-104 | OPS | Neo4j password contains shell-special char (!); trips zsh/plist/sed edits. Rotate once to a 24-char alphanumeric to remove the recurring friction. | 2026-07-05 session | OPEN |
| TD-108 | SEC | Per-fact consent-and-routing ledger not yet shipping: every fact must carry sensitivity classification, owner identity, source provenance, consent scope, allowed destinations, retention limit, and immutable audit trail before the router can enforce delivery. PRIMARY LIABILITY-SEVERITY REDUCER - ship pre-scale, not as a later hardening step. Breach blast radius is unbounded without this control. FINAL CHG-6 see HIP_FinalizationOrder. Delivered as HEL: Phase 1 BUILT (a637fa4, dual-write + encryption + crypto-shred); Phase 2 SPEC'd (fact.detect + value.decrypt, LATEST_HEL_PHASE2.md); per-fact timeline = Projection 2 over HEL, not a separate store. | HIP_PropagationWorkOrder CHG-1/CHG-6; specs/LATEST_EPISTEMIC_LEDGER.md | IN_PROGRESS |
| TD-119 | ENG | [model-robustness] Edge model (qwen2.5:7b) phrase-sensitive under PERSONAL_FACT_GROUNDING_GUARD: with "(about Elena) Jardiance 10mg" verbatim in context, "What medication is Elena on now?" answers correctly but "What medication does Elena take?" returns the refusal string. Deterministic; confirmed by prompt-identical A/B 2026-07-08 during TD-114 fix. Guard wording must not be loosened casually — it exists against verified confabulation (see orchestrator.py guard comment). Candidate fixes: guard phrasing that names the "(about X)" annotation explicitly, or paraphrase-robustness eval across N phrasings per fact before any wording change. RESOLVED via four stacked fixes, gated by T119 (5-phrasing paraphrase matrix, best-of-2 per leg): (1) cross-subject admitted facts render in a dedicated 'Confirmed facts about other people' prompt section on question turns (second-person section header beat the inline '(about X)' annotation; A/B inline 9/15 vs section 15/15; instruction-only guard clause had zero effect); (2) imperative openers tell/show/give/list/name/remind added to question-opener regex (was classified declarative, skipped guard, fired write-detection); (3) possessive normalization in entity extraction ('Elena's' now resolves to elena); (4) imperative-dative strip ('tell me X' no longer resolves subject to the requester). Edge text-path temperature 0.3→0.0 (held-fact answers flickered ~1-in-8; demo/test surface is now deterministic modulo Ollama load noise, absorbed by best-of-2). Declarative turns keep inline annotation — partition broke the E1 ack (caught by ratchet, scoped back). Tier L 10/10 twice consecutively, baseline locked. | G2 harness build 2026-07-08 | RESOLVED |
| TD-114 | ENG | [correctness, HIGH, SYSTEMATIC] HITL-3 — Bill's just-asserted Elena→Jardiance fact not retrievable one turn later ("not confirmed yet"). SECOND reproduction of write-then-can't-read pattern (first: TD-111 Sam penicillin). Epistemic pane shows the just-asserted fact in closed/struck state with SUPERSEDE label — active head landing in retired state on write. LIKELY ROOT CAUSE for both TD-111 and this finding: new fact is being written but closed immediately (possibly self-supersede loop residue, or the CORROBORATED seed is being set as the new head while the incoming ASSERTED fact is closed as the prior). Fix must explain why two independent members/attributes both exhibit the same symptom. RESOLVED: two independent defects — (1) Groq "switched from X to Y" emits update+retract; unscoped retract_fact(owner, attribute) closed the just-written head 25ms after creation (closed_by='retracted', no successor); (2) literal "null" subject string (TD-111). Fix in harness/fact_change.py: same-batch retract suppression for updated attributes + subject-scoped retracts + _clean_subject(). Gated by Tier L G2 (both legs), RED-confirmed pre-fix, 9/9 twice post-fix, baseline locked. See docs/testing/LATEST_TD114_FIX.md. | HITL-3 2026-07-08 | RESOLVED |
| TD-115 | ENG | [correctness] HITL-3 turn 3 — query about "my mother Elena" answered with "Dad's health." Wrong subject and wrong gender. Subject resolution is mapping Elena to the wrong household member. May be a Groq extraction error (subject="dad" instead of "elena") or a retrieval/injection error surfacing the wrong member's facts. Reproduce: ask as bill "What do we know about my mother Elena?" — check subject extracted by fact_change and what injection_contract surfaces. RELATED (2026-07-09, Layer 2): stable ack misattribution — Maya says "Ray takes metformin 500mg twice daily", ack replies "YOU take metformin..." even when the write correctly lands subject=ray (graph verified). Ack wording ignores the fact's subject. Pinned as known-failure L2:three_zone_demo.T01. | HITL-3 2026-07-08 | OPEN |
| TD-116 | ENG | [demo] HITL-3 — Dialogue pane does not auto-scroll to newest turn. Evaluator cannot see current exchange without manual scroll. Must auto-scroll on new turn render. RESOLVED (F-6, b5f0acc): scrollIntoView on a sentinel was unreliable inside the flex/overflow:hidden layout; replaced with scrollTop=scrollHeight on the scroll container, fires on each reveal. Browser-verified. | HITL-3 2026-07-08 | RESOLVED |
| TD-117 | ENG | [demo] HITL-3 — Q+A render simultaneously, reads as pre-packaged. Undercuts live-routing story — evaluator sees the answer appear at the same instant as the question, no latency signal that routing/thinking occurred. Consider streaming reply or staged render (Q lands → brief pause → A appears). RESOLVED (F-7, 77dbd01): visibleCount staged reveal — user Q near-instantly, HIP A 700ms later, always a prefix of the current turns array so a shrunk fetch resyncs. Also fixed a stale-session leak found in testing (first transcript poll raced the 1s status poll and used the previous run's session_start, flooding the pane with old history; status now refreshed before the session enables). Browser-verified: 4-turn run, 8/8 turns rendered, no drops/duplicates, stagger visible on every pair. | HITL-3 2026-07-08 | RESOLVED |
| TD-118 | ENG | [demo] HITL-3 — Routing pipeline does not record every turn. Some turns missing from routing row log, giving inconsistent history panel. Evaluator cannot reconstruct full session from the routing view. | HITL-3 2026-07-08 | OPEN |
| TD-113 | GATE | [demo-credibility] HITL-1 — Epistemic pane is developer-view, not operator-view. Raw engine vocabulary surfaces (SUPERSEDE/HARDEN/DERIVED/confidence enums), no visual hierarchy, hero object (medical supersede old→new) buried in list. Required for demo/credibility: (1) group-by-member; (2) confidence as visual indicator (makes trust-regression legible without explanation); (3) supersede event as centerpiece — "changed by X, on [date]" prominent; (4) transition-type labels moved to hover/tooltip. Currently unusable as a live demo pane without narration to decode every field. | HITL-1 2026-07-08 | OPEN |
| TD-111 | ENG | [correctness] HITL-1 turn 6 — Sam's penicillin allergy not retrieved after write+ack. Fact confirmed in timeline (Sam·allergy·penicillin ASSERTED). INJ-6b empty-set guard fires as if no admitted fact exists. Read path or injection contract not surfacing the just-written fact for the speaking member's own personal query. Possible link to TD-044 (INJ-6b keyword bypass). Reproduce: assert allergy as sam, immediately ask "What allergies do I have?" as sam → gets empty-set refusal instead of retrieval. Root cause: Groq literal "null" subject string; fixed via _clean_subject() with TD-114 (same commit); gated by Tier L G2 leg 1. See docs/testing/LATEST_TD114_FIX.md. | HITL-1 2026-07-08 | RESOLVED |
| TD-112 | ENG | [naturalness] HITL-1 turns 6+7 — single refusal string ("I don't have that confirmed yet") used for two structurally different guards: INJ-6b empty-set (missing fact) and INJ-3 cross-member privacy deny. Privacy boundary reads as uncertainty. Audience cannot distinguish "governed refusal" from "data gap." RESOLVED via INJ-7 (F-4): a personal question about another REGISTERED member is refused structurally with access_control_refusal() ("That's Maya's information — I can only share it with Maya") — model never invoked. Existence-invariant: membership is the boundary, so the refusal is identical whether or not the fact exists (nothing leaks). Registered members are now resolvable subjects (retrieval is owner-scoped, so the other member's facts never reach the contract — membership, not INJ-3 denial counts, is the detection signal). Care recipients (Ray/Elena) unaffected. Gated by G4 (4 assertions incl. existence-invariance) + E6 pins the empty-set string; Tier L 11/11 twice, injection harness 11/11. | HITL-1 2026-07-08 | RESOLVED |
| TD-110 | ENG | [governance-decision-required] Cross-member write authority gap — HITL-1 finding. Any authenticated member can supersede another member's health fact (e.g. Maya overwrites Ray's medication record) with no authority check, no provenance gate, and no corroboration requirement. Two unresolved forks: (A) caregiver authority is intended — demo must surface it visibly or it reads as a bug; (B) cross-subject writes should land as UNCONFIRMED and require a second signal to supersede. Also: new fact opens as ASSERTED while superseded seed was CORROBORATED — write path does not check trust-level regression. INJ-3 blocks cross-member READ but no equivalent gate exists on WRITE. Decision required before fix. | HITL-1 2026-07-08 | OPEN |
| TD-120 | ENG | [model-robustness] "What did I tell you about my mother's medication?" retrieval fails — same class as TD-119 (phrase-sensitivity under PERSONAL_FACT_GROUNDING_GUARD). Diagnosis (2026-07-09) found THREE defects: D1 first-person token suppressed named-entity subject resolution (FIXED, 20db3ed — Phase 3 now additive); D3 INJ-2 keywords missed inflections, e.g. plural "medications" (FIXED, 20db3ed); D2 the relational bridge ("my mother's") requires a relationship fact the write path never stores from "My mother Elena ..." utterances — OPEN, separate bounded task (touches Groq extraction). The headline phrasing is a D2 case and still fails: pinned as known-failure L2:care_coordination.T02 in the harness baseline. | care_coordination run 2026-07-09 | OPEN (D2 only) |
| TD-121 | ENG | [write-path, demo-credibility] Groq fact-change extraction appeared NONDETERMINISTIC across runs on identical utterances (three_zone T04 supersede sometimes never landed while the ack claimed it). DIAGNOSIS (2026-07-09, live-verified): Groq at temperature 0.0 is stable PER INPUT (12/12 identical on the T04 prompt); the variance was in the detector's INPUT — call sites passed the turn's retrieval output, and with the target fact absent from the block Groq returns changes:[] 7/8 (measured). Retrieval membership varied because NOTHING in the demo graph carries an embedding (see TD-122), so every retrieval silently falls back to read_user_facts(limit=8, timestamp DESC) — a recency window with timestamp-tie ordering variance. Temperature/seed pinning was already in place and was NOT the cause. FIXED: (F1) detect_and_apply now reads the full active owner+household set itself (limit 50, no embedding dependence) — callers' facts are only a fallback; (F3) write-confirmation reply gate — a supersede-phrased declarative whose detection cycle produced 0 mutations and 0 idempotent no-ops gets an explicit "unable to save, nothing was changed" reply instead of the model's false ack (no silent retry, by decision). Gated by Layer 2 with T04/T05 un-quarantined. AMENDED 2026-07-16 (risk-memo §9 item 0, HIP_SIA_PhaseB__risk-memo__v20260716_1624.md): F3's phrase scope (_SUPERSEDE_PHRASE_RE) let a plain declarative with no supersede keyword ("I take atorvastatin 20mg every morning") skip the check entirely — a zero-write outcome for it shipped as a false ack (I-10's cited evidence turn). Widened to every declarative (`is_declarative_utterance`), one condition, live-verified (forced Groq 401 → sam's shellfish-allergy declarative correctly refused instead of falsely acked). See TD-125 for the retry shipped alongside it. | Layer 2 first sweep 2026-07-09; amended 2026-07-16 | RESOLVED |
| TD-122 | ENG | [retrieval, systemic] memory_engine/store.encode() writes every fact with embedding: None ("engine track doesn't embed in Phase A") — and BOTH the demo seed (scripts/demo_seed.py) and the fact-change write path route through encode(). Consequence: NO fact in the demo/harness graph has an embedding; search_facts_by_embedding always returns [] and every retrieval silently runs on the read_user_facts fallback (timestamp DESC, limit voice_max_facts=8). "Semantic retrieval" is currently a recency window. This was the hidden variance source behind TD-121 and is a standing risk: any graph >8 visible facts silently drops the oldest from model context. TD-030 TENSION, and its resolution pattern: embedding the VALUE regresses TD-030 (embedding invertible next to ciphertext) — but extraction_queue.write_facts already demonstrates the safe pattern, embedding subject+predicate ONLY (embed_text(f"{owner} {attribute}")). Fix direction: encode() adopts the same subject+predicate embedding; separately consider raising/removing the fallback's limit-8 window. **RE-CONFIRMED LIVE 2026-07-21 (REQ_CRYPTO_HARNESS_V2, vector-index taint coverage work), with the missing piece of WHY named for the first time: harness/fact_change.py's live detect_and_apply was migrated from write_facts() to store.encode() specifically for Phase 2/crypto reasons — that file's own line-749 comment: "route through encode() instead of write_facts() so the new node gets write_state + confidence_log" — meaning the migration that caused this was deliberate and unrelated to embeddings; nobody noticed it silently orphaned write_facts()'s embedding computation as a side effect. Live-verified (memory_engine/store.py:210-211): a fact written via the current live path (`_write_plain` -> `store.encode()`, the same call `fact_change.py` now makes) carries `embedding: None`; a fact written via the still-live-but-uncalled `extraction_queue.write_facts()` DOES get a real 768-dim embedding, confirming the dead-code diagnosis precisely. NEW IMPACT surfaced by this REQ: the vector-index taint guarantee (a canary planted in a fact's value can never appear in the embedding, because only owner+attribute are embedded) is only PROVEN STRUCTURALLY today, on the one code path that still computes an embedding — it is not exercised by any live-written fact, because no live-written fact has an embedding at all. Confirming this held REQUIRED calling write_facts() directly on a throwaway synthetic fact, bypassing the live path entirely, since no real fact in the graph could be used for the check.** | TD-121 diagnosis 2026-07-09; re-confirmed 2026-07-21 | OPEN |
| TD-123 | ENG | [write-path, data-model] Groq fact detector misfiles the fact's VALUE into the person-typed subject slot (subject="shellfish" for "I'm allergic to shellfish") — P2 i008 root cause. FIVE fix attempts each patched one downstream layer and regressed another: INJ-2 shortcut (fact never reached INJ-2), INJ-1 bypass 183d372 (admitted but then partitioned to other-people), retrieval merge b7ca9e6 (no-op in harness: NO fact has an embedding, see TD-122), known-persons write guard 9c441ff (nulled first-introduction elena; reverted 7cce12e), value-bleed write+partition guard 0fce966/47aecd0 (narrative values name their person subject — seed D9 "Ray takes metformin..." — broke PW012; partition change reverted). FINAL: single intervention at the write boundary — _normalize_subject in _apply_changes enforces the subject field's person type BEFORE the fact acquires graph identity: accept iff subject is a known person (members + existing fact subjects) OR (named in the utterance AND not bleeding from the change's own value). Covers corrupt entities (in value), first introductions (in utterance, not in value), and narrative values (known short-circuits before bleed is consulted). WARNING on every null. Read side fully restored to the T119-gated baseline; 183d372 INJ-1 bypass retained for live graphs with legacy rows. REMAINING: detector prompt hardening ("subject must be a PERSON, never the allergen/item/event" + negative example). CITATION WARNING (2026-07-17, `DISPATCH_ENUM_AUDIT__seed-fixtures-outside-detector-schema__v20260717_1135.md`): this ticket has been cited as the fix track for at least two OTHER, DIFFERENT bugs that are not subject-slot misfiling — `DIAG__p2-i019-detection-miss__v20260714_1500.md` cites "TD-123" for dietary/preference attribute disambiguation, and D-21 (HIP_DefectRegister) was cited as fixed-via-TD-123 for a missing-enum-category gap (`incident` not in `CANONICAL_ATTRIBUTES`). Neither is this bug. Before citing TD-123 as a fix track, check that the target defect is actually subject-slot person-typing — it usually isn't. | P2 i008 diagnosis 2026-07-12 | OPEN (normalization shipped, prompt hardening pending) |
| TD-124 | ENG | [write-path, resilience] Fact-change detector migrated Scout->openai/gpt-oss-20b under Groq's 2026-07-17 Scout retirement (b394d89; A/B harness eval/fact_change_ab.py + 24-case golden set f2f7007; Developer tier 1,000 RPM required — free tier 30 RPM caused the P2 i016/i019 429-drop class). P1 FOLLOW-UPS, in order: (1) durable outbox for detection writes — persist-first, replay on failure, NEVER silent-drop (today a Groq outage past the retry budget silently loses the fact while the poll window burns); (2) LiteLLM gateway in front of the detector call for provider portability; (3) DeepInfra as automatic fallback provider. Tertiary local fallback (Qwen 7B on Mini) requires a 500-1000 example validation set before it may enter the chain — the 24-case golden set is a migration smoke, not a fallback qualification. | Scout retirement 2026-07-12 | OPEN |
| TD-125 | ENG | [write-path, resilience] risk-memo §10 required a detection retry to ship alongside TD-121's F3 widening: gating every declarative on zero-write means Groq's P2/i019 semantic false negative (gpt-oss-20b returns changes:[] for some multi-party contexts, deterministic at temperature=0.0) now surfaces as a spoken refusal instead of a silent no-op — a silent lie traded for a loud, frequent failure without a retry. MITIGATION (2026-07-16): `detect_and_apply` retries once at temperature=0.2 (non-identical resample, since a same-temperature retry of a deterministic call cannot recover) before finalizing a zero-changes outcome. COST, not fully closed: (1) every genuinely-idempotent turn (Groq correctly returns changes:[], ~29-40% of turns per DIAG__p2-i019-detection-miss__v20260714_1500.md §7) now costs a second Groq round-trip before the outcome is stored — doubles detection latency for that class, and pushes some turns closer to DETECT_WAIT_S=30s; (2) temperature=0.2 is a heuristic, not a validated recovery rate — no measurement yet of how often it actually recovers a true false negative vs. just adding latency to the legitimate-empty case. TD-123's prompt-hardening (dietary/preference disambiguation) is still the correct fix for the false-negative rate itself; this retry only prevents that rate from becoming user-visible in the interim. | risk-memo §10, 2026-07-16 | OPEN |
| TD-126 | OPS | `scripts/demo_reset.py`'s log and voiceprint paths are hardcoded to `~/hip-harness` (`_HARNESS_ROOT = pathlib.Path.home() / "hip-harness"`) regardless of which checkout invokes it — running it from hip-dev during REQ_D03_D18's live proof (2026-07-16) deleted `hip-harness/data/voiceprints/maya.npz`/`sam.npz` (the frozen demo checkout's speaker-verification enrollment) and truncated its `voice_orch.log`/`router.jsonl`, even though Neo4j deletion was correctly scoped to the dev instance via `NEO4J_URI`. `demo_seed.py` recreates SYNTHETIC placeholder voiceprints on reseed, not a re-enrollment of whatever audio was actually there. **PARTIALLY FIXED (REQ_TD126_REMEDIATION, 2026-07-16): `_LOGS` derived from `ROOT`/`__file__` (matches `turns_demo.jsonl`'s existing pattern) — a hip-dev reset now truncates hip-dev's own `logs/voice_orch.log`/`router.jsonl`, verified live (hip-harness's copies' mtimes confirmed unchanged across a live run). Voiceprint deletion still reaches hip-harness for maya/sam specifically — that path comes from the REGISTRY's recorded `voiceprint_path` (set by `harness/speaker_id.py:43`'s shared `DATA_DIR` at enrollment/seed time), not this script's local constant, and fixing it would touch the shared-voiceprint architecture, out of scope for this build. CORRECTED fix direction (supersedes the original "re-enroll" note above): per item 3 of the same dispatch, DO NOT re-enroll voiceprints and DO NOT build speaker verification further — see TD-127.** | REQ_D03_D18 live-proof session, 2026-07-16; log-path half fixed 2026-07-16 (REQ_TD126_REMEDIATION) | PARTIALLY FIXED |
| TD-127 | GATE | **Speaker verification (Resemblyzer GE2E) is a stand-in for a vendor, not a production component — error rate unquantified.** Same posture as Deepgram/Cartesia on the voice leg: a placeholder proving the seam exists, not a shipped capability. Real vendors for this exact problem exist and are already deployed at scale (Pindrop, Nuance, Veridas; cable operators already run Pindrop in call centers) — HIP is not competing on speaker verification itself. First measurement (2026-07-16, `DISPATCH_TD126__speaker-verification-floor-analysis__v20260716_1846.md`): a genuinely different real voice recording scored **0.632** against the **0.50** "medium" threshold the main attribution path trusts — a different human clears the bar. Independently reproduced same-day during REQ_TD126_REMEDIATION's live verification: two different real audio samples scored 0.677 and 0.654 against Bill's own real print. This is not a bug to fix with a better threshold; it is a research library (GE2E, not built for adversarial/production speaker verification) being asked to do a job production verifiers are built and sold for. **Do not claim speaker ID in any demo or document.** The deliverable is the seam (governed context assembly keyed off an identified speaker) — swapping in a real vendor later does not touch the seam's shape. | DISPATCH_TD126, item 3 of the follow-up dispatch, 2026-07-16 | OPEN — by design, not a target for this codebase to close |
| TD-128 | SEC | **Frontier tier (Script 1 T04, `harness/frontier_client.py`) uses ONE household-wide Anthropic API key from `~/.env.dev`, not a per-member key.** Bill, 2026-07-17 (DISPATCH FRONTIER_TIER_BUILD Q4): "one key in ~/.env.dev, same as everything else. Per-member key storage is debt, not demo work. Register it." Any household member who can reach the disclosure gate (harness/disclosure.py) can trigger a spend/usage-visible call under the same credential — no per-member attribution, rotation, or revocation exists. Same convention as GROQ_API_KEY/OPENAI_API_KEY elsewhere in this repo today, so this is not a regression, but it is the first time a BYOK-framed feature ("Maya's key" in the prep doc's own language) actually ships on a shared key instead of a per-member one — the framing and the mechanism now visibly diverge. Fix direction: per-member credential storage (encrypted at rest, keyed to member_id) before this feature is presented as "Maya's key" rather than "the household's key." Separately noted live during this build: the key currently in `~/.env.dev` returned `401 authentication_error` ("API key is invalid") on a real call — reported to Bill directly, not a debt-register item (a broken credential isn't tech debt, it's a blocker; see DISPATCH_FRONTIER_TIER_BUILD's OPEN section). | DISPATCH FRONTIER_TIER_BUILD, 2026-07-17 | OPEN |
| TD-129 | OPS | **Two `ollama serve` daemons running simultaneously on this box** (PID 1399 since 2026-07-11, PID 937 since 2026-06-03), plus multiple `ollama runner` subprocesses, causing intermittent local-model inference timeouts under load. Found during REQ_MASTERKEY_PATH's `--full` verification (2026-07-18): `L3:INJ-7` and `L4:PW027` both failed with `"Sorry, I couldn't process that."` (`server/voice_orch.py:3238`), traced to `APITimeoutError('Request timed out.')` on the local-Ollama fallback call. Root-caused as pre-existing resource contention, not fix-induced. **CORRECTION (2026-07-19): the original "fix direction" below was wrong and, when tried, took down a load-bearing service.** The two daemons are NOT a stale/live duplicate pair -- they serve two DIFFERENT, both-necessary ports: PID 1399 is a harness-classifier-pinned instance on :11435 ("[harness] classifier Ollama ready at http://localhost:11435"); PID 937 (brew-managed, `homebrew.mxcl.ollama.plist`) was the DEFAULT-port :11434 instance that embeddings, model warmup, and the local text-query fallback all depend on. Stopping PID 937 via `brew services stop ollama` (tried 2026-07-19 on the theory it was the stale one) took :11434 fully down (`Connection refused`, not a timeout) and cascaded into 41 harness failures across L1-L4 -- an environment outage, not a code regression. Restored immediately via `brew services start ollama`; both ports confirmed healthy again. Real root cause is still open: two genuinely necessary services contending for the same underlying GPU/inference hardware under a 100-iteration harness load, occasionally pushing a local call past its timeout. Neither daemon is safe to simply kill. Fix direction (revised): investigate whether the two services can share load more gracefully (e.g. raising the local-call timeout, or scheduling the harness run to avoid overlapping heavy local-model use elsewhere), not "stop one." | REQ_MASTERKEY_PATH `--full` verification, 2026-07-18; correction 2026-07-19 | OPEN |
| TD-130 | OPS | **`scripts/demo_player.py` cannot reach an HTTPS/self-signed-cert server** (e.g. `server/voice_https_orch.py`, the actual server behind `hip_client.html` on port 7860) — its `requests.post` call passes no `verify=False`/cert handling at all, so any HTTPS target fails with `SSLCertVerificationError: self-signed certificate`. Found live during `REQ_STEP2_CALLER_SIGNING`'s proof (2026-07-19): the script's own signing fix worked (confirmed against a throwaway plain-HTTP `demo_dashboard.py` instance, real reply returned), but the identical command against `voice_https_orch.py` over HTTPS failed on the cert, not on identity. The script's own default target (`http://localhost:8080`, `server_default` in `main()`) is plain HTTP, suggesting `voice_https_orch.py` (HTTPS-only, self-signed cert, port 7860) was never its actual intended target — but this was never confirmed, and the mismatch was never previously visible because the script has presumably not been run against a live HTTPS demo server. Fix direction: either add `verify=False` (matching every other dev-tooling caller of this box's self-signed cert, e.g. `eval/integration_live.py`, `HarnessServer`) if HTTPS targets are meant to work, or leave it HTTP-only and correct the docstring/usage examples to stop implying it works against `hip_client.html`'s actual server. | `DISPATCH_STEP2_CALLER_SIGNING__hip-client-demo-player-preflight-tier-l__v20260719_1254.md`, 2026-07-19 | OPEN |
| TD-131 | OPS | **A fresh `git worktree` checkout can't run `eval.harness` without two manual, undocumented steps.** Found building REQ_CRYPTO_P1_DYAD_KEYS (2026-07-20) on a brand-new worktree (`roadmap-crypto-p1`): (1) bare `python3 -m eval.harness` fails inside the harness-owned subprocess server with `ModuleNotFoundError: No module named 'pipecat'` — the project venv is `[REDACTED-USER-PATH]/hip-dev/.venv`, shared across worktrees, but nothing in `eval/harness.py`/the harness server checks or documents this, so a plain `python3` looks like it should work and doesn't; (2) `certs/voice.key` (gitignored, only `voice.crt` is tracked) doesn't exist in a fresh worktree, so the subprocess server fails with `Missing cert/key: .../certs/voice.crt, .../certs/voice.key` even though `voice.crt` IS present — worked around by copying a matching key+cert pair from `hip-dev`'s checkout (confirmed matching pubkey via `openssl x509 -pubkey`/`openssl pkey -pubout`), not by regenerating (which would have produced a mismatched, uncommitted `voice.crt` diff). Fix direction: a `--guards`-time check (mirroring the existing `DEV_MARKER.txt`/`NEO4J_URI` checks in `eval/harness.py:_guards`) that fails loud with the exact fix ("run via hip-dev/.venv/bin/python3", "certs/voice.key missing, copy from an existing worktree or regenerate with certs/san.cnf") instead of a bare traceback three layers down. | REQ_CRYPTO_P1_DYAD_KEYS build, 2026-07-20 | OPEN |
| TD-132 | SEC | **Canary/taint scanning (`eval/harnesslib/canary_taint.py`, REQ_CRYPTO_HARNESS_V2) matches on literal substring, so it catches a verbatim leak but not a paraphrased one.** Found live 2026-07-21 building `CT-OUTPUT-GAP`'s real model call: the local edge model naturally paraphrases rather than quoting a fact's value verbatim — a bracketed canary marker embedded in AUTHORIZED content (f1, the alice/bob/mary fixture) did not survive into the model's real reply ("You take lisinopril daily for your health." — the medication name persisted, the literal canary tag did not). This is not specific to that one check: `scan_for_canary`'s whole design (`plant_canary`/`scan_for_canary`) is a literal-substring search across every surface (prompt, retrieval, vector index, logs, output). If a FORBIDDEN fact's secret were restated in different words on its way into any of those surfaces — a summary, a translation, a synonym swap, a number reformatted — instead of quoted directly, the planted token would never appear and the scan would report clean while the secret's substance still leaked. IMPACT: output-taint (and every other canary-based surface this REQ checks) currently proves the literal marker didn't appear verbatim, NOT that the underlying secret didn't leak in substance — a materially narrower guarantee than "the review prescribed" methodology implies at face value. This is a real hole in the taint method itself, not a bug in one check's construction. Fix direction: a SEMANTIC-canary approach — e.g. embedding-similarity between the forbidden fact's value and each downstream surface's content (flag anything above a similarity threshold, not just an exact substring match), or an explicit paraphrase/entailment check — replacing or supplementing literal-substring matching. Not attempted here; this register entry exists so the gap is never silently assumed closed by a passing `scan_for_canary` result. | REQ_CRYPTO_HARNESS_V2, CT-OUTPUT-GAP build, 2026-07-21 | OPEN |
| TD-133 | GATE | **Four-part check-discipline gaps (REQ_HARNESS_DISCIPLINE): standing audit (`eval/harnesslib/harness_audit.py` + `check_registry.py`, runs on every `--full`).** The audit enumerates all gated checks (L7/L7V2 Scenario scan + L6 record invariants) and verifies each declares a fault-injection twin, ground-truth fixture, coverage entry, and metamorphic wrapper. Reference implementations pass all four (PS1/PS2/OB4/OB5/G0/OB6 + G1-G4). **BURN-DOWN 2026-07-26 (same day, later): 7 TWIN gaps CLOSED** — `seal-to-dyad` (a node stamped `key_version=1` despite a real dyad-sealed DEK correctly fails the `kv==2` stamp check), `DK1` (a genuine plaintext/unwrapped D_priv row inserted directly is caught by the parse-as-bare-X25519 check, removed immediately), `DK4` (a wrap row for a fake dyad_id, bypassing `create_dyad`, correctly flips the SAME `events>=wraps` comparison DK4 uses once scoped to that one dyad_id — the global aggregate has too much historical slack, 241 vs 137, for a single-row injection to move it, so this scopes the identical formula narrowly instead), `P1` (removing sam's real `household_key_wraps` row locks him — and only him — out of a real household fact bill/maya still read; row restored and re-verified immediately after), `N1` (a fact stamped `owner='bill'` but actually sealed to maya's member key is correctly decryptable by maya, proving the cross-member-decrypt count can go non-zero), `N4` (a real, bill-owned, bill-decryptable fact whose value coincidentally matches a dyad_values string IS flagged by the same value-match logic — read_user_facts's own decrypt-or-skip behavior means a mis-stamped-owner dyad fact could never reach bill's view in the first place, so this is the correct-granularity proof of the check's OWN match logic), `P4` (row4's real classification checked against a deliberately wrong expected class correctly reports MISMATCH). All 7 verified live: `L7: 23/23`, `AUDIT: 3/3`, `four-part-roster PASS`, `RATCHET PASS`, no side effects left behind (each SQL-level injection removed in a try/finally; P1's restore additionally re-verified). **REQ_CRYPTO_P3_OPERATOR_BLIND part (c) step 5 reconciliation, same session:** PS1/PS2's own dedicated fault-injection twins (`PS1-fault-injection`/`PS2-fault-injection`) were retired along with PS3/PS4 (Bill's decision: v1-simulation fixtures, now impossible on roadmap post master-key-destruction) — PS1/PS2 registry entries now honestly debt-flagged (were stale `{"scenario":...}` refs to deleted scenarios) rather than silently broken; PS3/PS4 registry entries removed entirely; `L7:OB6` (the destroyed-state successor) registered with `twin: {"na": ...}` (self-fault-injecting, same pattern as `L7V2:MT2`). REMAINING gaps, by artifact (UPDATED 2026-07-27, ITEM 5 first slice — see BURN-DOWN below, DK2 and MT1 CLOSED and removed from this TWIN list): TWIN — PS1 (retired, see above), PS2 (retired, see above), RI1, P4-EXT(+row12/row13), RE2-RE7 (inline fault attempts exist per REQ_CRYPTO_P4 but markers were not individually re-verified by this audit build), ABM-route, FF1, FF3, FF4, SC1, SC1-E2E, CT-OUTPUT-GAP have no red-on-command demonstration. FIXTURE gaps — RI1 (structural assertion, no human-verified fixture row). METAMORPHIC gaps (seal-to-dyad/DK2/N2/DK3/P2/P1/N1/N4 CLOSED 2026-07-27 via property-based generation, removed from this list) — P4/P4-EXT family (write_rule.classify's write-time directive/attribute path — explicitly SKIPPED 2026-07-27, Bill has an open ruling on whether write-time directives should check attribute and closing these mutants would lock in current behavior before he decides; not attempted, not silently dropped), RE1-RE7, CT/CT-VECTOR-INDEX/CT-OUTPUT-GAP (inherit TD-132's paraphrase-evasion limit — a literal-substring canary IS the missing semantic-metamorphic wrapper), SC1/SC1-E2E, MT2-DECRYPT-REVOKE still have no rewording/property wrapper. Additional characterized limit found building the audit's `ps_scan_pattern` probe: the PS1/OB4 static-scan regex does not match a call with a space before the paren (`encrypt_fact_value (x)`) — asserted as current behavior in the probe so any change is noticed. Every flag prints on every run (FLAGGED, not silent); burn-down = replace `{"debt": "TD-133"}` entries in `check_registry.py` with real scenario/marker/probe refs. This entry is the register-side half of the audit's "zero HIDDEN gaps, not zero gaps" contract. **BURN-DOWN 2026-07-27 (ITEM 5 of Bill's five-item 2026-07-27 sprint instruction, first slice — 10 of the 46 then-open TD-133 gaps CLOSED, in the priority order docs/deliverables/HIP_TestingBestPractices gives: "CheckList template expansion + Hypothesis generation... closes the largest number of flagged twin/metamorphic gaps with two mechanisms" ranked first):** PROPERTY-BASED (Hypothesis-style; the `hypothesis` package is not installed in this venv, so — per checklist_gen.py's own precedent of an in-repo generator over the real CheckList library — this is a small in-repo, seeded (`random.Random`, deterministic) value generator, `eval/harnesslib/layer7_crypto.py::_property_seeded_values`) closed 8 checks' metamorphic gaps: `seal-to-dyad`, `N2`, `DK3`, `P2`, `P1`, `N1`, `N4` each now re-assert their own invariant across 6 generated synthetic fact VALUES (not the one hand-picked demo string) through the SAME write helpers every other check in this file already uses, cleaned up by the SAME written_fact_ids/finally mechanism. `DK2` closed BOTH its twin and metamorphic gaps — the twin needed zero new code: `L7:fault-injection` (the existing misseal-to-wrong-D_pub fault) already asserts maya, DK2's own rightful custodian, is locked out of her own dyad's fact under that fault — DK2's exact property, inverted — so its registry twin now reuses `{"scenario": "L7:fault-injection"}`, the same twin N2/DK3/P2 already reference. CHECKLIST-MECHANISM closed 2 checks' twin gaps: `MT1`/`MT1-CHECKLIST` — neither proved the admit/deny judge (`apply_injection_contract`) could actually say NO; added ONE fault-injection (not a new corpus member — REQ_CRYPTO_HARNESS_V2's own CONSTRAINT that MT1/MT2 must never be conflated is respected) asserting bob (enrolled, but not on alice's dyad for f1) is correctly DENIED the SAME base query alice's 12/168 variants all admit, via INJ-3's cross-member-deny rule (`harness/injection_contract.py:475-519`) — live-verified: `bob_admitted=False`. MT1-CHECKLIST's twin reuses MT1's new one (`{"scenario": "L7V2:MT1"}`) — same judge, same directional-flip property, same reuse pattern ABM-route/FF1/FF3/FF4's metamorphic entries already use. EXPLICITLY SKIPPED, not attempted: `P4`, `P4-EXT`, `P4-EXT-row12`, `P4-EXT-row13` (7 flagged lines) — all on `write_rule.classify`'s write-time directive/attribute path; Bill has an open ruling on whether write-time directives should check attribute, and closing these mutants would lock current behavior in as intended before he decides. Also not attempted this slice (deferred, not skipped-forever): `PS1`/`PS2` (retired v1-fixtures — rebuilding either would violate the post-destruction/no-master-key-touch constraint), `CT`/`CT-VECTOR-INDEX`/`CT-OUTPUT-GAP` (belong to the doc's priority-2 "semantic canary" mechanism, not this slice's CheckList/Hypothesis focus), `RI1`/`RE1-RE7`/`ABM-route`/`FF1`/`FF3`/`FF4`/`SC1`/`SC1-E2E`/`MT2-DECRYPT-REVOKE` (kept the slice at exactly 10 checks, per Bill's own "ten, not more, so the work stays reviewable"). VERIFIED LIVE: `L7: 24/24`, `L7V2: 27/28` (1 unchanged opt-in skip), `AUDIT: four-part-roster PASS` (56 checks enumerated, 0 missing, 35 debt-flagged gaps — down from 46), `RATCHET PASS` — checkpointed after every stage of this build (property generator, directional twin, each registry batch), not just once at the end. `harness/g0_invariant.py`, `harness/write_rule.py`, and every other file outside `eval/harnesslib/layer7_crypto.py`, `eval/harnesslib/layer7_crypto_v2.py`, and `eval/harnesslib/check_registry.py` is untouched. See `docs/dispatches/DISPATCH_TD133_BURNDOWN_ITEM5__checklist-and-property-based-ten-checks__v20260727_1731.md` for the full session record.** | REQ_HARNESS_DISCIPLINE audit build, 2026-07-26; burn-down (7 twins) same day; ITEM 5 first slice (10 checks) 2026-07-27 | OPEN (35 of original 46 remain; write_rule family untouched pending Bill's ruling) |
| TD-134 | GATE | **Mutation-score survivors (REQ_COVERAGE_MEASUREMENT Metric 2, `eval/harnesslib/mutation_score.py` + `mutation_targets.py`, wired as `L7V2:MUTATION-SCORE`): 50 of 128 generated mutants SURVIVED (score 0.61) across the four named gate modules — each is gate logic no current check exercises, located to file:line. NO-SILENT-DISAPPEARANCE RULE (REQ acceptance item 7): this list is the baseline. A later run's survivor count/list may only shrink if each removed entry is either (a) killed by a new/strengthened killer in `mutation_targets.py`, or (b) still present but explicitly carried forward under this ID with a note — a shrink with no accounting is a FAIL of the metric's own contract, not a quiet improvement. BASELINE, by module: **`harness/injection_contract.py` `_inj1_subject_scope`** (1/5 survived) — :413 `delete_last_operand(Or)`. **`_inj2_relevance`** (10/20 survived) — :443 `delete_last_operand(Or)`; :447 `flip_bool_const(True->False)`; :450 `delete_last_operand(Or)`; :450 `swap_compare Eq->NotEq`; :450 `delete_last_operand(And)`; :451 `swap_compare IsNot->Is`; :452 `swap_compare In->NotIn`; :453 `flip_bool_const(True->False)`; :460 `swap_compare In->NotIn`; :471 `flip_bool_const(False->True)`. **`_inj3_cross_member_deny`** (6/15 survived) — :505 `flip_bool_const(True->False)`; :508 `delete_last_operand(Or)`; :510 `delete_last_operand(Or)`; :514 `negate_if_test`; :517 `negate_if_test`; :518 `flip_bool_const(True->False)`. **`apply_injection_contract`** (1/9 survived, INJ-7 block only) — :619 `delete_last_operand(And)` (drops the `intent in _PERSONAL_INTENTS` clause from the refusal guard — no current killer exercises a non-personal-intent turn through this exact path). `_inj4_household` and `_inj5_never_volunteer`: 0 survivors (both 100%). **`harness/write_rule.py` `classify`** (19/34 survived, the weakest module) — :146 `delete_last_operand(Or)` x2; :166 `negate_if_test`, `swap_compare Is->IsNot`, `swap_compare Eq->NotEq`, `delete_last_operand(Or)`; :170 `negate_if_test`, `swap_compare Eq->NotEq`; :174 `negate_if_test`, `delete_last_operand(And)`; :178 `swap_compare In->NotIn`; :181 `negate_if_test`, `swap_compare Eq->NotEq`; :184 `negate_if_test`, `delete_last_operand(And)`; :200 `swap_compare In->NotIn`, `delete_last_operand(And)`; :205 `swap_compare In->NotIn`; :216 `delete_last_operand(And)`. **`harness/g0_invariant.py` `check_g0`** (1/8 survived — 4 killed 2026-07-27, see UPDATE below) — :67 `delete_last_operand(Or)` (confirmed EQUIVALENT MUTANT, see UPDATE — not a live coverage gap). **`eval/oracle/record_invariants.py`**: `g1_no_orphan_generation` (3/12 survived) — :62, :76, :78, all `delete_last_operand(Or)`; `g2_no_cross_owner_admission` (3/5 survived) — :95, :96, :97, all `delete_last_operand(Or)`; `g3_guard_implies_no_inference` (1/5 survived) — :117 `delete_last_operand(And)`; `g4_no_double_valued_attribute` (1/10 survived) — :171 `delete_last_operand(Or)`. PATTERN across nearly every survivor: `delete_last_operand` on a multi-clause `and`/`or` — the existing killer probes each exercise ONE clause's true/false state but none constructs a case where dropping the LAST clause specifically changes the outcome (e.g. a fact that satisfies every OTHER clause but only the dropped one would have blocked/allowed it). Closing these is mechanical per-site (one more killer probe per survivor) but not attempted in this build — Bill's dispatch scoped this session to building and verifying the metric itself, not burning down what it found. Fault-twin self-test (both directions) PASS, verified live: seeded mutant (`negate_if_test` @ `injection_contract.py:619`, the INJ-7 refusal condition) killed with its 3 designated killers present, survives with them excluded. `L7V2:MUTATION-SCORE`/`MUTATION-SCORE-SELFTEST` registered in `check_registry.py`, AUDIT roster clean (rows=53, missing=0). UPDATE 2026-07-27: the no-silent-disappearance rule stated above is no longer aspirational -- it is mechanically enforced every run. eval/harnesslib/mutation_score.py now persists each run's survivors to logs/mutation_survivors.jsonl and compares against the previous run's record; this exact baseline (78/128 killed, these 50 survivors) is what the first persisted run wrote, matching this entry number for number. A future run's survivor list may only shrink if each removal is killed (present in that run's own killed-mutant set) or still named here under this ID -- verified live via two real harness runs plus a four-direction synthetic self-test, wired as L7V2:MUTATION-NO-SILENT-DISAPPEARANCE and its SELFTEST twin. UPDATE 2026-07-27 (later, burn-down): four of the five `harness/g0_invariant.py:check_g0` survivors killed by four new killer probes added to `mutation_targets.py` -- `_kill_g0_resolved_without_admitted_fires` (:65 `delete_last_operand(And)` -- exercises resolved_subjects truthy / admitted_facts falsy, the non-empty-but-mismatched half of line 65's OR the module's own docstring names, never previously constructed by a killer), `_kill_g0_none_member_no_crash` (:68 `delete_last_operand(Or)` -- member=None, distinct from merely-empty, crashes the mutant on `.strip()`), `_kill_g0_none_resolved_subjects_no_crash` (:74, same None-vs-empty distinction on `list(resolved_subjects or [])`), `_kill_g0_none_admitted_facts_no_crash` (:75, same on `len(admitted_facts or [])`) -- each verified to return None (no false-kill) against the real unmutated check_g0 and to positively kill only its own targeted mutant. check_g0 score 3/8 (0.38) -> 7/8 (0.88); overall 78/128 (0.61) -> 82/128 (0.64), all other modules' survivor counts unchanged (verified via a full re-run of the sweep). The fifth survivor, :67 `delete_last_operand(Or)` on `find_named_tracked_persons(reply or "", known_subject_ids)`, was investigated and is a TRUE EQUIVALENT MUTANT, not a coverage gap: every falsy `reply` (only `None`/`""` are possible for a `str | None`-typed input) is caught identically by `find_named_tracked_persons`'s own `if not reply: return []` guard whether or not the `or ""` fallback ran first, and every truthy `reply` short-circuits `or` to the same value either way -- no input to `check_g0` observably distinguishes the mutant from the original. Killing it would require changing `check_g0` or `find_named_tracked_persons` behavior, out of scope for a mutation-testing burn-down (and outside this session's mandate not to change check_g0's behavior). `harness/g0_invariant.py` itself was not modified — only `mutation_targets.py` (killers) and this register entry changed. See `docs/dispatches/DISPATCH_G0_MUTATION_BURNDOWN__kill-five-survivors-of-check_g0__v20260727_1648.md` for the full session record. This entry (TD-134) itself is what a disappearing survivor gets checked against by name, so it should not be deleted or rewritten out from under the mechanism -- burning down an individual survivor means removing ONLY that survivor's own file:line/operator line above, not this entry's structure. | REQ_COVERAGE_MEASUREMENT Metric 2 build, 2026-07-26; g0_invariant burn-down 2026-07-27 | OPEN (baseline; enforcement mechanism live 2026-07-27; burn-down IN PROGRESS — 4/50 killed 2026-07-27 (harness/g0_invariant.py:check_g0), 46 remain) |
| TD-109 | SEC | BUILD REQUIREMENT CHG-8 - Biometric consent-and-retention control for on-device speaker recognition. Gates any public claim that speaker recognition has consent controls. Scope: (1) speaker recognition off by default; (2) per-speaker written or electronic consent at enrollment (or legally authorized representative); (3) consent screen must state: what is collected (local speaker embedding), purpose (speaker recognition, role permissions, household privacy controls), storage (local device only, encrypted at rest), sharing (not sold, not advertising, not model training, not uploaded unless separately authorized), retention (deleted on profile removal, consent withdrawal, purpose satisfied, or statutory maximum); (4) published biometric retention schedule: delete on request or profile deletion, delete after speaker-recognition disablement, delete no later than 3 years after last interaction where BIPA-style rules apply, device reset and household transfer deletion; (5) non-biometric fallback (PIN, passphrase, device possession, manual speaker selection); (6) audit enrollment, consent, deletion, and export events. | HIP_FinalizationOrder CHG-8/DECISION-3 | OPEN |
| TD-135 | OPS | **`whitepaper/archive/HIP_White_Paper_Augmented.docx` is corrupt and cannot be rendered.** Found building REQ_DOC_RENDERING (2026-07-27): the file is 150 bytes, a truncated zip (PK header present, no end-of-central-directory record — confirmed via `unzip -l`), unreadable by macOS `textutil` ("Error reading ... couldn't be opened", exits 0 anyway) and by extension unreadable by any docx tool. `git log --follow` shows it has been this way since its first commit (`4eee583`) — not a regression, a bad file from the start. `scripts/docx_to_text.sh` refuses to write a rendering for it rather than emit a misleading empty one; `docs/rendered/whitepaper/archive/HIP_White_Paper_Augmented.docx.md` does not exist and the AUDIT:DOC-RENDERING check reports it MISSING every run, by name, until this is closed. Not fixed here — recovering a truncated archive file isn't a rendering-tool problem, and the file is superseded archive content (`whitepaper/HIP_White_Paper_Augmented__v20260702_1113.docx` and later dated versions are the live lineage). Fix direction: replace with a good copy if one exists (Drive, another checkout), or delete it from `whitepaper/archive/` as dead weight and drop the corresponding gate exemption once it's gone. | REQ_DOC_RENDERING build, 2026-07-27 | OPEN |
| TD-136 | GATE | **[PORTED 2026-07-27 from `main` branch `TD-131`, commit `4390240`, per DISPATCH 36 register reconciliation phase 2 -- see `HIP_RegisterReconciliation__cross-branch-id-collisions__v20260727_1930.md`. Body verbatim from that point on; roadmap's own `TD-131` is a different, unrelated git-worktree-checkout finding and was not touched.]** Household facts reach the outbound Groq payload unfiltered on every MID and CORE turn. Mechanism: `harness/injection_contract.py`'s `_inj4_household` (lines 341-343) admits `owner=="household"` facts unconditionally, before INJ-5's intent gate. `server/voice_orch.py:2603` renders them into the system prompt; that `messages` object passes untouched to `_groq.chat.completions.create()` at line 3314. `strip_context_for_tier` at line 3240 is the only filter before a client call and is gated on `_frontier_strip`, which never fires for MID or CORE. Traced live on both tiers, 2026-07-22: five rows went verbatim -- address, zone_district, schedule, trash pickup, and Dad's risk_pattern (elevated fall-risk). Member-owned facts were correctly excluded in the same trace -- 8 candidates, 5 admitted, Maya's own appointment and medication and Ray's medication all denied. Not a defect in the sense of broken code -- the household exemption (INJ-4) is by explicit design, and member isolation holds. What is unresolved is whether that exemption should extend past the network boundary: today it does not distinguish "shared within this household" from "sent to an external provider's servers." Proposed resolution: extend `strip_context_for_tier` to MID and CORE, or accept the exposure and document it as an architecture decision. This is Bill's call, not a session's. Cross-reference: run-of-show correction at `9e61641`; backlog #48 (adjacent but distinct -- that item is about household state carrying across demo scripts within the local graph, not about the network boundary). No architecture-weakness register exists in this repo today to cross-reference; checked, not found. | Live trace, run-of-show correction session, 2026-07-22 | OPEN -- Bill's decision required |

## Notes
- 2026-07-09 care_coordination script note (CORRECTED by Layer 2 first sweep): sarah is the SPEAKER of T04, not the queried subject, and she exists in scripts/dev_seed_registry.py but NOT in the live member registry — /api/text-query returns 400 "unknown member" and the scripted turn cannot fire at all. Fix fork: register sarah on the live path, or change T04's speaker to maya/sam. Pinned as known-failure L2:care_coordination.T04. (The original version of this note mischaracterized the failure as an INJ-7 refusal-type issue.)
- 2026-07-09 epistemic timeline note: the epistemic timeline (and vault) accumulate state across demo runs in the same browser session. Without a reset between runs (POST /api/reset), a second LOAD shows chains from the prior run, which makes the timeline inconsistent with the current dialogue. Operationally: always POST /api/reset before a fresh demo run; the dashboard currently has no auto-reset on LOAD. Consider adding one.
- 2026-07-09 launchd note (pattern-match to TD-103): `launchctl bootstrap gui/501 com.hip.demo.dashboard.plist` on the Mini failed with "Bootstrap failed: 5: Input/output error" after a bootout during F-6 deploy — same I/O-error-5 signature TD-103 records for com.hip.voice.orch. Dashboard is currently running as a MANUAL process (`.venv/bin/python -m server.demo_dashboard --host [REDACTED-TAILNET-ADDRESS] --port 7871`, log /tmp/hip-dash.log) started over SSH; it serves the pulled code fine but will NOT survive a Mini reboot. Re-bootstrap the plist (or extend TD-103's start_manual.sh approach to the dashboard) before the next demo.
- TD-101 is the highest-severity open item; it sits in the STALE security audit. Engine-track, but do not let a public-facing demo run against unauthenticated endpoints.
- TD-103 blocks the voice track and the TD-042 mic test until closed. The 2026-07-09 dashboard note above suggests the I/O-error-5 failure is a machine-level launchd condition, not plist-specific.
- TD-108 is the primary liability-severity reducer for the platform. The per-fact consent ledger caps breach blast radius. It must ship before scale, not after. See CHG-1 in HIP_PropagationWorkOrder and Ecosystem NDA Section 4.3 for the full liability argument.
- TD-109 is a build requirement, not a debt item. No public claim about speaker-recognition consent controls is accurate until TD-109 ships. The CHG-3 wording in the NDA states the architecture fact only; TD-109 is the control that backs any future consent claim.
